# 内部图片生产工具 UX 优化执行计划（V1.x 体验收口）

> **文档性质**：本文档是 UX 优化阶段的**唯一需求来源**，共 41 项，全部已经用户逐条确认。
> 执行会话以本文档为 goal，不需要重新征求方案层面的同意；仅当发现本文档与代码现状冲突、
> 或某项存在文档未覆盖的技术决策时，才停下向用户确认。
> 编号 E01–E41 与评审结论一一对应，禁止合并语义、禁止静默砍项。

---

## 0. 执行方式（给执行会话的硬性指令)

1. **遵守&#x20;********`CLAUDE.md`********\*\*\*\* 全部铁律**：业务真相只在 Rust；前端只经 `src/lib/ipc/` 出入；
   事件驱动不轮询；token 只从 `globals.css` 取；非测试代码禁 unwrap/expect/panic。
2. **分支策略（用户已书面确认，本阶段对 CLAUDE.md「一任务一分支一 PR」的特例豁免）**：
   全部 41 项在**同一个分支**（建议 `feat/ux-overhaul`）上一次性完成，最后一次 PR 合入 main，
   **不拆 41 个分支**。作为交换必须**小步提交**：每完成一项（或同组数项）一个 commit，
   message 注明条目编号（如 `feat(review): E01 验收重试+微调`），保证可 bisect、可单项回滚。
3. **门禁节奏（从简但分段）**：不要求每项单独跑全门禁；**每个里程碑收尾跑一次 \*\*\*\*\*\*\*\*****`pnpm check`**
   （M6/M7/M8/M9 共 4 次），全部完成后再全量跑最后一次。红了先修再进入下一里程碑。
4. **测试要求（两档）**：涉及引擎/数据层/文件删除的条目（E02、E03、E05、E16、E17、E18、E22、E40
   及同类）测试随实现写——这些碰钱碰数据，最后冒烟看不出错；纯 UI 条目**不要求单测**，
   由收尾的全局冒烟覆盖。禁止为让 CI 变绿修改/删除/放宽既有测试断言。
5. **IPC 变更**：新增/改动命令或事件后在 `specta_builder()` 登记，
   `cd src-tauri && cargo test --lib export_bindings` 重新生成并提交 `bindings.ts`。
6. **执行顺序与断点续做**：按 M6 → M7 → M8 → M9 顺序推进，里程碑内按条目顺序。
   **每完成一项立即把本文档对应&#x20;********`[ ]`********\*\*\*\* 勾为&#x20;********`[x]`********\*\*\*\* 并随该 commit 提交**——上下文耗尽时，
   新会话读本文档即可从第一个未勾条目继续，不需要交接说明。
7. **需求文档同步**：标注「📄需求修订」的条目，在同一 commit 内同步修改
   `内部图片生产工具_V1需求文档.md` 对应章节。
8. **收尾（全部勾完后，见 §5）**：全量 `pnpm check` → 八页全局冒烟（preview 走通
   配 Key→生成→任务→验收→作品→废纸篓闭环 + 本次新增交互）→ 全新上下文 `/code-review high`
   → 发现项修复 → 一次 PR 合入 main。
9. 新增 Rust/前端依赖须过 `cargo audit` / `pnpm audit`，与现有豁免流程一致。

---

## 1. 背景与第一性目标

本软件的存在目的：**批量生产合格图片素材，供短视频/图文发布使用**。
V1（M0–M5）已完成功能闭环，但只覆盖了需求文档的字面目标。本阶段从真实使用体验出发收口，
所有条目服务于三个北极星指标：

| 北极星     | 含义                  | 主要对应条目                                |
| ------- | ------------------- | ------------------------------------- |
| 每小时合格产出 | 减少验收/重试/重复配置的摩擦     | E01, E07–E09, E16, E17, E24, E32, E38 |
| 无人值守可走开 | 跑批时人不必守在软件前         | E04, E05, E18, E26, E31               |
| 资产安全    | 花钱生成的图与积累的提示词不可意外丢失 | E02, E19, E21, E22, E40               |

---

## 2. 已确认的产品决策（执行时直接采用，不再讨论）

### D1 · 生成参数优先级（关联 E16）

- 参数（尺寸/比例/质量等）**默认全部空选 = 不向 API 传该参数**，以提示词内容和模型默认为准。
- 用户在软件中显式设置了某参数时，**软件设置优先于提示词中的同类描述**（即照常传参，由 API 侧覆盖）。
- UI 必须显著区分「未设置（跟随提示词）」与「已设置」两种状态（如未设置显示虚线占位「跟随提示词」）。
- 批次 `params_json` 快照记录最终生效的显式参数集，任务页可回查。

### D2 · 抽卡次数（关联 E17，📄需求修订 §8/§14.3）

- 定义：**同一「参考图 × 提示词」组合独立生成 k 次**，每次一个独立任务。
- 任务总数 = Σ(参考图 × 挂靠组提示词数) × 抽卡次数 k；k 默认 1，生成页可调。
- **输出文件命名追加抽卡序号**：`参考图名_日期_提示词编号_序号`（如 `productA_260708_DZ0001_2.jpg`），
  否则同组合多张通过时文件名冲突。需求文档 §14.3 同步修订。
- 统计口径（关联 E25）：提示词「合格率」按**组合是否至少产出一张通过图**计算，
  避免抽卡稀释合格率数据。

### D3 · 批次与废纸篓生命周期（关联 E22、E40）

- 批次全部任务达终态（成功/失败/删除/取消均算）→ **立即归档**（现状已如此，保持）。
- **归档满 30 天的批次自动删除**（批次 + 任务记录级联；作品是独立快照，不受影响——
  此边界须写进确认文案/设置说明）。
- 废纸篓项目**默认保留 30 天后自动清理**，设置页可调天数/可关闭。
  注意与 E02（未通过图保留原图至清理时）叠加后，该保留期即原图的实际缓冲期，须在设置文案写明。

---

## 3. 任务清单（按里程碑）

> 优先级：`P0` 准 bug / 需求缺口 / 数据安全；`P1` 核心生产力；`P2` 完善与长期运维。
> 标记 `⚡后端现成`：后端命令/参数已存在，仅需前端接入，成本极低。

### M6 · 安全与合规修正（全 P0，最先执行）

建议提交分组：E01 ｜ E02(+E40 前置) ｜ E23 ｜ E29+E30a ｜ E19，各一个 commit。

- [x] **E01 · 验收页「重试 + 微调提示词」** `P0` `⚡后端现成` 📄对应需求 §13.2（已有要求，属实现缺口）
  - 现状：需求明文要求验收三操作（通过/不通过/重试可微调），`retry_task(id, edited_prompt)`
    （`src-tauri/src/commands/tasks.rs`）已支持微调写快照，但 `ReviewPage.tsx` 只有通过/不通过。
  - 做法：验收网格与大图模式均增加「重试」入口，弹出提示词编辑框（预填当前快照文本），确认后调
    `retryTask(id, editedText)`；任务回队后从验收列表移除。
  - 验收标准：大图模式对一张待验收图微调提示词并重试，任务重新生成；再次通过验收后，
    微调版本按既有 R8 逻辑写回提示词库（`accept_tasks` 已实现，验证联动即可）。
- [x] **E02 · 未通过图片保留原图至废纸篓清理** `P0`
  - 现状：验收点「不通过」立即物理删除原图，仅留缩略图；误触 = 花钱生成的图永久消失。
  - 做法：reject 时原图移入废纸篓暂存区（不删除物理文件），**「彻底删除/清空废纸篓」时才物理删除**；
    废纸篓行项可查看原图。📄需求修订 §13.4（「不保留原图」改为「原图暂存至废纸篓清理」）。
  - 验收标准：不通过一张图 → 废纸篓能看到原图；清理后物理文件消失、编号回收（既有行为）。
- [x] **E23 · 验收大图 1:1 查看（修复 cover 裁剪）** `P0`（准 bug）
  - 现状：`ReviewPage.tsx` 大图用 `background-size: cover`，图片边缘被直接裁掉，
    无法检查 AI 图高发的手部/文字/边缘缺陷。
  - 做法：大图改为 `<img>` contain 显示 + 滚轮缩放（10%–400%）+ 拖拽平移 + 双击在 fit/100% 间切换。
  - 验收标准：非正方形图完整可见无裁剪；可放大到 100% 查看局部并拖动。
- [x] **E29 · 验收页按批次筛选** `P0` `⚡后端现成`
  - 现状：`listPendingReview(batchId)` 支持过滤，前端写死传 `null`，多批次混排验收。
  - 做法：验收页顶部加批次筛选器（含「全部」），交互与任务页批次切换器一致。
  - 验收标准：两个批次各有待验收项时，可只看其中一批。
- [x] **E30a · 参考图导入时选择分组** `P0` `⚡后端现成`
  - 现状：`importRefImages(paths, groupId)` 支持带组导入，`RefsPage.tsx` / `GeneratePage.tsx`
    均传 `null`，导入后全落「未分组」需逐张改。
  - 做法：参考图库导入时弹分组选择（含「未分组」「新建分组」）；生成页快捷上传可保持默认未分组。
  - 验收标准：从参考图库导入 3 张图并选定分组，图直接归入该组。
- [x] **E19 · 数据备份与数据目录可见性** `P0`
  - 现状：SQLite + 资产目录位置对用户完全不可见，无任何备份手段；磁盘故障 = 全部资产丢失。
  - 做法：设置页新增「数据」区块——显示数据目录路径 + 「打开目录」按钮 +
    「导出备份」（DB 检查点后与资产目录打包 zip 到用户选定位置，进行中显示进度，队列运行中禁用并提示）。
  - 验收标准：导出的 zip 解包后含数据库与资产文件；备份期间数据一致（备份前 `wal_checkpoint`）。

### M7 · 生成与调度增强（P1 为主）

建议提交分组：E16 ｜ E17+E31（决策 D2 要求同组）｜ E03+E12 ｜ E11+E18 ｜
E05+E06 ｜ E07+E32 ｜ E04 ｜ E26，各一个 commit。

- [x] **E16 · 生成参数 UI（按决策 D1）** `P1` 📄需求修订 §9
  - 现状：`GeneratePage.tsx:99` 硬编码 `paramsJson: "{}"`；`provider/openai.rs` 写死 `n=1` 且不传 size。
  - 做法：生成页参数区（尺寸/比例、质量，均默认「跟随提示词」空选）；provider 仅透传显式设置的参数；
    批次快照记录生效参数；任务页批次信息可查看。
  - 验收标准：不设参数时请求体无 size/quality 字段（wiremock 断言）；设置后正确透传。
- [x] **E17 · 抽卡次数（按决策 D2）** `P1` 📄需求修订 §8、§14.3
  - 做法：生成页「抽卡次数 ×k」（默认 1，上限建议 5）；createBatch 按组合 ×k 展开任务；
    输出命名追加抽卡序号；genbar 任务数公式同步显示 `组合数 × k`。
  - 验收标准：2 参考图 × 3 提示词 × 抽 2 = 12 任务；同组合两张全通过时输出文件名不冲突。
- [x] **E31 · 「开始生成」确认摘要（与 E17 同组提交）** `P1`
  - 做法：点「开始生成」先弹确认卡：任务总数（含抽卡展开式）、参与的启用 Key 列表、
    预计请求数、预计耗时（按历史单张均值估算，无历史则不显示）。确认后才 createBatch。
  - 验收标准：确认卡数字与实际创建的批次任务数一致；取消不产生批次。
- [x] **E03 · 取消批次剩余任务** `P1`
  - 现状：只有暂停；`delete_task` 仅拦生成中/重试中，排队任务本可删但 UI 只对失败态显示删除。
  - 做法：新增 `cancel_batch_pending(batch_id)` 命令（单事务删除该批次全部 `q` 态任务，
    回收编号，重估归档，补发汇总）；任务页有排队任务时显示「取消剩余任务 · N」+ 确认框。
  - 验收标准：500 任务批次跑到一半取消，`q` 态全清、在途任务不受影响、批次汇总正确。
- [x] **E12 · 开始生成前置检查（与 E03 同组提交）** `P1`
  - 做法：无启用 Key（或启用 Key 并发合计为 0）时，genbar 按钮禁用并显示
    「无可用 API Key · 去设置」链接。
  - 验收标准：停用全部 Key 后按钮不可点且有引导；启用后恢复。
- [x] **E11 · API Key「测试连接」** `P1`
  - 做法：添加/编辑 Key 弹窗与 Key 行内增加「测试」按钮，复用 provider 客户端发一次最小请求
    （或该端点最廉价的探活方式），返回成功/具体错误（走 E35 的人话映射）。
  - 验收标准：错误 baseUrl 测试报可读错误；正确配置测试通过（wiremock 覆盖两种场景）。
- [x] **E18 · Key 级熔断 + RPM 限流** `P1` 📄需求修订 §9.2/§11
  - 做法：①同一 Key 连续 N 次（建议 3）Auth/欠费类失败 → 自动临时停用该 Key + 系统通知 +
    设置页显示「已熔断」状态与恢复按钮；②Key 配置增加可选「每分钟请求上限（RPM）」，
    调度器按滑动窗口限速（既有 per-Key Semaphore 之外的第二道闸）。
  - 验收标准：wiremock 模拟连续 401 → Key 被熔断且任务切到其他 Key；RPM=2 时 1 分钟内不超 2 请求。
- [x] **E05 · 队列全局熔断** `P1`
  - 做法：**跨 Key** 连续 N 个任务失败（建议 10，设置页可调）→ 自动暂停队列 + 系统通知
    「已自动暂停：连续失败 N 次」，任务页 banner 说明原因并提供「检查设置 / 继续队列」。
  - 验收标准：模拟全 Key 失效，队列在阈值处停住而不是烧完 500 任务。
- [x] **E06 · 按错误类型分组重试** `P1`
  - 做法：任务页失败任务按六类错误分组显示计数；「重试全部失败」默认**排除 ContentPolicy 类**
    并在按钮旁注明（如「重试全部 · 12（不含违规 3）」）；违规类单独引导走改词重试（E34）。
  - 验收标准：混合失败类型下，重试全部不触碰违规任务。
- [x] **E07 · 生成页配置持久化 + 再来一批** `P1`
  - 现状：`selGroupIds/selRefIds/mapping` 全是组件 state，切页/重启即丢。
  - 做法：①生成页选择状态入 Zustand 并持久化（经 Rust settings 或本地存储——注意铁律：
    业务真相在 Rust，此处属 UI 态，允许前端持久化）；②任务页批次行增加「按此配置再来一批」，
    从批次快照还原选择并跳转生成页。
  - 验收标准：配好组合后重启应用，生成页选择仍在；对历史批次一键还原配置。
- [x] **E32 · 挂靠记忆（与 E07 同组提交）** `P1`
  - 做法：记录每张参考图最近一次挂靠的提示词组（`ref_id → last_group_id`，Rust 侧落库）；
    生成页选中参考图时若该组在本次已选分组中则自动预填，可手动改。
  - 验收标准：上批 A 图挂 X 组；新批选 A 图与 X 组后挂靠自动填好。
- [x] **E04 · 批次进度 + ETA + 系统通知 + 角标** `P1`
  - 做法：①任务页顶部批次总进度条（终态数/总数）+ 预计剩余时间（历史单张均值 × 剩余数 ÷ 有效并发）；
    ②接入 `tauri-plugin-notification`：批次完成、失败达到阈值、熔断触发（E05/E18）时发系统通知；
    ③macOS Dock / Windows 任务栏角标显示运行中任务数（或待验收数，取一个语义并在设置说明）。
  - 验收标准：窗口失焦时批次完成能收到系统通知；进度条与汇总徽章一致。
- [x] **E26 · 关闭窗口拦截 +（可选）托盘后台** `P1`
  - 说明：关闭拦截确认已实现；「托盘后台」为可选二期，本阶段不做（未静默砍，标注延后）。
  - 做法：`on_window_event` 拦截 CloseRequested——有 run/retry 任务时弹确认
    「仍有 N 个任务生成中，退出将中断」；确认退出走既有中断恢复路径。
    可选二期：macOS 关窗不退出（常规行为）、Windows 最小化到托盘继续跑。
  - 验收标准：跑批中点关闭出现确认；空闲时直接关闭无打扰。

### M8 · 验收工作台（P1 为主）

建议提交分组：E09+E38 ｜ E08+E28 ｜ E24 ｜ E34+E35 ｜ E10，各一个 commit。

- [x] **E09 · 验收网格键盘流** `P1`
  - 做法：网格模式支持方向键移动焦点（有可视焦点环）、空格切换选中、⏎ 通过焦点项/所选、
    ⌫ 不通过（走 E02 后进废纸篓，风险已降级）、`Z` 或双击进大图。hintbar 同步更新。
  - 验收标准：全程不碰鼠标可完成一轮粗筛。
- [x] **E38 · 「待定」标记 + 范围多选（与 E09 同组提交）** `P1`
  - 做法：新增待定标记（快捷键 `S`，仅 UI 态即可，不需入库为业务状态）——待定项排序沉底、
    角标标识、可筛选「仅看待定」；网格支持 shift 范围选择与 ⌘A 全选当前筛选。
  - 验收标准：待定 5 张后它们集中在列表尾部；shift 点选可一次选中连续 20 张。
- [x] **E08 · 大图模式参考图对比** `P1`
  - 做法：大图侧栏显示该任务参考图缩略图，点击或**按住空格**在生成图/参考图间切换对比
    （松开回到生成图）。
  - 验收标准：大图模式可随时看到/切换到参考图。
- [x] **E28 · 验收暗场（与 E08 同组提交）** `P2`
  - 做法：大图精审模式使用深色背景环境（遮罩加深、侧栏用深色 token 变体），
    不做全局深色（仍留 V2）；颜色值一律经 `globals.css` 新增 token，遵守铁律 5。
  - 验收标准：大图模式下图片四周为暗场；reduced-motion 下过渡正常降级。
- [ ] **E24 · 验收排序：按参考图 / 按提示词组** `P1`
  - 做法：验收页增加排序切换（默认时间序 / 按参考图聚类 / 按提示词组聚类），聚类时插入分段小标题。
  - 验收标准：按提示词组排序时，同组图片连续排列且有组名分隔。
- [ ] **E34 · 任务页失败任务「改词重试」** `P1` `⚡后端现成`
  - 做法：失败行操作区在「重试/删除」旁加「改词重试」，弹提示词编辑框（预填快照），
    确认调 `retryTask(id, editedText)`。ContentPolicy 类失败将此按钮设为主按钮。
  - 验收标准：违规失败任务改词后重试成功，微调文本进入任务快照。
- [ ] **E35 · 错误信息人话化 + 建议动作（与 E34 同组提交）** `P1`
  - 做法：在六类错误基础上建立常见错误映射表（Rust 侧 classify 时输出 `hint` 字段更佳）：
    401/无效 Key、insufficient\_quota、429、超时、content\_policy…每条给「人话描述 + 建议动作」，
    建议动作渲染为可点链接（去设置/改词重试/重试）。失败行可展开查看完整原始报错（同时覆盖 E10 的错误详情）。
  - 验收标准：模拟 401 的失败任务显示「Key 无效或已过期 → 检查设置」且链接可跳转。
- [ ] **E10 · 任务页信息可达性** `P1`
  - 做法：①任务列表增加搜索框（按参考图名/提示词编号过滤）；②批次切换器每行显示创建时间 +
    首图缩略图 + 可选备注名（批次表加 `note` 字段 + `rename_batch` 命令，行内可编辑）；
    ③错误详情展开由 E35 覆盖。
  - 验收标准：可按提示词编号搜到任务；批次列表能一眼区分不同日期的批次并可命名。

### M9 · 资产管理与长期运维（P2 为主）

建议提交分组：E20+E36 ｜ E37+E14 ｜ E30b+E41 ｜ E21+E33 ｜
E15+E25 ｜ E22+E40 ｜ E13+E27+E39，各一个 commit。

- [ ] **E20 · 分组管理** `P1`
  - 现状：分组只能靠导入创建；无重命名/删除/合并，提示词不能移组；标签导入后无处筛选。
  - 做法：新增分组命令：重命名、删除（组内提示词入废纸篓或先要求移空，取前者+确认框）、
    合并（A 并入 B，编号前缀保留原值不重编）、移动提示词到指定组；提示词库页增加按标签筛选。
  - 验收标准：可完成建错组改名、两组合并、单条提示词移组；标签筛选正确。
- [ ] **E36 · 提示词批量操作（与 E20 同组提交）** `P2`
  - 做法：提示词库支持多选（点选 + shift 范围），批量收藏/删除/移动分组。
  - 验收标准：一次选中 10 条移入另一组。
- [ ] **E37 · txt 导入格式帮助 + 行号级报错** `P2`
  - 做法：导入弹窗内嵌格式示例（分组/标签/正文语法）+「保存模板 txt」；
    解析失败报具体行号与原因（importer 已两段式解析，补充错误定位输出）。
  - 验收标准：导入一个第 14 行缺分组标记的文件，报错含行号。
- [ ] **E14 · 拖拽导入 + 生成页导入走预览（与 E37 同组提交）** `P2`
  - 做法：①参考图库/生成页接受拖入图片文件，提示词库/生成页接受拖入 txt（Tauri file-drop 事件）；
    ②生成页导入 txt 改为与提示词库一致的 `ImportPreview` 确认弹窗后再 commit（现为直接入库）。
  - 验收标准：拖 txt 进生成页出现预览确认；取消不产生临时分组。
- [ ] **E30b · 参考图批量操作 + 导入去重** `P1`
  - 做法：参考图库多选批量删除/改分组；导入时按内容 hash 检测重复（含与库内已有图重复），
    弹窗列出重复项默认跳过、可选仍导入。
  - 验收标准：重复导入同一张图被识别并默认跳过。
- [ ] **E41 · 参考图导入规范化（与 E30b 同组提交）** `P2`
  - 做法：导入时超限图片自动压缩副本用于上传（长边缩至如 2048、转 JPEG，阈值常量化），
    原图保留用于展示；任务上传使用压缩副本。📄需求修订 §5 备注。
  - 验收标准：导入 12MB 手机原图后，任务请求体使用压缩副本；库内展示不受影响。
- [ ] **E21 · 作品↔输出文件一致性** `P2`
  - 做法：作品库检测 `imagePath` 文件缺失（懒检测：打开详情/打开文件夹失败时标记），
    卡片显示「源文件缺失」角标 + 「重新导出」（从资产区快照重写输出文件）；
    「删除作品」确认文案明确是否删除输出文件夹中的文件（决策：**默认不删外部输出文件**，文案写明）。
  - 验收标准：手动删掉输出文件后，作品可重新导出；打开文件夹失败有明确提示而非静默。
- [ ] **E33 · 作品复刻回路（与 E21 同组提交）** `P2`
  - 做法：作品详情增加：「复制提示词」（一键复制原文）、「复制图片」（写系统剪贴板）、
    「用此配置再生成」（带该提示词 + 参考图预填生成页，可调抽卡数）。
  - 验收标准：从一张作品出发，三次点击内发起一批变体生成。
- [ ] **E15 · 作品批量操作 + 输出入口 + 请求量统计** `P2`
  - 做法：①作品库多选批量导出到指定文件夹/批量收藏/批量删除；②批次完成通知与任务页批次行
    增加「打开输出文件夹」；③批次汇总记录实际请求次数（含重试）并在批次信息展示。
  - 验收标准：多选 20 张导出到桌面文件夹成功；批次行一键直达输出目录。
- [ ] **E25 · 提示词效果统计 + 生产总览（与 E15 同组提交）** `P2`
  - 做法：①分组卡片显示合格率（按 D2 口径：组合至少一张通过），提示词详情显示历史产出数/合格率；
    ②生成页顶部极简总览条：今日生成 x 张 · 通过率 y% · 今日请求 z 次。
  - 验收标准：统计数字与库内记录一致（SQL 聚合有集成测试）。
- [ ] **E22 · 批次生命周期（按决策 D3）** `P2`
  - 做法：①批次切换器归档批次默认折叠/按月分组；②启动时清理归档满 30 天的批次
    （批次+任务级联删除，作品不动），设置页显示策略说明与天数配置。
  - 验收标准：造一条 31 天前归档批次，启动后被清理且作品仍在。
- [ ] **E40 · 废纸篓保留期自动清理（与 E22 同组提交，按决策 D3）** `P2`
  - 做法：设置页「废纸篓保留天数」（默认 30，可关）；启动时自动清理到期项
    （复用既有 purge 逻辑：物理删 + 级联 + 编号回收）。文案注明这是未通过原图（E02）的实际缓冲期。
  - 验收标准：到期项启动自动清理，编号回收正确。
- [ ] **E13 · 首次使用引导** `P2`
  - 做法：首启（或资产全空时）显示 checklist 卡片：配 Key → 导提示词 → 传参考图 → 开始生成，
    每项完成自动勾选并可点击跳转对应页；全部完成后永久消失（状态入 settings）。
  - 验收标准：全新数据目录启动可见引导，四步完成后消失且不再出现。
- [ ] **E27 · 日志与诊断入口（与 E13 同组提交）** `P2` `⚡后端现成`
  - 做法：设置页加「打开日志目录」（`openLogsDir` 命令已存在）+「复制诊断信息」
    （版本/OS/Key 数量与状态/最近 5 条错误摘要，写剪贴板）。
  - 验收标准：两个按钮均可用，诊断信息不含 Key 明文。
- [ ] **E39 · 全局快捷键 + 帮助面板（与 E13 同组提交）** `P2`
  - 做法：`⌘/Ctrl+1–8` 切换八页；`?`（或 `⌘/`）呼出快捷键速查面板（含验收页全部快捷键）；
    输入框聚焦时不劫持。
  - 验收标准：任意页面 ⌘3 可跳转；帮助面板列出全部快捷键。

---

## 4. 横切注意事项

- **状态机不扩容**：E38 待定是纯 UI 态；E03 取消是删除 `q` 态任务而非新状态；
  八态状态机与 5 视觉组映射保持不变（若确需变更，先停下与用户确认）。
- **事件驱动**：进度/汇总/熔断状态一律走既有 Tauri 事件通道（250ms 节流），禁止前端轮询。
- **视觉**：新 UI 元素（焦点环/暗场/角标/进度条）颜色一律新增 `globals.css` token；
  动效尊重 `prefers-reduced-motion` 与设置页开关。
- **文案**：所有确认框/破坏性操作文案须写明后果边界（作品是否受影响、能否恢复），
  参照 D3 中的既定表述。
- **新依赖预估**：`tauri-plugin-notification`（E04）；剪贴板图片写入（E33，Tauri clipboard 插件）；
  zip 打包（E19，Rust `zip` crate）。均需过 audit。

## 5. 完成定义（整体）

1. E01–E41 全部勾选，或个别项经用户书面同意降级/移入 V2 backlog（在本文档条目处注明原因）。
2. 单分支全程小步提交（每项/每组一个 commit，注明 Exx 编号）；每个里程碑收尾 `pnpm check` 全绿；
   引擎/数据层/文件删除类条目的测试随实现提交（§0.4 两档要求）。
3. 收尾三连全部通过：全量 `pnpm check` → 八页全局冒烟（核心闭环 + 全部新增交互逐条过）
   → 全新上下文 `/code-review high`（发现项修复后才可合并），最后一次 PR 合入 main。
4. 📄需求修订项全部落实到需求文档，本文档与需求文档无矛盾。
5. `CLAUDE.md` 里程碑进度追加 M6–M9 条目。
6. 全部完成后按既有发布链发版（建议 v0.3.0），发布说明按里程碑归纳用户可感知变化。

