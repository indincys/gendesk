# 内部图片生产工具（GenDesk）V1 开发执行计划

> 生成于 2026-07-09。执行者：Claude Code（代码 100% 由 AI 编写）。
> 三份输入的权威边界：
> - **功能与业务规则** → 《内部图片生产工具_V1需求文档.md》
> - **技术基准** → 《内部图片生产工具_V1技术选型定稿.md》（2026-07-09 修订版，含 R1–R9 修订记录）
> - **视觉与交互** → `图片生成工具原型设计-handoff.zip`（原型 `GenDesk 图片生产工具.dc.html` 为像素级基准；《设计交付_Design Tokens.md》为 token 基准）
>
> 冲突裁决顺序：需求文档 > 原型 > 技术文档。本计划已消化 2026-07-09 的全部冲突修订，执行时无需再回读修订过程。

---

## 0. 开工前置事实（执行时直接依赖，勿再推导）

1. **任务组合模型（R1）**：每张参考图挂靠一个提示词组，任务数 = Σ(参考图 × 其挂靠组提示词数)。不是全笛卡尔积。
2. **参考图有分组（R2）**：`ref_images.group_id` 引用 `prompt_groups`，与提示词库共用分组。
3. **窗口是自绘标题栏（R3）**：macOS `titleBarStyle: Overlay + hiddenTitle`；Windows `decorations: false` + 自绘窗控按钮 + drag region。最小窗口 1140×700。
4. **任务进度是阶段型伪进度（R4）**：生图 API 无真实进度。排队 0% → 请求发起 10% → elapsed/expected 线性到 90% 封顶（expected = 该 Key 近 20 次成功耗时均值，无记录用 60s）→ url 下载阶段 90–98% → 落盘 100%。
5. **Base URL 已含 /v1（R6）**：端点 = `{base_url}/images/edits`。
6. **微调重试（R8）**：微调文本写入 `tasks.prompt_text_snapshot` 供重试；**验收通过时**才写回 `prompts.text` 并置 `edited=1`。
7. **临时分组（R7）**：生成页导入 txt 产生 `is_temp=1` 分组；该组任一提示词首次验收通过 → 整组转正式（is_temp=0），toast 提示"已自动写入提示词库"。
8. **编号规则**：`prompts.code` 格式 `前缀-0001`（如 `DZ-0001`）；输出文件名中去掉连字符：`参考图名_YYMMDD_DZ0001.JPG`（如 `productA_260708_DZ0001.JPG`）。编号发放优先取回收池，与业务写操作同事务。
9. **八态状态机**：待生成 q → 生成中 run →（重试中 retry → run）\* → 成功（即待验收 rev）→ 已通过 pass / 未通过 rej；失败 fail 为可手动重试的终态。UI 按 5 视觉组渲染：q=灰、run/retry=蓝(spinner)、fail=红、rev=琥珀、pass=绿 / rej=灰。
10. **删除语义**：未通过/删除的内容进废纸篓（保留缩略图+提示词记录，原图删除）；废纸篓清理 = 物理删文件 + 级联删记录 + 编号回收，不可恢复。通过作品长期保留。

---

## 1. 开发工作流与质量控制体系（AI 全代码专项设计）

> 前提：本项目代码的生成、debug、审查全部由 AI 完成，没有人工经验兜底。因此质量体系的设计原则是：
> **① 凡是能机器强制的规则，绝不依赖 CLAUDE.md 散文约定**（AI 在长会话中会遗忘散文规则，但绕不过 CI）；
> **② 针对 AI 特有失效模式设防**：契约漂移、自证测试（写代码和写测试是同一个 AI）、同上下文审查偏差、为过 CI 而削弱测试。

### 1.1 基本节奏

- **小步提交 + 分支 PR 流**：一个任务 = 一个分支 = 实现 + 测试 + 门禁全绿 + 审查通过 = 一次 merge。main 分支受保护（check.yml 必须全绿才可合并），即使单人开发也走 PR——这是 AI 无法绕过的结构性约束。
- **引擎逻辑先写测试再实现**（状态机/调度/重试/恢复/号池）。HTTP 层一律对 wiremock 跑，不碰真实 API。
- **测试完整性规则**：禁止为让 CI 变绿而修改/删除/放宽既有测试断言；确需改动测试必须在 PR 描述中单独说明理由，审查 pass 重点核对。
- **业务真相只在 Rust**：前端不持有可变业务状态，Zustand 只做事件镜像与 UI 态。
- **视觉还原**：以原型 HTML 源码为准（不截图猜测），token 全部走 `styles/globals.css` CSS 变量。

### 1.2 第一层：编译期与静态强制（每次提交，pre-commit 快速子集 + CI 全量）

| 机制 | 防御的失效模式 |
| ---- | ------------- |
| **IPC 绑定自动生成（specta + tauri-specta）**：`src/lib/ipc/bindings.ts` 由 Rust 命令/事件签名生成，**禁止手改**；CI 重新生成并 diff，不一致即红 | 契约漂移——把"两边字段名靠约定对齐"整类问题从人为约定升级为编译期保证 |
| **sqlx 编译期查询校验**（offline 模式，`.sqlx` 快照入库，CI 跑 `cargo sqlx prepare --check`） | SQL 与 schema 漂移在编译期暴露，而不是运行时 |
| clippy `-D warnings` + `unwrap_used`/`expect_used`/`panic` 在非测试代码 deny（Cargo.toml lints 配置，非散文约定） | AI 顺手写 unwrap 导致运行时崩溃 |
| **guardrails 脚本**（`scripts/guardrails.sh`，进 CI）：`invoke(`/`listen(` 只许出现在 `src/lib/ipc/`；`oklch(` 硬编码只许出现在 `globals.css`；禁 `console.log`；禁无说明的 `#[allow(...)]`；日志代码中出现 `sk-` 模式即红 | 架构铁律与脱敏规则的机器化——CLAUDE.md 里每条"禁止"都必须在这里有对应检查 |
| biome ci + `tsc --noEmit`（strict 全开）+ `cargo fmt --check` | 基础卫生 |
| pre-commit hook（lefthook）：fmt + clippy + tsc + guardrails（秒级子集），完整门禁在 CI | 防止坏提交进历史 |

### 1.3 第二层：自动化测试（每次提交）

- `cargo test` + `vitest run` + `pnpm build` + `cargo check`（既有门禁保留）。
- **覆盖率闸门**（cargo-llvm-cov，仅对核心风险模块设阈值，避免为凑数写废测试）：`engine/`、`ids/`、`importer/` 行覆盖 ≥ 85%，低于即 CI 红；其余模块只报告不设限。
- **属性测试（proptest）**——专治"自证测试"：状态机（任意迁移序列不产生非法状态）、号池（任意并发发放/回收序列不重号不漏号）、importer（任意字节输入不 panic、解析结果 round-trip 一致）。属性测试由不变量驱动而非用例驱动，AI 无法通过"照抄实现"让它变绿。
- **确定性异步测试**：调度退避/冷却/超时全部用 `tokio::time::pause` 虚拟时钟，测试零 flaky、零真实等待。
- **前端逻辑测试**：`@tauri-apps/api/mocks`（mockIPC/mockWindows）驱动 Zustand store 的事件消费逻辑（节流合并、徽章计数、状态组映射），不依赖真实后端。

### 1.4 第三层：审查协议（每个 PR）

- **实现与审查分离**：任务实现完成后，由**全新上下文**的会话执行 `/code-review`（默认 medium；引擎/数据层任务用 high）——同一上下文自己审自己会继承同样的盲点，新会话从 diff 与契约出发审查。发现项修复后才 merge。
- **审查 checklist**（PR 模板固化）：DoD 逐条勾选；IPC/schema 变更是否重新生成绑定与 `.sqlx`；是否动过既有测试断言（动过必须说明）；日志是否可能泄露 Key；错误分类是否落在 4.2 六类之内。
- **里程碑级深审**：M2（引擎）与 M4（发布链）完成时，由用户触发 `/code-review ultra` 对整个里程碑分支做多智能体云端深审（此为计费操作，由用户手动触发）。

### 1.5 第四层：里程碑深度关卡（非每次提交，防累积性劣化）

| 关卡 | 时机 | 内容 |
| ---- | ---- | ---- |
| **变异测试（cargo-mutants）** | M2 出口 + v1.0.0 前 | 仅对 `engine/` + `ids/` 跑：对代码注入变异（改比较符、删分支等），存活的变异体 = 测试没真正约束的行为。这是对"AI 写的测试是否真的在测"的最强检验。每个存活变异体必须补测试或书面豁免 |
| 500 压测 | M2 出口 + M5 | 见任务 2.8 |
| 依赖审计（cargo-audit + pnpm audit） | CI 每周 cron + 发版前 | 已知漏洞与 yanked 依赖告警 |
| 真机人工冒烟 | M3 各页完成时 + M5 | AI 不可替代的最后一环：双端视觉对照原型、键盘走查、reduced-motion 走查 |

### 1.6 其余约定

- **错误处理**：统一 `thiserror` 错误类型经 IPC 序列化给前端；前端未捕获错误经命令转发进 tracing 日志（AI 修 bug 的输入就是这份日志，任务 ID 全链路贯穿）。
- **IPC 契约**：前端只经 `src/lib/ipc/` 出入（`bindings.ts` 生成 + `index.ts` 薄封装）；载荷字段 camelCase 由 specta 序列化配置统一保证。
- 本地一键脚本：`pnpm check` = CI check.yml 的完整镜像；提交前跑它等于预演 CI。

---

## 2. IPC 契约总表（M1–M3 逐步实现，命名即规范）

### 2.1 命令（按域拆分，Rust `commands/` 下同名模块）

**settings 域**
- `get_settings() -> Settings`（调度策略、重试次数、输出根目录、动效偏好、暂停态）
- `update_settings(patch: SettingsPatch) -> Settings`
- `pick_output_dir() -> Option<String>`（tauri-plugin-dialog）
- `open_logs_dir()` / `open_path_in_folder(path)`（tauri-plugin-opener）

**api_keys 域**
- `list_api_keys() -> Vec<ApiKeyView>`（含近 50 次成功率、当前占用并发；key 本体永不出 Rust，视图仅 `sk-…后4位`）
- `add_api_key(input: {alias, key, baseUrl, model, concurrencyLimit}) -> ApiKeyView`（key 进 keyring，库存引用）
- `update_api_key(id, patch)` / `delete_api_key(id)` / `set_api_key_enabled(id, enabled)`

**refs 域**
- `import_ref_images(paths: Vec<String>, groupId: Option<i64>) -> Vec<RefImageView>`（拷入 refs/、生成 512px 缩略图）
- `list_ref_images(groupId?, page?) -> Page<RefImageView>`
- `get_ref_image(id) -> RefImageDetail`（含使用次数、产出作品数）
- `replace_ref_image_file(id, path)` / `set_ref_image_group(id, groupId)` / `trash_ref_image(id)`

**prompts 域**
- `parse_prompt_txt(path) -> ImportPreview`（两段式导入第一步：解析分组/标签/正文 + 编码探测 + 预分配编号区间预览，**不落库**）
- `commit_prompt_import(preview, ctx: 'library'|'generate') -> ImportResult`（第二步：落库；ctx=generate 时建 is_temp 分组）
- `list_prompt_groups() -> Vec<GroupView>`（含数量、收藏数、标签、场景、is_temp）
- `list_prompts(groupId, query?) -> Vec<PromptView>` / `search_prompts(query) -> …`（编号或正文 LIKE）
- `get_prompt(id) -> PromptDetail`（原文、已产出通过作品数）
- `update_prompt_text(id, text)` / `toggle_prompt_favorite(id)` / `trash_prompt(id)`

**batches / tasks 域（引擎）**
- `create_batch(input: {refs: Vec<{refImageId, promptGroupId}>, paramsJson}) -> BatchView`（组合展开一次事务写入，返回任务总数；调度器自动开跑）
- `list_batches() -> Vec<BatchView>`（当前/已归档 + 汇总）
- `list_tasks(batchId, statusGroup?, page?) -> Page<TaskView>`（TaskView 含：状态、参考图名+缩略图路径、提示词组名+编号、Key 标识、伪进度、错误类型+文案、重试次数、结果缩略图路径）
- `get_task(id) -> TaskDetail`（含 task_attempts 执行记录、提示词原文快照、目标文件名）
- `retry_task(id, editedPrompt?: String)`（editedPrompt 存 snapshot，重新入队）
- `retry_failed_tasks(batchId)` / `retry_interrupted_tasks(batchId)`
- `pause_queue()` / `resume_queue()`

**review 域**
- `list_pending_review(batchId) -> Vec<ReviewItemView>`
- `accept_tasks(taskIds: Vec<i64>)`（写 accepted_works 快照 + 输出原图到批次文件夹 + 微调写回 + 临时分组转正，单事务）
- `reject_tasks(taskIds: Vec<i64>)`（置 rej + 原图删除/缩略图留存 + 进 trash_items）

**works 域**
- `list_works(filter: {groupId?, favoriteOnly?}, page?) -> Page<WorkView>`
- `get_work(id) -> WorkDetail`（图片、提示词原文快照、分组/标签/编号、批次、文件路径）
- `toggle_work_favorite(id)` / `trash_work(id)`

**trash 域**
- `list_trash() -> Vec<TrashItemView>`（缩略图、编号、来源、时间）
- `purge_trash_items(ids)` / `purge_all_trash()`（物理删除 + 编号回收）

**updater/misc**
- `check_update_now()`（手动检查；启动自动检查走前端 updater 插件流程）
- `log_frontend_error(payload)`（前端未捕获错误入日志流）

### 2.2 事件（Rust → 前端，Zustand 订阅）

| 事件 | 载荷 | 节流 |
| ---- | ---- | ---- |
| `task://status-changed` | {taskId, status, errorType?, errorMessage?, retryCount, apiKeyId} | 即时 |
| `task://progress` | {taskId, pct, phase} | 250ms 合并 |
| `batch://summary` | {batchId, counts 按 5 视觉组, activeConcurrency, paused} | 250ms |
| `keys://health` | {keyId, state: ok/limited/auth_failed/disabled, usedConcurrency, successRate} | 状态变化时 |
| `update://state` | {state: checking/downloading/ready/error, version?} | 即时 |
| `import://done`、`trash://changed`、`works://changed` | 各域刷新信号 | 即时 |

导航徽章（任务页运行数、验收待处理数、废纸篓数）由 `batch://summary` 等事件驱动，不轮询。

---

## 3. 数据库 Schema 定稿（sqlx migration 0001，forward-only）

连接参数：`journal_mode=WAL · synchronous=NORMAL · busy_timeout=5000 · foreign_keys=ON`。

```
settings        (key TEXT PK, value_json TEXT)
api_keys        (id, name, keyring_account, base_url, model, concurrency_limit, enabled, created_at)
prompt_groups   (id, name, prefix UNIQUE, scene, is_temp DEFAULT 0, created_at)
prompts         (id, group_id FK, code, text, favorite DEFAULT 0, edited DEFAULT 0,
                 status TEXT CHECK(status IN ('active','trash')), source TEXT, created_at, updated_at,
                 UNIQUE(group_id, code))
tags            (id, name UNIQUE)
tag_bindings    (tag_id FK, entity_type, entity_id, PK(tag_id, entity_type, entity_id))
ref_images      (id, name, group_id FK NULL, file_path, thumb_path, width, height, file_size,
                 created_at, deleted_at NULL)
batches         (id, created_at, output_dir, params_json, status CHECK(status IN ('running','archived')))
batch_refs      (batch_id FK, ref_image_id FK, prompt_group_id FK, PK(batch_id, ref_image_id))
tasks           (id, batch_id FK, ref_image_id FK, prompt_id FK, prompt_text_snapshot,
                 status CHECK(八态), api_key_id NULL, error_type NULL, error_message NULL,
                 retry_count DEFAULT 0, result_image_path NULL, result_thumb_path NULL,
                 created_at, updated_at)
task_attempts   (id, task_id FK, api_key_id, started_at, finished_at NULL, outcome,
                 error_type NULL, error_message NULL, http_status NULL, duration_ms NULL)
accepted_works  (id, task_id, image_path, thumb_path, prompt_id, prompt_text, group_id,
                 ref_image_id, batch_id, favorite DEFAULT 0, accepted_at)   -- 快照冗余，长期可追溯
id_pools        (prefix PK, next_seq)
id_recycled     (prefix, number, PK(prefix, number))
trash_items     (id, entity_type, ref_id, thumb_path NULL, prompt_text NULL, code NULL,
                 source_label, file_paths_json, deleted_at)
```

索引：`tasks(batch_id, status)`、`task_attempts(task_id)`、`task_attempts(api_key_id, started_at)`（成功率窗口查询）、`prompts(group_id, status)`、`accepted_works(group_id)`、`ref_images(group_id)`。

---

## 4. 里程碑与任务拆解

> 每个任务标注【DoD】完成定义。顺序即依赖序，M2 是最高风险区，先于全部页面。

### M0 骨架与门禁（先让"空应用+流水线"跑起来）

| # | 任务 | DoD |
| - | ---- | --- |
| 0.1 | 初始化 Tauri 2 + React 19 + TS + Vite + pnpm；锁定当日最新稳定版并提交 lockfile | `pnpm tauri dev` 双端可起空窗 |
| 0.2 | 接入 Tailwind 4 + shadcn/ui + Motion + Zustand + lucide-react + TanStack Virtual；`styles/globals.css` 落全部 Design Tokens（颜色/圆角/间距/字号/阴影/动效时长，oklch 原值） | token 与《设计交付_Design Tokens.md》逐项对得上，无硬编码色值 |
| 0.3 | 窗口壳：macOS Overlay 交通灯 / Windows 无边框自绘窗控（min/max/close + drag region）；min 1140×700；single-instance；window-state | 双端手动冒烟：拖动、缩放、双开被拒 |
| 0.4 | 应用外壳布局：44px 标题栏（logo、更新 pill 占位、⌘K 按钮、Win 窗控）+ 212px 侧栏导航（制作/资产两组、8 项、徽章占位、底部批次进度小卡占位）+ 主面板容器（渐变、13px 圆角、环境光），按原型 `.app/.tbar/.side/.main` 还原 | 与原型逐区块对照一致；8 个空路由页可切换 |
| 0.5 | ⌘/Ctrl+K 命令面板（跳转 8 页 + 占位操作）+ ⌘1–8 切页 + Esc 层级关闭；键盘事件全局管理器（INPUT/TEXTAREA 内不劫持） | 快捷键全部生效，reduced-motion 下动效降级 |
| 0.6 | CLAUDE.md（命令、铁律、IPC 约定、门禁清单、审查协议）；Biome/tsc/vitest/cargo 工具链配置；`pnpm check` 一键脚本；GitHub Actions check.yml；main 分支保护 + PR 模板（审查 checklist） | CI 全绿；直推 main 被拒 |
| 0.7 | tracing 日志滚动落盘（14 天保留）+ `log_frontend_error` 命令 + 全局 ErrorBoundary/onerror 转发 | 前后端错误均出现在同一日志文件 |
| 0.8 | **质量基建（§1.2–1.3 机制落地）**：specta + tauri-specta 绑定生成管线（含 CI diff 校验）；guardrails.sh 全部规则 + 进 CI；lefthook pre-commit；Cargo.toml lints 配置（unwrap/expect/panic deny）；cargo-llvm-cov 覆盖率闸门接入 check.yml；每周 cargo-audit/pnpm audit cron | 故意注入一处违规（手改 bindings.ts / 硬编码 oklch / 加 unwrap）CI 必须变红 |

### M1 数据层与基础域

| # | 任务 | DoD |
| - | ---- | --- |
| 1.1 | migration 0001 全量 schema（§3）+ 连接池 + 各域 repo 骨架；**sqlx offline 模式接通**（`.sqlx` 快照入库，CI `cargo sqlx prepare --check`） | `cargo test` 内存库跑通 CRUD；改 schema 不更新快照时 CI 红 |
| 1.2 | 号池模块 `ids/`：发放（回收优先→自增）、回收，全部与业务写同事务 | 单测 + **proptest**（任意并发发放/回收序列不重号不漏号）、前缀隔离 |
| 1.3 | settings 域命令 + 前端设置 store | 设置读写往返类型安全 |
| 1.4 | api_keys 域：keyring 存取、CRUD 命令、脱敏视图、成功率窗口查询（近 50 次 attempts） | 单测 + 手动验证 Keychain/凭据管理器中可见条目；日志无明文 |
| 1.5 | files 模块：app_data_dir 目录树初始化（refs/thumbs/outputs/trash/logs）、缩略图生成（image crate，长边 512 JPEG q80）、输出命名器（`参考图名_YYMMDD_编号无连字符.JPG`）、废纸篓文件搬运/清理 | 单测：命名规则含中文/特殊字符参考图名；缩略图尺寸正确 |
| 1.6 | importer 模块：txt 解析（分组/标签/正文字段匹配，缺省默认分组）+ chardetng/encoding_rs 编码探测（UTF-8/GBK）+ 两段式命令（parse → commit）+ 编号预分配 | 单测：GBK 文件、缺分组、缺标签、空行、超长正文（500+ 字）用例全过 |

### M2 任务引擎（核心风险区，全部对 wiremock 测试）

| # | 任务 | DoD |
| - | ---- | --- |
| 2.1 | 状态机 `engine/status.rs`：八态合法迁移表（纯函数） | 全部合法/非法迁移单测覆盖 + proptest（任意迁移序列不出非法状态） |
| 2.2 | Provider trait + `OpenAiCompatible`：multipart images/edits、b64_json/url 双返回、连接 10s/整请求 180s 超时（可配置）、结果转 JPG q95 流式落盘、下载阶段字节进度 | wiremock 用例：正常 b64、正常 url、超时、429、4xx 违规文案、401、坏 JSON |
| 2.3 | 错误分类器：Timeout/RateLimited/ContentPolicy/Auth/Interrupted/Other → 各自重试/换 Key/终态策略（技术文档 4.2 表） | 每类错误的处置路径单测 |
| 2.4 | 调度器 dispatcher：单循环、per-Key Semaphore（1–10）、策略 trait（RoundRobin / SuccessRateFirst + 指数退避冷却 30s–10min）、状态迁移统一落库、task_attempts 全记录 | 集成测试（`tokio::time::pause` 虚拟时钟，零 flaky）：多 Key 并发上限不被突破；两策略分配正确；坏 Key 冷却与恢复 |
| 2.5 | 暂停/恢复 + 批次归档（全终态自动 archived） | 暂停后无新派发、进行中跑完；归档态正确 |
| 2.6 | 伪进度引擎：phase + elapsed/expected（Key 级近 20 次均值）+ 250ms 节流事件 | 事件频率、90% 封顶、下载段推进单测 |
| 2.7 | 中断恢复 `recovery.rs`：启动时 run/retry → fail(Interrupted)；q 保留续跑；恢复计数事件供前端 banner | 集成测试：模拟中断重启后状态修复、队列续跑、编号/记录无损 |
| 2.8 | **1→500 压测**（headless 集成测试）：500 任务 × 6 Key × wiremock（注入 5% 超时/3% 429/2% 违规），断言全部达终态、计数准确、无状态错乱、attempts 完整 | 压测入 `cargo test --release -- --ignored` 可重复执行 |
| 2.9 | **M2 出口关卡（§1.5）**：cargo-mutants 跑 `engine/` + `ids/`，逐个处理存活变异体（补测试或书面豁免）；引擎覆盖率 ≥ 85% 确认；用户触发 `/code-review ultra` 深审引擎分支 | 存活变异体清零或全部有豁免记录；深审发现项处理完毕 |

### M3 业务页面（按原型逐页实现；每页 DoD = 与原型对照一致 + 交互全通 + 门禁绿）

> 通用件先行：状态徽章（5 视觉组）、pid 编号 chip、图片卡、空态组件、确认弹窗（`.mdl w360`）、toast（sonner 定制为原型深色胶囊样式）、抽屉（右侧 400px）、提示词详情弹窗（w700，正文 12.5px/1.85 可选中复制，编辑态 textarea）。

| # | 页面 | 要点（对照原型区块） |
| - | ---- | ------------------- |
| 3.1 | **设置页** | API Key 列表（kline 网格：别名/URL/模型/并发 stepper 1–10/成功率/状态徽章/开关/删除）、添加 Key 弹窗（w420：别名/key/baseUrl/model 默认 gpt-image-2/并发默认 2）、调度策略双卡单选、重试次数 stepper（0–3）、输出目录 pathwell+更改、命名规则示意、主题（深色置灰标 V2）、动效标准/减弱、检查更新按钮 |
| 3.2 | **参考图库** | 按分组 gsec 分节网格（142px 瓦片）、导入（多选文件→选分组）、详情弹窗（w640：大图、使用统计、更换图片、改分组 pickrow、删除→废纸篓） |
| 3.3 | **提示词库** | 默认全分组展开；分组头（色点/名称/前缀 chip/场景徽章/标签/数量+收藏数/临时徽章/查看作品→作品库带筛选）；编号 pchip 网格（收藏琥珀点、title 悬停预览前 88 字）；搜索（编号或正文）；导入 .txt 两段式弹窗（drop → 解析预览分组/标签/编号区间/条数 → 确认导入）；提示词详情弹窗（查看/编辑/收藏/删除→确认→废纸篓） |
| 3.4 | **图片生成页** | 提示词卡在上（空态引导双按钮/已选分组行+编号 chip 网格/临时分组琥珀徽章/移除）；参考图卡在下（空态引导/118px 瓦片网格/挂靠 mapchip 三态：needmap 琥珀脉冲×2→mapped 蓝）；选择提示词组弹窗（w640 多选 gpick）；从参考图库选择弹窗（w640 按分组多选）；上传（dialog 多选→入库+入选区）；指定提示词组弹窗（w640 单选挂靠）；底部 genbar 公式 chips（`ref × 组(n)`、未挂靠 `ref × ?` 警示）+ 任务总数 + 阻断提示文案 + 开始生成（未就绪 disabled）；点击开始→`create_batch`→toast→跳任务页 |
| 3.5 | **任务队列页** | 页头（批次切换弹窗：进行中/已归档、重试全部失败 · n、暂停/继续队列）；5+1 汇总徽章排；API Key 状态条（默认折叠：8 个健康色点+并发汇总+异常徽章升权；展开 kexp 卡片网格：状态/并发占用/成功率/开关；"完整管理"→设置页）；中断恢复琥珀 banner（一键重试中断任务/关闭）；状态分组 seg 筛选（全部/待处理/生成中/异常/待验收/已完成）；任务表（tgrid 7 列固定 40px 行高 + TanStack Virtual；行内：徽章+spinner、缩略图+参考图名、pid+组名、Key chip、进度条/错误文案（Auth 行内"停用该 Key"按钮）/结果文案、重试数、hover 重试按钮）；行点击→任务详情抽屉（缩略图、进度、错误 banner、chips、执行记录时间线、提示词原文、重新生成）；归档批次空态+跳作品库；侧栏运行小卡（批次进度%+并发数，点击跳任务页） |
| 3.6 | **图片验收页** | 页头（待验收数、本批已处理 x/y、多选操作组：通过所选/移入废纸篓/清除、每行 3–8 网格滑杆）；网格卡（单击多选 rck 勾、hover 快捷通过/不通过/放大、双击进大图）；底部快捷键 hintbar；大图逐张模式（左图右栏 344px：pid/组/chips/提示词原文滚动、微调折叠区 textarea+"用微调提示词重试"、底部 ←→/位置/重试 R/不通过 ⌫/通过 ⏎）；键盘全套；通过→accept 事务（输出+快照+微调写回+临时组转正 toast）；不通过→废纸篓 toast |
| 3.7 | **作品库** | seg 筛选（全部/收藏/各分组）；wgrid 4:3 卡（pid/组名/日期/收藏星）；虚拟滚动；详情弹窗（w700：图+chips+输出文件路径 pathwell+打开所在文件夹（opener）+提示词原文滚动区+收藏/删除→确认→废纸篓） |
| 3.8 | **废纸篓** | xrow 列表（勾选、灰化缩略图、pid、参考图名、来源徽章：验收未通过/手动删除、时间、单项删）；批量"彻底删除所选 · n"/"清空废纸篓"→确认弹窗（文案明确编号回收不可恢复）→purge |
| 3.9 | 全局串联 | 导航徽章（运行数/待验收数/废纸篓数）事件驱动；⌘K 操作项补全（导入提示词/暂停继续队列/检查更新）；图片一律 convertFileSrc 走 asset protocol（scope 收敛到 app_data_dir 与输出根目录）；列表只挂缩略图，原图仅大图审阅加载；**mockIPC 驱动的 store 测试**（事件节流合并、徽章计数、8 态→5 视觉组映射） |

### M4 更新与发布链

| # | 任务 | DoD |
| - | ---- | --- |
| 4.1 | tauri-plugin-updater + process：启动检查→后台下载→标题栏 pill"已就绪 · 重启安装"→确认 relaunch；设置页手动检查；`update://state` 事件驱动 UI | 对本地静态 latest.json 演练全流程 |
| 4.2 | minisign 密钥对生成（公钥入 conf，私钥入 GitHub Secrets + 离线备份提醒用户）；`bundle.createUpdaterArtifacts: true`；endpoint 指向公开 releases 仓库 | 配置审查通过 |
| 4.3 | release.yml：tag `v*` 触发，macos universal dmg + app.tar.gz、windows NSIS per-user + WebView2 downloadBootstrapper，tauri-action 自动上传 latest.json | 打 v0.1.0 测试 tag 产物齐全 |
| 4.4 | 真机演练：双端安装、SmartScreen/Gatekeeper 首次引导 README、旧版→新版应用内更新、更新失败不影响使用、生成中强杀进程→重启中断恢复 | 演练清单逐项打勾（人工） |

### M5 收尾质量关（发布 v1.0.0 前）

- 双端 UI 冒烟（WKWebView/WebView2 渲染差异排查，重点 oklch、backdrop、滚动条样式降级）。
- 500 任务真机端到端（可用 wiremock 本地服务顶替真实 API）：内存/磁盘无异常增长，UI 不卡顿（虚拟滚动生效）。
- 键盘全图走查、reduced-motion 走查、日志脱敏抽查（无明文 Key）。
- **发版关卡（§1.5 全套）**：cargo-mutants 复跑 engine/+ids/；cargo-audit + pnpm audit 清零或豁免；用户触发 `/code-review ultra` 对 v1.0.0 候选分支做最终深审。
- CLAUDE.md 与本计划状态同步，未尽事项列入 V2 backlog。

---

## 5. CLAUDE.md 应包含的内容（0.6 任务的产出规格）

1. 常用命令：`pnpm tauri dev` / `pnpm check`（全门禁）/ `pnpm test` / `cargo test -p gendesk` / 压测与 mutants 命令 / 绑定重新生成命令。
2. 架构铁律：业务真相在 Rust；单写者事务；前端只走 lib/ipc/；事件驱动不轮询。**并注明：每条铁律都有 guardrails/CI 对应检查，改规则必须同步改检查。**
3. IPC 约定：`bindings.ts` 由 tauri-specta 生成禁手改；新增命令/事件后的再生成流程。
4. 错误规范：thiserror 六类分类表、unwrap/expect 由 lints 强制禁止、前端错误转发。
5. 视觉规范：token 只从 globals.css 取（guardrails 强制）；原型 HTML 为像素基准；8 态→5 视觉组映射表。
6. 领域词汇表：批次/挂靠/临时分组/号池/废纸篓/归档 的精确语义（§0 前置事实的浓缩）。
7. **审查协议（§1.4）**：实现与审查分离、PR checklist、测试完整性规则（不许为过 CI 削弱既有断言）。

---

## 6. 残余风险与决策点（执行中遇到时的默认处置）

| 风险 | 默认处置 |
| ---- | -------- |
| GPT-Image-2 实际返回格式与 OpenAI images/edits 有出入 | Provider 层兼容字段做宽容解析；差异全部隔离在 `provider/openai.rs`，不外溢 |
| oklch 在 WebView2（旧 Evergreen）渲染异常 | Win10 最低版 WebView2 Evergreen 已支持 oklch；若个别机器异常，构建期由 Tailwind/LightningCSS 输出 srgb 回退 |
| 500 行任务表事件风暴 | 已定 250ms 节流 + 行级 memo + 虚拟滚动；仍卡则批次汇总驱动、行数据按需 invoke |
| 编号回收与并发发放竞态 | 号池操作全部在单写者事务内，压测 2.8 覆盖 |
| 归档批次任务行长期膨胀 | V1 保留不删（SQLite 体量可忽略）；清理策略留 V2 |
| 更新公钥/私钥管理 | 私钥仅 GitHub Secrets + 用户离线备份；丢失=更新链断裂，在 4.2 完成时向用户显式确认已备份 |

---

## 7. V2 预留自检（V1 完成时逐项确认未被破坏）

- 多态 `tag_bindings` 未被写死为 prompt_group 专用（entity_type 字符串开放）。
- 资产表可扩展"外部路径引用"（视频不入库场景）。
- 引擎/Provider/commands 模块边界清晰，sidecar 进程管理可作为新模块加入。
- 批次/任务/资产 ID 稳定，输出目录与命名确定性（RPA 任务单契约基础）。
