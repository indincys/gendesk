# 内部图片生产工具 V1 技术选型（最终落地版）

> 定稿于 2026-07-08，修订于 2026-07-09（开发前审查）  
> 配套阅读：《内部图片生产工具_V1需求文档.md》（需求权威来源）、《图片生成工具原型设计-handoff.zip》（视觉与交互权威来源，含 Design Tokens）。  
> 本文档是开发阶段的唯一技术基准；具体执行拆解见《内部图片生产工具_V1开发执行计划.md》。

### 修订记录（2026-07-09，依据原型 handoff 与需求复核，原型/需求优先）

| #   | 修订                                                                                                                                                                                          | 原因                                                  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| R1  | 任务组合模型由"参考图 × 全部所选提示词"改为"**每张参考图挂靠一个提示词组**"，`batch_refs` 增加 `prompt_group_id`                                                                                                               | 原型与设计摘要 §9 已定稿挂靠交互；需求 8.1 的"多参考图共用同一组提示词"在挂靠模型下依然成立 |
| R2  | `ref_images` 增加 `group_id`（与提示词库共用分组体系）                                                                                                                                                     | 设计摘要 §11：参考图库与提示词库同一套分组                             |
| R3  | 新增窗口样式方案：macOS 原生交通灯 overlay，Windows 无边框自绘窗口按钮                                                                                                                                              | 原型为自定义标题栏（44px tbar），原文档未覆盖                         |
| R4  | 引擎新增**暂停/恢复队列**与**批次归档**；明确任务进度为**阶段型伪进度**（生图 API 无真实进度）                                                                                                                                    | 原型任务页有暂停按钮、批次切换/归档态、行内百分比进度条                        |
| R5  | 移除 TanStack Table（任务表为固定网格，无排序列操作，自绘 + TanStack Virtual 足够）；移除 csv crate（需求仅 txt 导入）                                                                                                        | 减少无用依赖与 AI 维护面                                      |
| R6  | Base URL 约定已含 `/v1`（如 `https://api.example.com/v1`），端点拼接为 `{base_url}/images/edits`                                                                                                         | 原型设置页/添加 Key 弹窗均按含 /v1 展示，消除双 /v1 隐患                |
| R7  | `prompt_groups` 增加 `is_temp`（生成页导入的临时分组，首次验收通过自动转正式入库）；标签绑定在**分组级**（tag_bindings entity_type='prompt_group'）                                                                             | 原型分组卡片展示"临时 · 验收通过后自动入库"徽章与分组级标签                    |
| R8  | 验收微调提示词：重试用 `tasks.prompt_text_snapshot`，**验收通过时**才写回提示词库并标记 `edited`                                                                                                                       | 需求 13.2"微调后验收通过，默认按微调版本进入提示词库"（原型的立即写库行为以需求为准修正）    |
| R9  | 前端新增 ⌘/Ctrl+K 命令面板与 ⌘1–8 切页、验收页全套快捷键（⏎/⌫/R/←→）                                                                                                                                              | 原型与 Design Tokens §7 的关键交互约定                        |
| R10 | AI 全代码质量体系升级：IPC 绑定由 tauri-specta 自动生成（替代手写 TS 类型）；sqlx offline 编译期查询校验；clippy lints 强制禁 unwrap；guardrails 架构检查脚本；proptest 属性测试；核心模块覆盖率闸门（cargo-llvm-cov）；里程碑级变异测试（cargo-mutants）；实现/审查分离协议 | 代码 100% 由 AI 生成/审查，散文规则必须升级为机器强制（详见执行计划 §1）         |

## 0. 决策记录

选型结论：**Tauri 2 + React/TypeScript + Tailwind CSS/shadcn/ui/Motion（前端） + Rust/tokio（任务引擎） + SQLite（数据） + tauri-plugin-updater（更新，GitHub Releases 托管）。**

用户已确认的六条决策及其技术响应：

| # | 决策                                | 技术响应                                                                                                                                   |
| - | --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| 1 | 安装包托管在 GitHub Releases            | 更新器 endpoint 指向 GitHub Releases 的 latest.json；发布仓库必须公开（见 6.2）；国内访问波动预案为多端点回退（见 6.3）                                                    |
| 2 | Windows 不购买代码签名证书                 | 接受 SmartScreen 首次拦截，提供安装引导文案（见 7.1）；NSIS 按当前用户安装免 UAC                                                                                  |
| 3 | 代码 100% 由 Claude Code 写、AI 审、AI 修 | Rust 学习曲线不再是权衡项；以编译器 + 类型系统 + 自动化测试 + 质量门禁作为正确性安全网（见第 8、9 节）                                                                           |
| 4 | UI 要求视觉高效、精美、高颜值、现代感、交互动效         | 前端组件方案由 Ant Design 调整为 **Tailwind CSS 4 + shadcn/ui + Motion**，获得完全视觉控制权（见 3.1）                                                        |
| 5 | Windows 仅支持 10/11                 | WebView2 Evergreen 绝大多数已预装，NSIS 内置 bootstrapper 兜底；无旧系统兼容负担                                                                            |
| 6 | 采用 Tauri 方案                       | 定稿。选型理由存档：① 未签名 Mac 的应用内更新开箱可用（自有 minisign 签名校验，不依赖苹果证书）；② 任务引擎（需求核心风险区）获得 Rust 编译期可靠性保障；③ sidecar 一等公民支持，正对 V2 的 Python 集成规划；④ 体积内存优势 |

---

## 1. 总体架构

```
┌────────────────────────── 桌面应用（单进程 + Webview） ──────────────────────────┐
│                                                                                │
│  前端 Webview（React + TS）              Rust 核心（tauri 主进程）                 │
│  ┌──────────────────────┐   IPC 命令    ┌──────────────────────────────┐       │
│  │ 页面/组件（shadcn/ui） │ ───────────► │ commands/（按业务域拆分）       │       │
│  │ Zustand stores        │   事件推送    │ engine/（调度·并发·重试·恢复）  │       │
│  │ 类型化 ipc 封装        │ ◄─────────── │ provider/（OpenAI 兼容生图）    │       │
│  └──────────────────────┘              │ db/（SQLite 单写者）            │       │
│         │ asset protocol               │ files/（目录·命名·缩略图·废纸篓）│       │
│         ▼ 本地图片直读                   │ importer/（txt 解析 + 编码探测）│       │
│                                        └──────────┬───────────────────┘       │
└───────────────────────────────────────────────────┼───────────────────────────┘
                                                    ▼
                外部：生图 API（多 Key） · GitHub Releases（更新） · 本地文件系统
```

两条铁律，贯穿所有实现：

1. **业务真相只在 Rust 侧**：任务状态、编号发放、文件操作、数据库读写全部经 Rust 命令完成；前端只做展示与交互，不持有可变业务状态。
2. **单写者原则**：所有状态迁移由调度器串行提交事务，配合 tauri-plugin-single-instance 禁止双开，从机制上杜绝"高并发下任务状态混乱"。

---

## 2. 技术栈清单

版本策略：下表标注撰写时的主流稳定大版本；项目初始化时以当日最新稳定版锁定，`pnpm-lock.yaml` 与 `Cargo.lock` 提交入库，此后升级须过全部质量门禁。

### 2.1 应用框架与官方插件

| 组件   | 选型                                          | 用途                     |
| ---- | ------------------------------------------- | ---------------------- |
| 框架   | Tauri 2.x                                   | 应用壳、打包、IPC             |
| 更新   | tauri-plugin-updater + tauri-plugin-process | 检查/下载/安装更新，relaunch 重启 |
| 对话框  | tauri-plugin-dialog                         | 文件/目录选择器               |
| 打开文件 | tauri-plugin-opener                         | 在 Finder/资源管理器中显示输出文件夹 |
| 单实例  | tauri-plugin-single-instance                | 禁止双开（保护任务队列与号池）        |
| 窗口状态 | tauri-plugin-window-state                   | 记住窗口尺寸与位置              |

明确不用 tauri-plugin-sql：数据库只从 Rust 侧访问，保证事务边界与状态机一致性。

### 2.2 前端

| 组件      | 选型                                  | 用途                                                |
| ------- | ----------------------------------- | ------------------------------------------------- |
| 框架      | React 19 + TypeScript 5 + Vite      | 基础栈                                               |
| 样式      | Tailwind CSS 4（@tailwindcss/vite）   | 原子化样式 + design tokens（CSS 变量）                     |
| 组件      | shadcn/ui（Radix UI 原语）              | Button/Dialog/Table/Toast(sonner) 等基础件，源码进仓库可完全定制 |
| 动效      | Motion（`motion` 包，framer-motion 后继） | 交互动效、状态过渡、列表 stagger                              |
| 状态      | Zustand 5                           | 轻量全局 store，承接 Rust 事件推送                           |
| 虚拟滚动    | TanStack Virtual 3                  | 500 行任务表（固定行高自绘网格）、素材/作品图片墙                       |
| 图标      | lucide-react                        | 统一图标语言                                            |
| Tauri 桥 | @tauri-apps/api 2                   | invoke/事件/convertFileSrc                          |

### 2.3 Rust 核心

| crate                                          | 用途                                       |
| ---------------------------------------------- | ---------------------------------------- |
| tokio                                          | 异步运行时；调度循环、per-Key Semaphore             |
| reqwest（`rustls-tls, multipart, json, stream`） | 生图 API 调用；rustls 免 OpenSSL 构建麻烦          |
| sqlx（`runtime-tokio, sqlite`）                  | SQLite 访问 + 内置 migration                 |
| serde / serde\_json                            | 序列化；IPC 载荷与参数快照                          |
| thiserror                                      | 错误类型建模                                   |
| specta + tauri-specta                          | 由 Rust 命令/事件签名自动生成前端 TS 绑定（消除契约漂移，R10）   |
| proptest（dev）                                  | 属性测试：状态机/号池/导入器的不变量检验（R10）               |
| tracing + tracing-appender                     | 结构化日志，滚动写本地文件                            |
| image                                          | 缩略图生成、格式转换（输出统一 JPG）                     |
| encoding\_rs + chardetng                       | 提示词 txt 导入：GBK/UTF-8 编码探测（需求仅 txt）       |
| keyring                                        | API Key 存系统钥匙串（Keychain / Windows 凭据管理器） |
| wiremock（dev）                                  | 引擎集成测试的 Mock 生图服务                        |

### 2.4 开发与质量工具链

| 工具                                                                                     | 用途                                                                                                                            |
| -------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| pnpm + Node 22 LTS                                                                     | 前端包管理                                                                                                                         |
| Biome                                                                                  | 前端 lint + format（单工具，速度快，AI 维护成本低）                                                                                            |
| tsc --noEmit / vitest                                                                  | 前端类型检查 / 单元测试（含 @tauri-apps/api/mocks 的 store 测试）                                                                             |
| cargo fmt / clippy（`-D warnings` + unwrap/expect/panic deny，Cargo.toml lints 配置）/ test | Rust 格式、静态检查、测试                                                                                                               |
| cargo sqlx prepare --check                                                             | SQL 与 schema 的编译期一致性校验（offline 模式，R10）                                                                                        |
| cargo-llvm-cov                                                                         | 覆盖率闸门：engine/ ids/ importer/ 行覆盖 ≥ 85%（R10）                                                                                   |
| cargo-mutants                                                                          | 里程碑级变异测试（engine/ + ids/），检验 AI 所写测试的有效性（R10）                                                                                  |
| cargo-audit / pnpm audit                                                               | 依赖漏洞审计（CI 每周 cron，R10）                                                                                                        |
| lefthook + scripts/guardrails.sh                                                       | pre-commit 快速门禁；架构铁律机器化检查——invoke/listen 只许出现在 lib/ipc/、oklch 硬编码只许在 globals.css、禁 console.log、禁无说明 #\[allow]、日志防 Key 泄露（R10） |
| GitHub Actions                                                                         | CI 门禁 + 双端构建发布（见第 8 节）；main 分支保护，PR 全绿才可合并（R10）                                                                               |

---

## 3. 前端架构

### 3.1 为什么从 Ant Design 调整为 Tailwind + shadcn/ui + Motion

对应决策 #4（高颜值、现代感、动效）与决策 #3（AI 编码）：

1. **完全视觉控制**：antd 高效但视觉基因是企业后台，深度定制成本高；shadcn/ui 组件以源码形式进仓库，token 到像素全可控，能承接 Claude design 的高保真设计而不被组件库默认样式拖拽。
2. **动效一体化**：Motion 与 React 组件模型深度结合（enter/exit、layout 动画、stagger），"交互动效"是一等能力而非补丁。
3. **AI 亲和**：Tailwind + shadcn 是 AI 生成前端代码的最强语料生态，样式即代码、无隐式全局 CSS，AI 改样式不产生远程副作用。
4. 代价（组件不如 antd 全）在本项目可接受：所需组件均在 shadcn 覆盖内，复杂表格用 TanStack Table headless 自绘。

### 3.2 仓库结构

```
GenDesk/
├── CLAUDE.md                     # AI 开发约定（见 9.1）
├── index.html · vite.config.ts · tsconfig.json · biome.json
├── src/                          # 前端
│   ├── main.tsx · App.tsx · routes.tsx
│   ├── components/ui/            # shadcn 基础组件（生成后入库）
│   ├── components/               # 通用业务组件（状态徽章、图片卡…）
│   ├── features/                 # 按页面域：generate/ tasks/ review/ refs/
│   │                             #   prompts/ works/ trash/ settings/
│   ├── stores/                   # Zustand（tasks.ts、update.ts…）
│   ├── lib/ipc/                  # 唯一 IPC 出入口：bindings.ts（tauri-specta
│   │                             #   自动生成，禁手改）+ index.ts（薄封装）
│   └── styles/globals.css        # tokens（CSS 变量）+ Tailwind 入口
├── src-tauri/
│   ├── tauri.conf.json · Cargo.toml
│   ├── migrations/               # sqlx 迁移（forward-only）
│   └── src/
│       ├── main.rs · lib.rs · state.rs · error.rs · logging.rs
│       ├── commands/             # IPC 命令，按域拆分，薄壳只做参数校验+调用
│       ├── engine/               # dispatcher.rs · worker.rs · strategy.rs ·
│       │                         #   status.rs（状态机）· recovery.rs
│       ├── provider/             # traits.rs · openai.rs
│       ├── db/                   # 连接池、各域 repo
│       ├── files/                # 数据目录、命名、缩略图、废纸篓
│       ├── importer/             # txt 解析 + 编码探测 + 字段映射
│       └── ids/                  # 编号号池（发放/回收）
└── .github/workflows/            # check.yml · release.yml
```

### 3.3 数据流

- 前端一切读写走 `lib/ipc/` 的类型化函数；IPC 载荷与事件类型在 Rust 侧用 serde + specta 定义，**前端&#x20;********`bindings.ts`********\*\*\*\*\*\*\*\* 由 tauri-specta 自动生成（禁手改，CI 重新生成并 diff 校验）**，字段名 camelCase 由序列化配置统一保证（R10，替代原"两边手写 + CLAUDE.md 约定锁定"方案——契约漂移从人为约定升级为编译期保证）。
- 高频进度不走轮询：引擎经 Tauri 事件推送 `task://progress`、`task://status-changed`、`batch://summary`、`update://state`，Zustand store 订阅更新；进度事件按 250ms 节流合并。
- 列表数据（任务表、图库）由前端按需 invoke 分页/筛选查询，不整表常驻内存。

### 3.4 图片与长列表性能

- 素材/作品图片墙与 500 行任务表全部虚拟滚动（TanStack Virtual），任务表行高固定。
- 图片经 asset protocol（convertFileSrc）直读本地文件；列表只加载缩略图（导入/验收时由 Rust 生成，长边 512px JPEG 质量 80），原图仅在大图审阅时按需加载。
- 生成结果在 Rust 侧解码后直接流式落盘，不经前端内存。

### 3.5 主题与窗口样式

- 浅色优先；颜色/圆角/间距全部走 CSS 变量 + Tailwind token（token 值以 handoff 的《设计交付\_Design Tokens.md》为准，oklch 色彩体系直接映射 Tailwind 4 `@theme`）；动效全局遵守 `prefers-reduced-motion` 降级，另提供设置页"标准/减弱"开关。
- **窗口样式（对应原型自定义标题栏）**：
  - macOS：`titleBarStyle: "Overlay"` + `hiddenTitle: true`，保留原生交通灯叠加在自绘 44px 标题栏上（原型中的三个圆点即交通灯占位，不自绘）。
  - Windows：`decorations: false`，标题栏自绘最小化/最大化/关闭按钮（`@tauri-apps/api/window`），标题栏空白区加 `data-tauri-drag-region`。
  - 窗口最小尺寸 1140×700，`tauri-plugin-window-state` 记忆尺寸位置。
- **键盘体系（Design Tokens §7）**：⌘/Ctrl+K 命令面板（跳转 8 页 + 常用操作）；⌘/Ctrl+1–8 切页；验收大图 ←/→ 切换、⏎ 通过、⌫ 不通过、R 微调重试；Esc 逐层关闭弹窗/抽屉/详情。

---

## 4. Rust 核心设计

### 4.1 任务引擎

- **组合展开（R1，按挂靠模型）**：生成页中每张参考图必须挂靠一个提示词组（多张参考图可挂同一组）；创建批次时按 `Σ(参考图 × 其挂靠组的提示词数)` 展开为任务行，一次事务写入（任务数量准确、可审计，对应需求 8.1 与设计摘要 §9；未全部挂靠时前端禁用"开始生成"）。
- **调度器**：单一 dispatcher 循环——从队列取「待生成」任务 → 按策略选可用 Key → 获取该 Key 的 `tokio::sync::Semaphore` 许可（并发上限 1–10，对应需求 9.2）→ spawn worker 执行。所有状态迁移由 dispatcher 统一落库。
- **暂停/恢复（R4）**：暂停 = 停止派发新任务，进行中请求跑完当前尝试；恢复 = 继续派发。暂停状态入 settings，重启后沿用。
- **批次归档（R4）**：批次内任务全部达到终态（已通过/未通过/失败）后标记 `archived`；任务行保留在库中（成本可忽略），任务页对归档批次展示汇总归档态而非任务列表。
- **任务进度语义（R4）**：生图 API 无真实进度，任务行进度为**阶段型伪进度**——排队 0% → 已发起请求 10% → 按该 Key 近期平均耗时的 elapsed/expected 线性推进至 90% 封顶 →（url 返回时下载阶段 90–98%）→ 落盘完成 100%。进度事件按 250ms 节流推送。
- **策略 trait**（需求 10.2），V1 两个实现：
  - `RoundRobin`：在启用且未满载的 Key 间轮转；
  - `SuccessRateFirst`：按各 Key 近 50 次尝试的成功率排序取最高，同分回退轮转；连续失败的 Key 进入指数退避冷却（30s 起，上限 10 分钟），避免坏 Key 被反复选中，也避免其被永久饿死。
- **状态机**（需求 10.3 八态）：`待生成 → 生成中 →（重试中 → 生成中)* → 成功 → 待验收 → 已通过/未通过`；失败为终态（保留错误原因，可手动重试）。合法迁移表写成纯函数并配测试，非法迁移在编译/测试期暴露。

### 4.2 错误分类与重试（需求 11）

| 错误类型          | 判定             | 默认处置                     |
| ------------- | -------------- | ------------------------ |
| Timeout       | 连接/读取超时        | 重试 1 次，换 Key             |
| RateLimited   | HTTP 429       | 重试 1 次，换 Key，退避后执行       |
| ContentPolicy | 4xx + 违规错误码/文案 | 重试 1 次（同提示词），仍失败则终态并标注违规 |
| Auth          | 401/403        | 不重试；建议停用该 Key 并前台提示      |
| Interrupted   | 应用中断恢复时标记      | 不自动重试；任务列表提示"因任务中断请重试"   |
| Other         | 其余             | 按用户设置的重试次数               |

重试次数用户可配（需求 11.1）；每次尝试写入 `task_attempts`（Key、错误类型、HTTP 状态、耗时），任务列表的"失败原因/重试次数/Key 标识"直接由此渲染。

### 4.3 中断恢复（需求 12）

- SQLite WAL + 每次状态迁移即时事务提交，崩溃不丢状态。
- 启动恢复流程：`生成中/重试中` → 标记 `失败(Interrupted)` 并提示重试；`待生成` 原样保留，调度器启动后自动续跑；已生成未验收的图片及全部关联记录（参考图、提示词、参数、Key 调度记录）本就在库中。
- 前端启动时若检测到被中断任务，顶部 banner 提示并提供"一键重试全部中断任务"。

### 4.4 生图 Provider（需求 9.1）

```rust
trait ImageProvider {
    async fn generate(&self, req: GenRequest) -> Result<GenImage, ProviderError>;
}
```

- V1 唯一实现 `OpenAiCompatible`：图生图走 `POST {base_url}/images/edits`（multipart：参考图文件 + prompt + model）。**Base URL 约定已含 \*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*****`/v1`**（R6，与 OpenAI SDK baseURL 惯例一致，如 `https://api.example.com/v1`）；保存 Key 时对 base\_url 做尾部 `/` 归一化。兼容 `b64_json` 与 `url` 两种返回（url 需二次下载）。
- 超时：连接 10s，整请求 180s（生图慢），可在设置中调整。
- 结果统一转存 JPG（image crate，质量 95），按需求 14.3 命名：`参考图名_日期_提示词编号.JPG`。
- V2 接入其他兼容模型 = 新增 trait 实现，不动引擎。

---

## 5. 数据设计

### 5.1 SQLite 表清单

连接参数：`journal_mode=WAL · synchronous=NORMAL · busy_timeout=5000 · foreign_keys=ON`。

- **settings**：key, value\_json（调度策略、重试次数、输出根目录等）
- **api\_keys**：id, name, keyring\_account（Key 本体在系统钥匙串，库中只存引用）, base\_url, model, concurrency\_limit, enabled, created\_at
- **ref\_images**：id, name, **group\_id（R2，与提示词库共用分组体系，可空=未分组）**, file\_path, thumb\_path, width, height, file\_size, created\_at, deleted\_at
- **prompt\_groups**：id, name, prefix（编号前缀）, scene, **is\_temp（R7，生成页临时导入分组；该组任一提示词首次验收通过时置 false 自动转正式）**, created\_at
- **prompts**：id, group\_id, code（如 YS-0001）, text, favorite, **edited（R8，被微调过）**, status(active/trash), source(library/temp\_import), created\_at, updated\_at
- **tags** / **tag\_bindings**：id, name ／ tag\_id, entity\_type, entity\_id（多态标签；**V1 绑定在分组级 entity\_type='prompt\_group'（R7）**，V2 复用到视频/文案）
- **batches**：id, created\_at, output\_dir, params\_json（模型参数快照）, status（running/archived，R4）
- **batch\_refs**：batch\_id, ref\_image\_id, **prompt\_group\_id（R1，该参考图在本批次挂靠的提示词组）**
- **tasks**：id, batch\_id, ref\_image\_id, prompt\_id, prompt\_text\_snapshot（微调后快照）, status（八态）, api\_key\_id, error\_type, error\_message, retry\_count, result\_image\_path, created\_at, updated\_at
- **task\_attempts**：id, task\_id, api\_key\_id, started\_at, finished\_at, outcome, error\_type, error\_message, http\_status, duration\_ms
- **accepted\_works**：id, task\_id, image\_path, thumb\_path, prompt\_id, prompt\_text, group\_id, ref\_image\_id, batch\_id, favorite, accepted\_at（快照式冗余，保证长期可追溯，对应需求 13.3/14.4）
- **id\_pools** / **id\_recycled**：prefix, next\_seq ／ prefix, number（编号发放与废纸篓回收，对应需求 7.3）
- **trash\_items**：id, entity\_type, ref\_id, file\_path, deleted\_at（废纸篓清单；清理=物理删除文件+级联清除记录，不可恢复，对应需求 13.4）

编号发放/回收与业务写操作同事务：优先取回收号，无则 next\_seq 自增。

### 5.2 应用数据目录（Tauri app\_data\_dir）

```
GenDesk/
├── gendesk.db                # SQLite（含 -wal/-shm）
├── refs/                     # 参考图原图
├── thumbs/                   # 缩略图
├── outputs/{批次ID}/          # 通过验收的成品（输出根目录可在设置中更改，
│                             #   默认系统"图片"目录下 GenDesk）
├── trash/                    # 废纸篓暂存
└── logs/                     # tracing 滚动日志（保留 14 天）
```

### 5.3 API Key 安全

Key 明文只进系统钥匙串（keyring crate：macOS Keychain / Windows 凭据管理器），数据库与日志永不落明文；日志打印 Key 时统一脱敏为 `name(****后4位)`。

---

## 6. 应用内更新（GitHub Releases 落地）

### 6.1 流程（对应需求 4.2）

启动时 updater 请求 latest.json → 有新版则后台下载并做 minisign 签名校验 → 完成后前台弹窗"重启并安装" → 用户确认 → 安装并 relaunch。Windows 走 NSIS 静默升级；macOS 替换 .app（应用自行下载的更新包无 quarantine 属性，未签名不构成障碍）。

### 6.2 配置要点

- **发布仓库必须公开**：updater 需匿名访问。若代码仓库私有，建立独立公开仓库 `gendesk-releases` 只放安装包与 latest.json（推荐做法）。
- endpoint：`https://github.com/{owner}/{releases仓库}/releases/latest/download/latest.json`
- `tauri.conf.json`：`bundle.createUpdaterArtifacts: true`；`plugins.updater` 配 pubkey 与 endpoints。
- minisign 密钥对本地生成一次：公钥进 tauri.conf.json，私钥只存 GitHub Actions Secrets（`TAURI_SIGNING_PRIVATE_KEY` / `..._PASSWORD`）。**私钥丢失 = 更新链路断裂**，另做一份离线备份。
- latest.json 与各平台产物由 tauri-action 在发布时自动生成上传，无手工步骤。

---

## 7. 打包与分发

标识：identifier 建议 `com.gendesk.app`；版本号 semver，`tauri.conf.json` 为唯一版本源。

### 7.1 Windows（10/11，x64）

- NSIS 安装包，per-user 安装（免管理员权限）；WebView2 `downloadBootstrapper` 模式兜底未预装机器。
- 未签名 → 首次运行 SmartScreen 拦截，引导文案（随安装包附 README）："更多信息 → 仍要运行"，仅首次。

### 7.2 macOS（12+，Universal）

- 构建 `universal-apple-darwin` 单产物（Intel + Apple Silicon 共用，updater 清单简单）；分发 .dmg，更新走 .app.tar.gz。
- 未签名首次启动引导：拖入"应用程序"后启动（避免 App Translocation）；macOS 15+ 需"系统设置 → 隐私与安全性 → 仍要打开"，仅首次；后续版本经应用内更新替换，不再触发。

---

## 8. CI/CD 与质量门禁

### 8.1 check.yml（每次 push / PR，全绿才可合并）

1. `cargo fmt --check` · `cargo clippy --all-targets -- -D warnings` · `cargo test` · `cargo sqlx prepare --check`
2. `biome ci` · `tsc --noEmit` · `vitest run` · specta 绑定重新生成并 diff（bindings.ts 未同步即红）
3. `scripts/guardrails.sh`（架构铁律机器检查） + `cargo llvm-cov`（engine/ids/importer ≥ 85%）
4. `pnpm build`（前端可构建） + `cargo check`（Tauri 侧可编译）

另有每周 cron：`cargo audit` + `pnpm audit`。本地 `pnpm check` 为上述完整镜像，lefthook pre-commit 跑秒级子集（fmt/clippy/tsc/guardrails）。

### 8.2 release.yml（推送 tag `v*` 触发）

矩阵：`macos-latest`（universal）+ `windows-latest`（x64）→ tauri-action 构建、用 Secrets 中的 minisign 私钥签名 → 上传 dmg / nsis-exe / app.tar.gz / latest.json 至 GitHub Releases。发版动作 = 改版本号 + 打 tag，其余全自动。

---

## 9. AI 开发工作流约定（决策 #3 的落地）

代码 100% 由 Claude Code 编写时，正确性依赖"机器可验证的约束"而非人工经验，因此：

### 9.1 CLAUDE.md（仓库根，项目初始化时创建）

内容：常用命令（dev/build/test/lint/绑定生成 一键脚本）；架构铁律（业务真相在 Rust、单写者、前端只走 lib/ipc/，**每条铁律都有 guardrails/CI 对应检查**）；IPC 绑定生成流程（bindings.ts 禁手改）；错误处理规范（unwrap/expect 由 Cargo lints 强制禁止，统一 thiserror）；提交前必须全绿的门禁清单；审查协议与测试完整性规则（见 9.2）。

### 9.2 开发循环与审查协议（R10）

- 小步提交 + 分支 PR 流：每个功能 = 实现 + 测试 + 门禁全绿 + 审查通过才可 merge（main 分支保护，即使单人开发）；引擎类逻辑先写测试用例再实现。
- **实现与审查分离**：任务完成后由全新上下文会话执行 /code-review（引擎/数据层用 high 档），发现项修复后 merge——同一上下文自审会继承同样盲点。M2、M4 与发版前由用户触发 /code-review ultra 云端深审。
- **测试完整性规则**：禁止为让 CI 变绿而修改/删除/放宽既有测试断言；确需改动须在 PR 中说明，审查重点核对。
- **变异测试关卡**：M2 出口与 v1.0.0 前用 cargo-mutants 跑 engine/ + ids/，存活变异体逐个补测试或书面豁免——这是对"AI 写的测试是否真的在测"的最终检验（属性测试 proptest 覆盖状态机/号池/导入器不变量，日常防同类问题）。

### 9.3 测试重点（引擎是皇冠，优先覆盖）

1. 状态机：全部合法/非法迁移。
2. 调度：多 Key 并发上限不被突破；两种策略的分配正确性；冷却与恢复。
3. 重试：各错误类型的重试/换 Key 行为；重试预算耗尽后的终态。
4. 恢复：模拟中断后重启的状态修复与队列续跑。
5. 号池：发放、回收、并发发放不重号。

HTTP 层全部对着 wiremock 假服务跑（可注入超时/429/违规/坏响应），不依赖真实 API。前端 vitest 覆盖纯逻辑（筛选、映射、格式化），页面交互以人工冒烟 + 设计稿对照为主。

### 9.4 可调试性

tracing 结构化日志滚动落盘（任务 ID 全链路贯穿）；设置页提供"打开日志目录"；前端未捕获错误经命令转发进同一日志流——AI 修 bug 的输入就是这份日志。

---

## 10. 实施顺序建议

1. **M1 骨架**：Tauri + 前端栈初始化、CLAUDE.md、CI 门禁、SQLite 迁移与数据层、日志。
2. **M2 引擎**（核心风险最先证伪）：Provider + 调度/重试/恢复 + 全套引擎测试（对 wiremock），命令行级驱动跑通 1→500 任务。
3. **M3 业务页**：按原型 handoff 实现八大页面（图片生成/任务队列/图片验收/作品库/提示词库/参考图库/废纸篓/设置）+ 导入 + 验收流。
4. **M4 发布链**：updater 接 GitHub Releases、双端安装包、真机安装/更新/中断恢复演练。

> 任务级拆解、IPC/事件契约、逐页实现规范与验收标准见《内部图片生产工具_V1开发执行计划.md》。

## 11. V2 架构预留（V1 不实现）

| V2 规划         | 技术路径                                                                           | V1 预埋（已含在上文设计中）                        |
| ------------- | ------------------------------------------------------------------------------ | -------------------------------------- |
| Python 开源项目集成 | Sidecar 子进程（PyInstaller 或内置运行时），stdio JSON-RPC / 本地 HTTP，Rust 管生命周期；不用 PyO3 嵌入 | 命令与引擎模块化，进程管理可作为新模块加入                  |
| 视频/图文/文案/标题管理 | 数据模型加法；ffmpeg/ffprobe sidecar 截帧与元数据；检索加 SQLite FTS5                           | 多态标签表；资产"外部路径引用"（视频留在原文件夹，存路径+指纹）      |
| RPA 发布任务单     | 查库 → 导出 xlsx/csv（影刀类 RPA 对 Excel 支持最好）+ JSON；素材列写绝对路径；预留回执导入闭环                 | 稳定 ID 贯穿批次/任务/资产；确定性输出目录与命名本身就是 RPA 契约 |

## 12. 风险清单

| 风险                                | 对策                                  |
| --------------------------------- | ----------------------------------- |
| GitHub Releases 国内访问波动            | 多端点回退预留（6.3）；更新失败不影响使用，下次启动重试       |
| minisign 私钥丢失 → 更新链断裂             | Secrets + 离线双备份（6.2）                |
| WKWebView / WebView2 渲染差异         | 避免仅 Chromium 实验特性；双端各留一轮 UI 冒烟      |
| 未签名安装摩擦（SmartScreen / Gatekeeper） | 已接受；随包附首次安装引导（7.1/7.2），仅首次          |
| AI 编码的回归风险                        | 第 8、9 节门禁 + 引擎测试全覆盖；lockfile 锁版本    |
| 500 张批量的内存/磁盘压力                   | 流式落盘、缩略图分级加载、虚拟滚动（3.4）              |
| 双开导致队列/号池冲突                       | single-instance 插件 + 单写者事务（第 1 节铁律） |

