# 内部图片生产工具 V1 需求文档

> 修订说明（2026-07-08）：结合最新技术选型文档定稿过程中的产品决策，补充了视觉/交互体验要求、平台细节，并新增"后续规划方向"一节，明确 V1 不实现但需要架构预留空间的内容。功能范围本身未变化。

## 1. 背景

本软件是一个 Windows 和 Mac 双端桌面软件，第一批用户为个人本地使用。软件长期会扩展更多工作能力，但 V1 只聚焦图片批量生成、提示词库、参考图素材库、图片验收和本地输出。

V1 的产品本质是一个稳定的本地图片生产流水线：

参考图素材库 -> 提示词分组 -> 批量生成任务 -> 进度追踪 -> 人工验收 -> 合格图片输出与归档

## 2. V1 核心目标

### 2.1 功能稳定性目标

V1 最重要的目标是稳定完成 1 到 500 张规模的批量图生图任务。

重点保障：

- 多参考图和多提示词组合后，任务数量计算准确。
- 多个 API Key 可以稳定分摊任务。
- 高并发下任务状态不混乱。
- 失败、重试流程清晰可靠。
- 通过验收的图片和对应提示词可长期追溯。
- 未通过内容不进入正式资产库。

### 2.2 视觉与交互目标

V1 对前端 UI/UX 有明确的体验要求，不是"功能可用即可"的原型级观感：

- 视觉需要高效、精美、颜值高、有现代感。
- 交互需要有动效，状态变化和操作反馈应有恰当的过渡呈现。

这一要求在原型设计和实现阶段都需要作为硬约束落实，而不是留到后期再补。

## 3. 用户与使用边界

### 3.1 用户

- 第一批用户：个人使用。
- 使用方式：本地单机独立使用。

### 3.2 V1 不做

- 不做多人协作。
- 不做云端同步。
- 不做权限系统。
- 不做提示词版本管理。
- 不做提示词变量模板。
- 不做数据库和图片文件夹备份迁移。
- 不做自动图片质量判断，验收以人工为主。

### 3.3 后续规划方向（V1 不实现，但架构需预留扩展空间）

以下内容不在 V1 范围内，V1 不需要为此设计具体功能，但技术架构和数据结构需要考虑后续接入的可能性，避免做出难以扩展的设计：

- **接入 Python 开源项目**：后续可能会将部分 Python 开源项目集成到软件中，作为补充功能。
- **内容管理功能扩展**：后续可能新增视频管理、视频封面管理、图文管理、文案管理、标题管理。
- **RPA 任务单生成功能**：日常需要发布短视频/图文任务，V1 生成的图片可能被用作视频封面素材或图文发布素材，因此后续需要能生成供 RPA 执行的任务单，任务单可能包含：
  - 图文任务：图片素材 + 标题 + 文案 + 标签。
  - 视频任务：视频文件 + 视频封面 + 标题 + 标签等；由于视频不会全部导入软件，可能仍存放在本地文件夹中，任务单需要包含视频文件路径。

## 4. 平台与更新

### 4.1 平台

- 支持 Windows 桌面端和应用内更新，仅支持 Windows 10 和 11。
- 支持 Mac 桌面端和应用内更新。
- Windows 端不购买代码签名证书，按内部小规模分发场景处理安装引导与安全提示（首次运行可能触发 SmartScreen 拦截提示）。
- Mac 端不考虑 Apple Developer证书签名 ，按内部小规模分发场景处理安装、签名和安全提示。

### 4.2 应用内更新

启动软件时检查更新。

检查到新版本后：

- 后台自动下载安装包。
- 下载完成后，在前台提示用户重启并安装更新。
- 用户确认后执行更新安装流程。

## 5. 参考图素材库

### 5.1 功能定位

参考图不是一次性临时文件，需要进入本地素材库，后续可以重复使用。

### 5.2 需求清单

- 支持导入参考图。
- 支持查看参考图。
- 支持选择多张参考图进入生成任务。
- 参考图在本地保存，作为后续批量生成的可复用素材。
- 参考图需要和生成任务、通过验收的图片建立关联。

## 6. 提示词库

### 6.1 功能定位

提示词是核心生产资产。V1 的提示词库以分组、标签和场景为主要管理维度，而不是以单条提示词原文作为主视觉对象。

### 6.2 展示规则

提示词库页面按分组展示。

主题视觉模块应突出：

- 分组名称。
- 标签信息。
- 场景分类。
- 提示词数量。

提示词原文不是主视觉内容，可以作为详情信息查看，但不作为列表或卡片的主要展示对象。

### 6.3 管理规则

- 支持提示词分组。
- 支持提示词标签。
- 支持提示词编号（和图片一一对应）。
- 支持查看某个分组或标签下通过验收的图片作品。
- 支持收藏或删除已通过验收的提示词记录。
- V1 不支持提示词版本管理。
- V1 不支持变量提示词，只支持纯文本提示词。
- 提示词库页面不提供单条提示词新增、撰写和录入功能。但提供编辑功能。

### 6.4 提示词文件导入

提示词库页面需要支持文件导入。

支持格式：

- txt

导入时需要解析并匹配字段，建议支持以下信息：

- 分组。
- 标签。
- 提示词正文。

导入后，提示词应自动归入对应分组和标签。若文件缺少分组或编号，需要提供默认分组或自动编号规则。

## 7. 图片生成页

### 7.1 最小工作流

选择参考图 -> 选择提示词分组 -> 批量生成 -> 人工验收 -> 导出合格图

### 7.2 提示词选择方式

图片生成页不以单条提示词逐一勾选作为主要交互。

主要选择方式为：

- 按分组选择提示词。

用户选择一个提示词分组后，系统使用该分组内的提示词参与批量生成。

### 7.3 临时提示词导入

图片生成页也需要支持提示词文件导入，格式同提示词库：

- txt

导入后的提示词可以直接用于当前生成批次，并按文件中的分组、标签解析匹配。

是否同步写入提示词库，可在验收通过后自动导入提示词库。

另外，不管是在提示词库还是图片生成页面导入的提示词，导入成功后，都会自动获得软件内部的唯一ID编码（进入废纸篓后被删除的自动回收编码）。格式为：分组前缀YS-0001递增。

## 8. 批量生成规则

### 8.1 组合规则

多张参考图可以共用同一组提示词。

任务生成规则为参考图和提示词的组合：

- 参考图 A、B。
- 提示词 1、2、3。
- 生成任务为 A1、A2、A3、B1、B2、B3。

### 8.2 任务规模

单次批量生成规模为 1 到 500 张不等。

系统需要支持任务排队和并发执行，而不是一次性无控制地同时请求所有任务或单条排队。

## 9. 模型与 API 配置

### 9.1 接入方式

模型接入采用以下配置形式：

- API Key。
- Base URL。
- 模型名称GPT-Image2。

接口形态按 OpenAI API 端点方式设计。当前目标为 GPT-image 2，后续可接入其他兼容生图模型（暂不支持）。

### 9.2 API Key 管理

用户可能配置 5 到 10 个 API Key。

每个 API Key 支持单独配置：

- Base URL。
- 模型名称。
- 并发上限。
- 启用或停用状态。

每个 Key 的并发上限可设置为 1 到 10。

## 10. 任务调度与并发

### 10.1 调度目标

任务调度需要在多个 API Key 之间稳定分摊生图任务。

### 10.2 调度策略

V1 支持两种策略：

- 平均轮询：任务平均分配到可用 API Key。
- 成功率优先：优先使用历史成功率更高的 API Key。

### 10.3 任务状态

每个生成任务需要有明确状态：

- 待生成。
- 生成中。
- 重试中。
- 成功。
- 失败。
- 待验收。
- 已通过。
- 未通过（直接进入废纸篓）。

任务列表需要展示：

- 当前状态。
- 使用的参考图。
- 使用的提示词分组和提示词ID编号。
- 使用的 API Key 标识。
- 生成进度。
- 失败报错原因。
- 重试次数。

## 11. 失败重试与故障切换

### 11.1 重试设置

用户可以设置任务失败后的重试次数。

V1 默认规则：

- 网络超时触发一次重试。
- 接口限流触发一次重试。
- 提示词违规触发一次重试。
- 第二次仍失败时，任务报错中断并保留错误原因。

### 11.2 故障切换

重试时允许切换到另一个可用 API Key 重新生成。

后续可根据真实错误类型继续扩展重试规则。

## 12. 中断恢复

如果软件关闭、崩溃或系统重启，再次打开后需要恢复任务现场。

恢复内容包括：

- 继续未完成任务。
- 保留已生成但尚未验收的图片。
- 保留任务状态。
- 保留失败原因。
- 保留参考图、提示词、模型参数和 API Key 调度记录。
- 因软件关闭、崩溃或系统重启导致的任务中断的提示词，需提示因任务中断请重试；队列中的未开始任务不能因此被清空，依然处于排队队列，重启后继续。

## 13. 图片验收

### 13.1 验收方式

生成完成后进入人工验收。

验收操作包括：

- 通过。
- 不通过。
- 重试。

### 13.2 重试前微调

如果某张图细节不满意，用户点击重试前可以临时微调提示词，再重新生成。

微调后的提示词用于本次重试任务。微调后验收通过，默认按微调版本进入提示词库。

### 13.3 通过规则

通过验收的图片需要保留：

- 图片文件。
- 对应提示词。
- 提示词分组、标签和编号。

### 13.4 未通过规则

未通过图片直接淘汰，不进入正式资产库。

未通过内容不保留原图，不保留为可追溯作品记录。相关生成图片和提示词进入废纸篓的待清理状态，清理后不可恢复。

## 14. 图片输出与归档

### 14.1 输出条件

只有通过验收的图片才输出到本地文件夹。

### 14.2 输出目录

按生成批次创建输出文件夹。

### 14.3 文件命名

文件名规则：

参考图名_日期_提示词编号

示例：

productA_260708_PH0001.JPG

### 14.4 历史管理

通过验收的图片和提示词记录长期保留。

用户可以对通过作品进行：

- 查看。
- 收藏。
- 删除。

V1 暂不考虑备份、迁移和云端同步。
