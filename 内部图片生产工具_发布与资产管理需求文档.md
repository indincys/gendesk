# 内部图片生产工具 发布与资产管理模块 需求文档（V1 草案）

> 本文档是「资产管理 + 任务单编排 + 回执对账」模块的权威功能来源，供原型设计与后续开发使用。
> 配套文档：`docs/prototype/发布模块_设计输入摘要.md`（原型设计输入）、
> `docs/收件箱收录格式规范.md`（内容生成端的落盘格式契约，可立即投入使用）。
> 现有八页（设置/生成/任务/验收/作品/提示词/参考图/废纸篓）不受本模块改动。

## 1. 背景与定位

### 1.1 业务现状

- 每天需要在多个平台（抖音、小红书、快手、视频号等）发布短视频与图文内容。
- SKU 超过一百个，冷热不均：热门款高频发布，冷门款低频轮播。
- 图片素材 100% 由 AI 生成；文案与标题在 Claude / Codex 中通过 Skill 生成，以 TXT 文件落盘。
- 发布执行由外部工具承担：Windows 上的 RPA（如影刀）或 computer-use 类 agent，
  以「模拟真人操作平台网页/客户端」的方式发布。**明确不采用**平台开放 API 与
  Playwright/CDP 一类浏览器协议自动化。

### 1.2 模块定位（分工边界）

- **GenDesk 是大脑**：SKU 与内容资产管理、任务单编排生成、任务包导出、回执回收对账、看板日报。
- **执行器是手**（外部，不在本模块范围）：读取任务单 xlsx，逐行拟人发布，把结果回写进任务单并留截图。
- **两侧唯一契约**：一个同步根目录 + 任务单 xlsx 文件规格（见 §5）。执行器不访问 GenDesk 数据库。

### 1.3 明确不做（V1）

- 不做发布执行本体（RPA 流程、agent 编排属于执行侧工程）。
- 不接平台开放 API，不做浏览器协议自动化。
- 不做验证码、登录、风控的任何自动绕过；登录/验证码由人工在执行机处理。
- 不做发布数据（播放/点赞）回收——列入 V2 预留（§10）。

## 2. 核心概念与分类模型

### 2.1 三层分类逻辑

| 层 | 认什么 | 说明 |
| --- | --- | --- |
| 管理层 | SKU | 一级分类。资产管理主视图按 SKU 组织，每个 SKU 下挂自己的内容资产 |
| 存储层 | 内容池 | 二级分类。SKU 内按内容类型分池：素材池、标题池、正文池 |
| 发布层 | 日内容套装 | 运行时组合。排期时每天每 SKU 选定一套统一内容，任务只存引用 |

**没有独立于 SKU 的全局文案库/标题库作为存储结构**；跨 SKU 按类型浏览是视图能力。
另设一个「通用」分组，收纳不挂具体 SKU 的内容（如节日文案）。

### 2.2 内容类型规则（已拍板）

- **短视频任务**：只需要标题（+ SKU 固定话题标签）。不消费正文池。
- **图文任务**：需要标题 + 正文（+ SKU 固定话题标签）。
- **话题标签是 SKU 的固定属性**，不是池：每个 SKU 维护一组固定话题标签（最多 5 个进任务单），
  所有平台、所有发布共用。不做话题变体轮换。

### 2.3 日内容套装（关键决策）

每天生成任务单时，**每个 SKU 只选定一套内容**：一个素材包 + 一条标题（图文再加一条正文）。
该 SKU 当天所有平台、所有账号的任务统一使用这套内容。

- 理由：资产追溯简单（某天某款发了什么，一查一条记录）；任务单肉眼可核对；素材文件在任务包内
  按 SKU 只需一份，不按任务重复拷贝。
- 变体轮换发生在「天」与「天」之间：今天用套装 A，明天轮到套装 B（选取规则见 §4.2）。
- 注意事项：同平台多账号当天发布相同内容存在平台查重风险。缓解手段：排期时同平台多账号
  自动错开时段；「每账号独立变体」作为开关列入 V2 预留，V1 不做。

### 2.4 词汇表

- **SKU 档案**：一个款式的主数据（编码、款式名、商品名、冷热分层、固定话题标签、状态）。
- **素材包**：一次可发布的素材单元。视频型 = 1 个视频 + 可选封面；图集型 = N 张图 + 可选封面。
- **标题条目 / 正文条目**：标题池、正文池中的一条记录，可带平台标签（小红书/抖音/通用）。
- **日内容套装**：某天某 SKU 选定的（素材包 + 标题 [+ 正文]）组合，当天统一使用。
- **任务单**：某天全部发布任务的集合，物化为一个 xlsx 文件；一行 = 一次发布。
- **任务包**：任务单 + 该日全部素材文件 + 执行说明的自包含文件夹，是交给执行器的交付物。
- **回执**：执行器回写进任务单的「任务状态 + RPA 回写信息」两列，加上截图文件。
- **使用台账**：记录「某套装何时在哪个平台哪个账号发过」，驱动查重窗口与素材生命周期。
- **查重窗口**：同素材包同平台的最短复用间隔（默认 30 天，可设置）。
- **收件箱**：根目录下的落盘目录，Claude/Codex 生成的 TXT 与外部 AI 图片放入后被自动收录。
- **待认领**：收件箱中无法关联到已知 SKU 的内容，进入待认领队列由人工处理，不丢弃。

## 3. 资产管理

### 3.1 SKU 档案

字段：SKU 编码（唯一，如 `SF-YD-201`）、款式名、商品名、冷热分层（热/温/冷）、
固定话题标签组（有序，导出时取前 5 填任务单话题列）、状态（在售/停发）、备注。

需求清单：

- SKU 列表为资产库主视图：显示分层标签、各池余量（素材 n / 标题 n / 正文 n）、预警标记。
- 支持新建、编辑、停发（停发后不再参与排期，资产保留）。
- 冷热分层可手动调整，立即影响后续排期（不追溯已生成任务单）。
- 固定话题标签在 SKU 详情中维护；收件箱 TXT 携带【话题】时：SKU 尚无标签则采纳前 5 个，
  已有则忽略并在收录报告中提示差异。

### 3.2 素材池

- 素材包类型：视频型（video + 可选 cover）、图集型（img_01…img_NN + 可选 cover）。
- 封面可选：缺封面时任务单封面列留空，由平台自动取帧/首图。
- 生命周期：**新入库**（待完善，未参与排期）→ **可用** → **已用尽**（全部目标平台均在查重
  窗口内）→（窗口期满自动回**可用**）；**退役**为人工终态，不再排期。
- 「已用尽/回可用」由使用台账自动驱动，无需人工管理冷却。
- 被「未关闭任务单」引用的素材包锁定：禁止改名、删除、退役（Rust 业务层强制）。
- 素材文件由 GenDesk 统一规范化命名（ASCII，见 §7.3），原始文件名存档案字段。

### 3.3 标题池 / 正文池

- 条目字段：文本、可选平台标签（小红书/抖音/快手/视频号/通用）、来源（收件箱/手动）、使用计数。
- 消费规则：视频任务只消费标题；图文任务消费标题 + 正文。
- 套装选取时优先匹配目标平台标签的条目，无则回退「通用」。
  （注：日内容套装当天跨平台统一，平台标签的匹配以该 SKU 当天的主要平台或「通用」优先，
  细则见 §4.2。）
- 支持手动新增、编辑、停用单条条目。

### 3.4 使用台账

- 粒度：日内容套装。记录：SKU、素材包、标题、正文、平台、账号、任务 ID、实际发布时间、发布链接。
- 与业务库写入同事务（沿用单写者事务铁律）。
- 用途：查重窗口判定、素材生命周期驱动、SKU 详情中的发布历史展示。

### 3.5 余量预警

- 每 SKU 每池设预警阈值（默认：素材 < 2、标题 < 3；正文仅对需要图文的 SKU 生效）。
- 预警出现在：SKU 列表标记、资产库导航徽章、任务单生成时的缺料清单（§4.3）。

### 3.6 收件箱自动收录

- 监听根目录 `收件箱/`（Rust `notify`），防抖等待文件写入完成后解析。
- 识别顺序：文件头【SKU】>【文件名前缀】> 文件夹名，三处冗余任一命中即可。
- TXT 三种类型：标题文件（一行一条）、正文文件（`====` 分隔多条）、图文套装文件
  （【标题】+【正文】成套）。图片/视频文件按所在 SKU 文件夹进素材池。
- 成功收录后原文件移入 `收件箱/已收录/` 归档；解析失败保留原文件并进「待人工确认」；
  未知 SKU 进「待认领」。
- 格式细节以 `docs/收件箱收录格式规范.md` 为准（该文档同时是 Claude/Codex Skill 的输出契约）。

### 3.7 作品库联动

- 现有作品库中验收通过的图片支持「一键入资产库」：选择目标 SKU，打包为图集型素材包。
- GenDesk 自产图片不经收件箱。

## 4. 任务单编排

### 4.1 策略配置

- 分层频率：热款（默认每天 1 次 × 指定平台集）、温款（每周 N 次）、冷款（轮播池，每周轮出 N 个）。
- 平台矩阵：全局启用的平台清单；每 SKU 可覆盖（如某款只发小红书）。
- 账号档案：归属平台、账号名称（任务单「平台账号名称」列的值）、每日发布上限（默认 3）、
  可用时段、状态（正常/停用）。
- 时段模板：如「11:30–13:00 / 18:00–20:00 / 21:00–22:30」，分配时在时段内随机抖动。

### 4.2 日内容套装生成

- 触发：每晚定时（默认 22:00，可设置）或手动「生成明日任务单」。
- 对每个当日应发的 SKU，从其池中选一套：素材包 + 标题（+ 正文，若含图文任务）。
- 选取规则：查重窗口过滤 → 最少使用优先 → 同分即随机。标题/正文优先匹配当天目标平台的
  平台标签，跨平台冲突时取「通用」条目优先。
- 无可用素材/标题的 SKU 不排任务，进入缺料清单。

### 4.3 排期展开与约束

- 展开：日内容套装 × 启用平台 × 该平台账号 → 候选任务行。
- 约束过滤：账号每日上限；同平台多账号自动错开时段（最小间隔可设置，默认 ≥ 60 分钟）。
- 时段分配：按时段模板分配「定时发布时间」，加随机抖动；留空表示立即发（人工改出）。
- 输出：任务单草稿 + 缺料清单。缺料是副产物而非报错——有料的 SKU 照常出草稿。
- 排期器为 Rust 纯函数，约束不变量以 proptest 覆盖（同号池/状态机测试思路）。

### 4.4 编排工作台（人工环节，每日一次）

- 按时间轴或按平台分组预览任务行：封面缩略图 + 标题 + 话题 + 账号 + 时间。
- 可执行动作：整包换某 SKU 的套装（重选素材/标题/正文）、改任务时间、删任务行、增补任务行。
- 「确认」后任务单锁定进入待导出；「导出任务包」生成交付物（§4.6）。

### 4.5 状态机

任务单（sheet）状态：**草稿**（编辑中）→ **已确认**（锁定）→ **已导出**（待执行）→
**回收中**（部分回执）→ **已关闭**（全部终态）。

单任务状态：**待执行**（初值）→ **已发布** / **失败**（执行器回写）；
**疑似已发**（GenDesk 对账超时标记，见 §6.4）；**已取消**（人工）。

视觉沿用现有 8 态 → 5 视觉组映射：待执行=灰、已发布=绿、失败=红、疑似已发=琥珀、已取消=灰；
任务单级：草稿=灰、已确认/已导出/回收中=蓝、已关闭=绿。

### 4.6 任务包导出

目录结构（示例，包名 = 日期）：

```
任务包/20260714/
├── 任务单.xlsx          ← §5 规格；执行器逐行执行并就地回写
├── 执行说明.md          ← 给执行器（人或 agent）的操作约定，GenDesk 生成
├── READY.txt            ← 全部文件落盘后最后写入的就绪标志
├── 素材/
│   └── SF-YD-201/       ← 按 SKU 组织（日内容套装统一，素材每 SKU 一份）
│       ├── video.mp4    （或 img_01.jpg…img_06.jpg）
│       ├── cover.jpg    （可选）
│       └── body.txt     （图文任务的正文；视频任务无此文件）
└── 回执截图/
    └── T260714-001.png  ← 执行器写入，按任务 ID 命名
```

- 导出 = 复制素材到包内 + 按「执行机根路径」设置拼接绝对路径写入 xlsx（§7.3）。
- 正文以 `body.txt` 物化（避免 Excel 单元格内换行的 RPA 兼容问题），任务单存其路径。
- `READY.txt` 最后写入；执行器只处理含就绪标志的包，且每行执行前校验素材文件存在且大小 > 0。

## 5. 任务单文件规格（两侧契约，22 列）

| 列 | 字段 | 必填 | 说明 | 来源 |
| --- | --- | --- | --- | --- |
| 1 | 任务 ID | ✓ | `T260714-001`，全程唯一，贯穿对账 | GenDesk |
| 2 | 任务日期 | ✓ | `2026-07-14` | 排期 |
| 3 | 定时发布时间 | | `12:30`；留空 = 立即发布 | 排期 |
| 4 | 发布平台 | ✓ | 抖音 / 小红书 / 快手 / 视频号… | 排期 |
| 5 | 平台账号名称 | ✓ | 执行器据此选账号 | 账号档案 |
| 6 | SKU 款式名 | ✓ | | SKU 档案 |
| 7 | SKU 编码 | ✓ | `SF-YD-201` | SKU 档案 |
| 8 | 商品名称 | ✓ | | SKU 档案 |
| 9 | 内容类型 | ✓ | `视频` / `图文`，执行器据此分流程 | 排期 |
| 10 | 视频/图片文件名 | ✓ | `video.mp4` 或 `img_01.jpg…img_06.jpg` | 素材包 |
| 11 | 素材本地路径 | ✓ | 素材所在目录，执行机视角绝对路径 | 导出拼接 |
| 12 | 封面本地路径 | | 无封面留空，平台自动取帧 | 导出拼接 |
| 13 | 标题 | ✓ | 视频与图文均必填 | 日内容套装 |
| 14 | 正文文件路径 | | 图文必填（指向 body.txt）；视频留空 | 导出拼接 |
| 15–19 | 话题标签一～五 | | SKU 固定话题标签，取前 5，不足留空 | SKU 档案 |
| 20 | 任务状态 | ✓ | 初值 `待执行`；执行器回写 `已发布` / `失败` | 双方 |
| 21 | RPA 回写信息 | | `发布链接｜失败原因｜完成时间` | 执行器 |
| 22 | 截图文件名 | | `T260714-001.png`，对应 `回执截图/` | 执行器 |

规则：执行器**只写第 20–22 列**，其余列只读；`疑似已发` 只由 GenDesk 在对账时标记，
执行器永不写该值；xlsx 为唯一载体（不用 CSV，规避 Windows 编码问题）。

## 6. 回执闭环与对账

### 6.1 回执载体与单写方约定

- 回执 = 任务单 xlsx 第 20–22 列 + `回执截图/` 目录，无独立回执文件格式。
- 任一文件任一时刻只有一个写方：xlsx 导出前归 GenDesk、导出后归执行器、
  任务单关闭归档后只读。避免同步工具产生冲突副本。

### 6.2 回收对账

- GenDesk 监听任务包目录中 xlsx 的变更（同收件箱 watcher 基建），按任务 ID 逐行对账；
  提供「手动导入回执」按钮兜底。
- 三分支：
  - 回写为**已发布** → 记使用台账 + 归档链接/截图/实发时间。
  - 回写为**失败** → 按 §6.3 分类处置。
  - **超时未回写**（任务计划时间 + 可设置超时，默认 4 小时）→ 标记**疑似已发**。

### 6.3 失败分类与处置

六类（对应现有错误分类思想的发布版）：登录失效 / 风控拦截 / 素材不合规 / 页面变更 /
网络超时 / 其他。处置：网络超时可自动重排次日；风控拦截当日熔断该账号（该账号剩余任务改期）；
素材不合规标记素材包并转人工；登录失效通知人工处理后续跑；页面变更/其他转人工。

### 6.4 疑似已发规则（硬性）

疑似已发的任务**绝不自动重发**（内容可能已实际发出，重发触发平台查重，比失败更糟）。
只能由人工到平台后台核实后手动定态为已发布或失败。看板以琥珀色突出待核对项。

### 6.5 看板与日报

- 今日看板：计划 / 已发布 / 失败 / 待核对计数，按平台与账号的完成率，账号健康标记。
- 任务单全部终态后自动关闭并生成日报（成功率、失败清单、缺料清单、明日建议）。
- 导航徽章（事件驱动，沿用现有机制）：发布计划页 = 待确认任务单 + 待核对数；
  资产库页 = 待认领 + 余量预警数。

## 7. 部署拓扑与路径规范

### 7.1 三种拓扑（均需支持）

| 拓扑 | 管理端 | 执行端 | 根目录同步 |
| --- | --- | --- | --- |
| A | macOS | Windows（另一台） | 同步软件（Syncthing / 坚果云 / OneDrive 等） |
| B | Windows | 同一台 Windows | 无需同步，两个根路径相同 |
| C | Windows | 另一台 Windows | 同步软件 |

GenDesk 已双端打包（macOS + Windows NSIS），无额外适配工作。

### 7.2 单根目录四分区

用户指定一个根目录（如 Mac 的 `~/Desktop/视频发布`、Windows 的 `D:\视频发布`），
GenDesk 在其中管理四个分区：`资产库/`、`收件箱/`、`任务包/`、（回执在任务包内）。
素材随生成持续同步，发布时执行端已有全量文件。

### 7.3 路径规则

- **相对路径是真相**：数据库与包内只存根目录内相对路径，绝对路径从不入库。
- **导出是唯一转换点**：`执行机根路径 + \ + 包内相对路径`，纯字符串拼接（管理端为 Mac
  时拼 Windows 路径无任何障碍），分隔符按执行端风格统一生成。
- **目录与文件名只用 ASCII**：SKU 编码、任务 ID、固定文件名（video.mp4 / cover.jpg /
  img_01.jpg / body.txt）。中文只出现在 xlsx 单元格内，绝不进文件名——一次性规避
  macOS/Windows 文件名 Unicode 规范化差异（NFC/NFD）、编码乱码与 RPA 转义问题。
- 包名短、层级浅（防 Windows 260 字符路径上限）；不含空格与 Windows 保留字符 `<>:"|?*`；
  统一小写规避大小写敏感差异。
- 根目录内结构变动只能由管理端 GenDesk 发起，执行端不得手动改名挪动。

### 7.4 设置项清单（设置页新增区块）

本机根路径、执行机根路径（同机模式提供「同本机」一键）、执行机路径风格（Windows `\`）、
查重窗口天数（默认 30）、回执超时小时数（默认 4）、每日生成时间（默认 22:00）、
余量预警阈值、账号默认日上限、同平台多账号最小间隔。

## 8. 页面与导航（原型设计输入）

新增两个导航项，与现有八页并列；视觉遵循 `docs/prototype/Design-Tokens.md` 与
`设计输入技术摘要.md` 的既有约束（浅色优先、Linear 式克制、信息密度高但有呼吸感）。

### 8.1 资产库页

- 主视图：SKU 列表（编码、款式名、分层标签、三池余量、预警标记、最近发布时间），
  支持按分层/预警/停发筛选与搜索。
- SKU 详情：档案编辑（含固定话题标签）+ 三池分页签（素材池网格缩略图 + 生命周期状态色、
  标题池/正文池列表 + 平台标签）+ 发布历史（读使用台账）。
- 收件箱状态区：待认领队列（指认 SKU 或丢弃）、解析失败清单（查看原文件 + 重试）。

### 8.2 发布计划页

- 三个分区/页签：**看板**（今日计划/完成/失败/待核对 + 账号健康 + 日报入口）、
  **任务单**（按日期列表 → 任务单工作台：行级预览编辑、换套装、确认、导出、对账状态）、
  **策略与账号**（分层频率、平台矩阵、账号档案与日限额、时段模板）。
- 任务单工作台是本模块的核心交互面，信息密度参照现有任务页。

### 8.3 设置页

新增「发布与同步」区块，承载 §7.4 设置项。

## 9. 数据模型草案（实现参考）

`skus`（档案 + 固定话题 JSON + 分层 + 状态）· `asset_packs`（类型/文件清单/生命周期，
复用 files 域）· `title_items` / `body_items`（文本 + 平台标签 + 使用计数）·
`accounts`（平台/名称/日限额/时段/状态）· `daily_sets`（日内容套装：日期 + SKU + 三引用）·
`task_sheets`（日期 + 状态）· `publish_tasks`（22 列对应字段 + 套装引用）·
`usage_ledger`（套装粒度台账）· `inbox_items`（收录记录/待认领/失败）。
全部沿用「Rust 域命令 + specta 绑定 + 事件推送 + 单写者事务」既有模式；
文件监听用 `notify`；无新 IPC 模式。

## 10. 非目标与 V2 预留

- 发布数据回收（播放/点赞）反哺冷热分层。
- 「每账号独立变体」开关（打破日内容套装的账号维度统一）。
- 话题标签按平台差异化。
- 文案 AI 变体在 GenDesk 内直接生成（复用现有 provider 基建）。
- 多执行机并行、执行说明.md 的 agent 深度定制。

## 11. 里程碑拆解

- **P1 资产管理**：SKU 档案 + 三池 + 使用台账 + 余量预警 + 收件箱收录 + 作品库联动 +
  资产库页。DoD：收件箱放入规范 TXT/图片可自动入库；SKU 列表展示余量与预警；门禁全绿。
- **P2 编排与导出**：策略配置 + 套装生成 + 排期展开 + 编排工作台 + 任务包导出 +
  发布计划页（看板可后置）。DoD：一键生成明日任务单草稿，人工调整确认后导出的任务包
  可被影刀直接消费；排期约束有 proptest 覆盖。
- **P3 回执闭环**：xlsx 回收对账 + 三分支处置 + 疑似已发 + 看板日报 + 导航徽章。
  DoD：执行器回写后自动对账入台账；超时任务正确标黄且不会被重发。

P2 完成后建议「陪跑一周」：GenDesk 出包 + 影刀执行 + 人工核对回执质量，再上 P3 自动回收。
