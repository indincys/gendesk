# 发布模块优化执行计划（35 项 · 六批次）

> 交接文档：每个「任务」自成一体，可用 `/goal` 单独指派给全新上下文的执行 Claude。
> 权威约定见 `CLAUDE.md`。执行前请先读 `CLAUDE.md` 的架构铁律、IPC 约定、审查协议（§1.4），
> 以及 `内部图片生产工具_发布与资产管理需求文档.md`（下称「需求」）。生成日期 2026-07-15。
> 来源：对 v0.8.0（main）发布与资产管理模块的三轮全量审查（需求逐条核对 + 后端
> `src-tauri/src/publish/` 与 7 个命令域 + 前端两页/store/设置区块）。
>
> 批次：**A 发版阻断**（5）→ **B 回执与导出正确性**（5）→ **C 排期算法**（5）→
> **D 需求缺口 UI 闭环**（7）→ **E 稳健与性能**（8）→ **F 新功能**（10 内容合并为 10 项）。
> A/B/C 为缺陷修复（其中 A1 为发版阻断级），D 为需求验收缺口，E/F 可按需排期。

---

## 通用约束（所有任务适用）

- **分支/提交/审查**：一个任务 = 一个分支 = 实现 + 测试 + 门禁全绿 + 全新上下文
  `/code-review` 通过 = 一次 merge（`main` 受保护走 PR）。纯前端任务 review 用 medium；
  涉及 Rust 引擎/数据层用 high。
- **提交前门禁**：`pnpm check` 必须全绿。触及 IPC 命令/事件签名后必须
  `cd src-tauri && cargo test --lib export_bindings` 重新生成并提交 `bindings.ts`。
- **架构铁律**：业务真相只在 Rust；前端只经 `src/lib/ipc/` 出入；事件驱动不轮询；
  `oklch(` 硬编码仅限 `globals.css`；非测试代码禁 `unwrap/expect/panic`。
- **测试完整性**：禁止为让 CI 变绿而改/删/放宽既有测试断言。现有 189 个 Rust 测试全部保绿。
- **硬性红线（需求 §6.4）**：疑似已发绝不自动重发/自动改态；任何改动不得触碰
  `resolve_suspect` 是唯一定态路径这一不变量（`reconcile.rs` 已有负向断言测试，必须保绿）。
- **落地顺序**：批次内按编号；批次间 A → B → C 串行（同改 `reconcile.rs`/`planner/`，
  避免冲突）；D 内 D7 先做（事件基建，其余 D 任务的刷新依赖它）；E/F 可与 D 并行。
- **迁移编号**：涉及 schema 的任务共用下一个可用迁移号（当前到 0011），
  执行时先 `ls src-tauri/migrations/` 确认，不同分支各自新建迁移文件避免冲突。

---

# 批次 A — 发版阻断修复

## A1 — 素材包生命周期闭环：新入库可转「可用」（发版阻断）

**分支**：`fix/pack-activation`　**Review**：high

### 背景与根因

入库默认 `lifecycle="new"`（`db/repo/assets.rs:62`），排期只选 `active`
（`publish/planner/set_picker.rs:126`）。后端有 `activate_pack`/`update_pack`
（`commands/publish_assets.rs:294,309`）但前端 `PackModal`
（`src/features/assets/AssetsPage.tsx:997`）没有任何激活入口 → 收件箱/手动导入/作品库
入库的所有素材包永不参与排期，每个 SKU 恒报「无可用素材包」。现有测试都是手动
`set_lifecycle("active")` 才通过的，掩盖了主链路断裂。

### 改动清单

1. **入库默认改 active**：`assets::insert` 的 lifecycle 初值 `new` → `active`
   （文件齐备即可发；「新入库待完善」门槛对本业务是伪需求——封面本就可选）。
   同步更新 `assets.rs:62` 注释与 `CLAUDE.md` 词汇表里 asset\_pack 的一句话描述。
2. **保留 new 态语义**给未来需要人工审核的来源：`PackModal` footer 在
   `derived === "new"` 时显示「标为可用」按钮 → `commands.activatePack(id)`。
3. **PackModal 补编辑入口**：封面选择（列出包内文件单选，调 `updatePack({cover})`）+
   备注输入（`updatePack({note})`）。`update_pack` 里「patch 即自动转 active」的隐式行为
   删掉（改显式激活后它只会造成意外）。
4. 迁移：一条 `UPDATE asset_packs SET lifecycle='active' WHERE lifecycle='new'`
   的 data migration（存量解锁）。

### 验收标准

- 新建临时库：收件箱放图 → 自动入库 → 不做任何人工操作 → `generate_sheet` 能选中该包
  （补一个端到端测试断言，替代现有测试里的手动 set\_lifecycle，至少加一例）。
- PackModal 可改封面/备注；new 包可一键转可用。`pnpm check` 全绿。

## A2 — 时区修正：本地时间不再按 UTC 解析

**分支**：`fix/reconcile-timezone`　**Review**：high

### 背景与根因

`publish/reconcile.rs:53`（回执时间）与 `:267`（超时基准）用
`NaiveDateTime::parse → and_utc()`，把北京时间当 UTC：疑似已发延迟 8 小时才标记
（12:30 计划 + 4h 超时 → 实际次日 00:30 才标）；台账 `published_at` 记成未来 8 小时。

### 改动清单

1. 两处改 `chrono::Local.from_local_datetime(&dt).single()`（歧义/无效时刻回退
   `earliest()`，再退 now/export\_at）。抽一个 `local_naive_to_unix(dt) -> Option<i64>`
   放 `reconcile.rs` 内并单测。
2. 排查同类：`grep -rn "and_utc()" src-tauri/src/publish/` 逐处确认语义
   （`ticker.rs` 用 `Local::now().timestamp()` 是对的，不动）。
3. 测试：固定一个本地时区断言偏移（用 `chrono::FixedOffset` 注入或按环境时区计算期望值，
   不要写死 +8，CI 可能是 UTC）。

### 验收标准

- 单测：本地 12:30 解析出的 Unix 秒 = 该本地时刻真实 epoch；`timeout_scan` 在
  「计划时刻 + 超时」整点即触发（虚拟 now 注入已支持）。`pnpm check` 全绿。

## A3 — 收件箱语义修复：丢弃、逐文件容错、toast 纠错

**分支**：`fix/inbox-semantics`　**Review**：high

### 背景与根因

三个叠加缺陷：① `discard_inbox_item` 只删 DB 记录、文件留原位
（`commands/publish_inbox.rs:96-102` 注释自认）→ 下次任何收件箱事件触发 rescan，
文件被重新收录，「丢弃」的东西复活；② `ingest::rescan`（`inbox/ingest.rs:550`）
单文件错误 `?` 传播中断全部收录；③ `watcher.rs:129` 把 `sku_code` 塞进
`InboxIngestEvent.file_name`（unclaimed/failed 传空串），且 rescan 全量重扫使滞留的
失败/待认领文件**每次**都重发一遍 toast。

### 改动清单

1. **丢弃 = 移档**：新增 `paths::DISCARDED = "已丢弃"`（收件箱内子目录）；
   `discard_inbox_item` 把文件移入 `收件箱/已丢弃/{YYYYMMDD}/`（复用
   `archive_ingested` 的去重命名逻辑，抽成通用 `archive_to(root, file_rel, subdir)`），
   记录改 state=`discarded`（保留可追溯，不删行）。`rescan` 的 walkdir 排除
   `已丢弃`（与 `已收录` 同款过滤）。`list_inbox_items` 不返回 discarded。
2. **逐文件容错**：rescan 循环里 `ingest_txt` 错误 → `tracing::warn` + 记
   `IngestOutcome::Failed` 到该文件的 inbox\_items，继续下一个；返回值不变。
3. **事件纠错 + 去重**：`InboxIngestEvent.file_name` 一律传文件名末段（rescan 返回值
   需要带上 file\_rel，把 `rescan` 返回类型改为 `Vec<(RelPath, IngestOutcome)>`）；
   `rescan_and_emit` 只对**本轮状态发生变化**的条目发事件——实现：`record_item`
   返回「是否新建或状态变化」，rescan 透传该标志，watcher 只对 true 的发 toast。
4. 媒体归集同理纳入结果流（见 A4，两任务若同期执行请先合计划再动 `rescan` 签名）。

### 验收标准

- 测试：丢弃后触发 rescan，条目不复活、文件在 `已丢弃/` 下；一个坏文件不阻断其余收录；
  同一滞留文件连续两轮 rescan 只发一次事件。`pnpm check` 全绿。

## A4 — 未知 SKU 的媒体文件进「待认领」

**分支**：`fix/inbox-unclaimed-media`　**Review**：high

### 背景与根因

`collect_media` 未识别 SKU 直接 `return Ok(vec![])`（`inbox/ingest.rs:310-313`），
图片/视频静默滞留收件箱，UI 待认领队列看不到——违背需求 §3.6「进队列由人工处理，不丢弃」。
TXT 有待认领，媒体没有。

### 改动清单

1. `rescan` 步骤 1 里：`collect_media` 对未知 SKU 文件夹返回一个标记（改签名或新增
   `collect_media_or_unclaim`），对该文件夹内每个媒体文件（或以文件夹为单位一条记录，
   **建议按文件夹一条**，避免 50 张图刷 50 条）record 一条 inbox\_items：
   `kind="media"`、`file_rel=收件箱/{folder}`、state=`unclaimed`、
   detail 带文件计数。已有记录则跳过（`find_by_rel` 幂等）。
2. **认领媒体**：`claim_inbox_item` 现在只走 `ingest_txt`；按 `item.kind=="media"`
   分流：对文件夹调 `collect_media` 的内部管线但强制 SKU（给 `collect_media`
   加 `forced_sku: Option<&str>` 参数，跳过 `find_by_code_or_alias`），成功后
   记录转 ingested。前端 `ClaimModal` 无需改（同一命令）。
3. 丢弃媒体记录 → 整个文件夹移入 `已丢弃/`（依赖 A3 的 `archive_to`）。

### 验收标准

- 测试：收件箱放 `未知款/1.jpg` → rescan → 出一条 media 待认领；指认 SKU 后成包入资产库、
  记录转 ingested；丢弃后文件夹进 `已丢弃/` 且不复活。`pnpm check` 全绿。

## A5 — 路径安全：拒绝 `..`、Windows 保留名、大小写冲突

**分支**：`fix/path-safety`　**Review**：high

### 背景与根因

`paths::is_valid_sku_code`（`publish/paths.rs:189`）允许 `..`、`.`、`CON`/`COM1` 等：
① SKU 编码 `..` 使 `RelPath::from_parts(["资产库",".."])` 穿越出根目录（`RelPath::new`
只过滤空段不过滤 `..`，`paths.rs:80-93`）；② Windows 保留设备名会让执行机上
`素材/CON/` 目录不可创建；③ 编码大小写不唯一（`sf-1` 与 `SF-1` 并存），Windows
大小写不敏感文件系统上资产库目录冲突。需求 §7.3 要求「统一小写规避大小写敏感差异」。

### 改动清单

1. **RelPath 层兜底**：`RelPath::new` / `from_parts` 过滤段 `.` 并拒绝 `..`
   （含 `..` 的段直接剔除或整体判非法——选剔除+`debug_assert`，保持构造不可失败的现有
   签名；同时在 `to_local` 前无需再担心穿越）。补单测：`RelPath::new("a/../../b")`
   不越界。
2. **编码校验收紧**：`is_valid_sku_code` 追加：非纯点段；转小写后不属于
   `{con, prn, aux, nul, com1-9, lpt1-9}`；长度 ≤ 64。
3. **大小写唯一**：`create_sku` 查重改 `LOWER(code)` 比对（`find_by_code` 加
   `COLLATE NOCASE` 或单独查询）；迁移加唯一索引
   `CREATE UNIQUE INDEX idx_skus_code_nocase ON skus(LOWER(code))`（先写好存量
   冲突检测：迁移前 SELECT 分组计数，有冲突则迁移中止并提示人工改码——写进迁移注释与
   错误信息）。别名 `find_by_alias` 同理 NOCASE。
4. `sku_mapping.rs` 导入路径同样过这套校验（预检报告里进 errors）。

### 验收标准

- 单测覆盖 `..`/`.`/`CON`/`com1`/纯点/大小写重复六类拒绝；RelPath 穿越测试；
  存量库迁移幂等。`pnpm check` 全绿。

---

# 批次 B — 回执与导出正确性

## B1 — 导出预检清单 + 重导出回执保护

**分支**：`fix/export-preflight`　**Review**：high

### 背景与根因

① `exporter.rs:59` 允许 `exported` 状态重导出且整包覆盖 xlsx——执行器已回写的
20–22 列被抹掉，违背需求 §6.1 单写方约定；② `exporter.rs:100` 源文件缺失静默跳过，
任务单引用不存在的素材，问题推迟到执行机才暴露。

### 改动清单

1. **预检纯函数** `exporter::preflight(pool, sheet_id, s) -> PreflightReport`：
   逐行校验（a）素材包全部文件存在且 size>0（含封面/body 依赖）；（b）拼接执行机路径
   ≤ `PATH_LIMIT`；（c）涉及账号 status=active；（d）执行机根路径已配置且风格与
   `root_exec` 首字符一致（`D:\` ↔ windows、`/` ↔ unix，不一致给 warning）；
   （e）**重导出时**读包内现有 xlsx（`read_receipts` 容错 Ok(空)），任何一行状态非
   「待执行」→ `blocking` 级错误「检测到执行器回执，禁止覆盖；请先导入回执对账」。
   Report 分 `errors: Vec<String>`（阻断）与 `warnings: Vec<String>`。
2. `export_package` 开头调 preflight，errors 非空即拒绝（错误信息串起来）。
3. 新命令 `preflight_export(sheet_id) -> PreflightReport`（specta 登记 + 重生成绑定）。
4. **前端**：`PlanPage.tsx` 的导出 ConfirmModal 打开时先调 preflight，逐条渲染
   ✓/✗/⚠ 列表；有 error 时禁用「确认导出」。
5. `ExportResult` 增加 `missing_files: Vec<String>` 冗余上报（正常应为空）。

### 验收标准

- 测试：删掉一个包内文件 → preflight 报 error、export 拒绝；模拟执行器回写后重导出被拒；
  对账完成关单后不再可导出（现有状态机保证，补断言）。`pnpm check` 全绿。

## B2 — 任务状态机收口：取消校验 + 熔断误报

**分支**：`fix/task-state-machine`　**Review**：high

### 背景与根因

① `cancel_task_row`（`commands/publish_planning.rs:279`）无状态校验，可把
published/failed/**suspect** 改成 canceled（绕过 §6.4 唯一定态路径），且不调
`maybe_close` → 单据可能永不关闭；② 看板把「有任一 canceled 任务」判为账号熔断
（`publish_reconcile.rs:199-201`），人工取消也亮「当日熔断」。

### 改动清单

1. `cancel_task_row`：仅允许 `status=="pending"`，否则 `InvalidInput`；成功后调
   `reconcile::maybe_close(pool, sheet_id)` 再 emit。
2. **区分取消原因**：`publish_tasks` 加列 `cancel_kind TEXT`（迁移，值
   `manual|risk`）；`cancel_pending_of_account`（风控熔断路径）写 `risk`，
   人工取消写 `manual`。看板熔断判定改「存在 cancel\_kind='risk' 的行」。
3. 前端 Workbench：exported/reconciling 单的 pending 行加「取消」按钮（现在 UI
   没有暴露人工取消，需求 §4.5 有「已取消（人工）」态）——按钮带 ConfirmModal。
4. 补测试：对 suspect 行调 cancel 必须报错（加入 §6.4 负向断言组）；取消最后一个
   pending 后单自动关闭并出日报。

### 验收标准

- 上述测试绿；看板对人工取消不再显示熔断。`pnpm check` 全绿。

## B3 — 失败处置补全：超时补排次日 + 素材不合规标记

**分支**：`feat/fail-dispositions`　**Review**：high

### 背景与根因

需求 §6.3 六类处置只落了「风控熔断」一类。① timeout 类「自动重排次日」未实现，
但日报 tips（`reconcile.rs:368`）声称会补排——文案说谎；② content 类「标记素材包并
转人工」未实现，素材包明天还会被选中。

### 改动清单

1. **timeout 补排**：`generate_sheet` 选取阶段（`planner/mod.rs:136` 循环前）新增：
   查前一日（`date - 1`）任务里 `status='failed' AND fail_kind='timeout'` 的
   (sku\_id, platform, account\_id) 集合（新 repo 查询 `planning::timeout_fails_of_date`），
   这些 SKU **即使不在 due\_ids 也纳入**当日应发（并入 due 集合）；排期展开仍走正常
   约束（日限/间隔）。在 shortage\_json 同格式追加一条
   `{reason:"timeout_backfill", code, ...}` 供工作台展示「补排」徽标（复用
   ShortageItem，reason 枚举文档化）。
2. **content 标记**：`apply_receipts` 的 failed 分支里 `kind=="content"` 时，同事务
   `UPDATE asset_packs SET lifecycle='retired', note=note||'[素材不合规 T…]'`
   （事务内已知 set → pack\_id）。UI 上 retired + note 已可见（依赖 A1 的备注展示）。
3. **login 通知**：`kind=="login"` 时发一条现有 `SheetChangedEvent` 之外的提示——
   直接复用 F6 前不引新事件，先在 `ReconcileResult` 加 `login_fail_accounts: Vec<String>`，
   前端导入回执 toast 里列出「账号 X 登录失效，需人工处理」。
4. 日报 tips 改为按实际动作生成（补排了 N 个 → 写 N；没有则不写「自动补排」）。

### 验收标准

- 测试：昨日 timeout 失败 → 今日 generate\_sheet 该 SKU×账号出现且带 backfill 标记；
  content 失败 → 包转 retired；tips 与事实一致。`pnpm check` 全绿。

## B4 — 写路径事务化 + 输入校验

**分支**：`fix/planning-tx-validation`　**Review**：high

### 背景与根因

① `reroll_set`（`publish_planning.rs:419-432`）两条 UPDATE 非事务，中断留下
content\_kind 与套装不一致；`add_task_row` 的「建套装 + 插任务」同样跨两次 acquire；
② `update_task_row` 不校验 `planned_time` 格式（任意串进 xlsx / timeout\_scan
静默回退）；③ 设置 `autogen_time` 非法时后端 `parse_hm` 静默回退 22:00 但存储与
UI 仍是错值。

### 改动清单

1. `reroll_set` / `add_task_row` 全部写操作收进单个 `pool.begin()` 事务。
2. `update_task_row`：`planned_time` 用 `scheduler::parse_slot` 同款 `HH:MM` 校验
   （抽 `parse_hhmm(s) -> Option<(u32,u32)>` 到 scheduler 或 paths，供三处复用），
   非法报 `InvalidInput`。
3. `update_publish_settings`：`autogen_time` 走同一校验，非法直接报错（而非静默）；
   `sanitize()` 里对 time\_slots 逐条 `parse_slot`，剔除非法段时返回 warning 字段
   或至少 `tracing::warn`。
4. 前端设置输入框失焦校验（正则 `^\d{2}:\d{2}$` + 范围），错了红框提示不提交。

### 验收标准

- 单测：非法时间三处均被拒；reroll 中途失败无半写（用故意冲突模拟或代码评审确认
  事务边界）。`pnpm check` 全绿。

## B5 — 回执 xlsx 快照留底

**分支**：`feat/receipt-snapshots`　**Review**：medium

### 背景与根因

xlsx 是双侧唯一契约，会被执行器重写、被同步软件搬运；写坏/冲突副本覆盖后无据可查。

### 改动清单

1. `read_sheet_receipts`（`commands/publish_reconcile.rs:40`）成功读到非空回执且
   **与上次快照内容不同**时，把 xlsx 复制为
   `任务包/{date}/.snapshots/任务单.{unix_ts}.xlsx`（点前缀目录，watcher/收录均已
   忽略点开头；确认 `rescan`/pkg watcher 过滤逻辑覆盖目录名，不够就补）。
   「是否不同」用文件 bytes 哈希（`blake3` 已有依赖则用之，否则 `std` 手写 fnv 即可）
   对比最新快照。
2. 保留最近 20 份，超出删最旧。
3. 快照失败只 warn 不阻断对账。

### 验收标准

- 测试：两次内容不同的回写产生两份快照；相同内容不重复存；上限裁剪生效。
  `pnpm check` 全绿。

---

# 批次 C — 排期算法修正（全部纯函数，proptest 必须扩展覆盖）

## C1 — 查重窗口跨平台漏洞 + 文本回退「通用」

**分支**：`fix/dedup-and-text-fallback`　**Review**：high

### 背景与根因

① `pack_exhausted`（`set_picker.rs:74-85`）要求**所有**目标平台在窗口内才过滤；
只要有一个平台没发过，包就会被选中并展开到**全部**平台——包括 5 天前刚发过的那个，
直接违反「同素材包同平台 30 天」定义（需求 §2.4/词汇表）。② `pick_text`
（`set_picker.rs:102-106`）平台无命中时回退**全部**条目而非「通用」，抖音标签的标题
可能发到小红书（需求 §3.3「无则回退通用」）。

### 改动清单

1. 选包偏好三级：（a）首选「所有目标平台均出窗」的包；（b）无 a 时，其余非全用尽的包
   仍可选，但 `SetPick` 增加 `conflicted_platforms: Vec<String>`（窗口内的平台清单），
   `generate_sheet` 据此**把这些平台从该 SKU 当日展开中剔除**（DueSet.platforms 过滤），
   并在 shortage\_json 记一条 `{reason:"dedup_partial", code, platforms}` 供工作台提示；
   （c）全用尽维持 NoPack 缺料。这样窗口硬约束在任何路径都不被突破。
2. `pick_text` 回退池改为 `platform == GENERAL_TAG` 的条目；连通用都没有时才落到
   全部（保底不缺料，但排序仍最少使用；此保底行为写进函数注释）。
3. proptest 新增不变量：任意输入下，`pick` 的产出展开到的每个平台，该包在该平台
   `last_pub + window <= now`。
4. `pick_set_for`（换套装路径，`publish_planning.rs:438`）同步享受新逻辑
   （它复用 `pick`，确认 target\_platforms 传全量目标而非单平台 LIMIT 1——顺手把
   `reroll_set` 里 `LIMIT 1` 取平台改为取该 set 关联任务的全部 distinct 平台）。

### 验收标准

- 新 proptest 绿；既有 set\_picker 测试按新语义更新（明示改动理由于 PR 描述，
  符合测试完整性规则）。`pnpm check` 全绿。

## C2 — 日限裁剪按日轮转，消除固定饿死

**分支**：`fix/schedule-fair-trim`　**Review**：high

### 背景与根因

`scheduler.rs:159` 裁剪前固定 `sort_by_key(sku_id)` 再 truncate → 容量不足时每天
被裁的都是 id 大的同一批 SKU，永远发不出去。

### 改动清单

1. 裁剪顺序改为：`rows.sort_by_key(sku_id)` 后用 `Rng::new(input.seed ^ acct_id)`
   `shuffle`（seed 含日期派生，每天轮换且当日可复现），再 truncate。
2. proptest 新增公平性不变量（弱断言即可）：固定 4 SKU × 日限 2，遍历 14 个不同
   seed（模拟 14 天），每个 SKU 至少被保留一次。

### 验收标准

- 新旧测试全绿（`daily_limit_trims` 只断言数量，不受影响）。`pnpm check` 全绿。

## C3 — 冷款轮播步进 = 每周 M 个

**分支**：`fix/cold-rotation-step`　**Review**：high

### 背景与根因

`frequency.rs:85` `start = wk % len`：窗口每周只前移 1 位，M=2 时相邻两周重叠 1 个，
全池覆盖周期从 ⌈len/M⌉ 周退化为 len 周。

### 改动清单

1. `start = ((wk * m) % len)`（`wk.rem_euclid` 保留）。
2. 测试：5 冷款、每周 2 个 → 3 周内 5 个全被轮到（现有
   `cold_rotation_covers_pool_over_weeks` 收紧为 3 周）；连续两周窗口无重叠
   （len % m \== 0 时严格成立，非整除允许首尾衔接重叠 ≤ m-1，断言写宽容版本）。

### 验收标准

- 收紧后的测试绿；PR 描述说明断言变更理由。`pnpm check` 全绿。

## C4 — 发布时间独立抖动 + 跨平台错峰

**分支**：`fix/schedule-jitter`　**Review**：medium

### 背景与根因

`available_points`（`scheduler.rs:99-120`）= 起点小抖动 + 严格等距网格 → 同平台
每天完美等间隔（12:03/13:03/14:03），是平台风控可识别模式；跨平台可能同分钟撞点，
单执行机串行会挤压。需求 §4.3 本意是「时段内随机抖动」。

### 改动清单

1. 每个点独立抖动：步进从固定 `gap` 改 `gap + rng.below(jitter_max)`，
   `jitter_max = (gap/2).clamp(5, 30)` 分钟；保证仍 ≥ gap（不变量 2 不破坏）。
2. 跨平台错峰：`schedule` 末段维护全局已用分钟集合，平台内分配时若命中已用分钟则
   +1 顺延（最多顺延至下一个点前，防破坏 slot 边界——顺延后仍须在原 slot 内，
   否则保留撞点，宁撞不越界）。
3. proptest：既有五不变量全部保绿（间隔断言 ≥ gap 不变）；新增「非空时间全局重复
   分钟数 ≤ 平台数-1」弱断言。

### 验收标准

- proptest 绿；肉眼抽查一日排期不再等差。`pnpm check` 全绿。

## C5 — 账号级可用时段消费 + 无账号平台进缺料 + hotDaily 语义

**分支**：`feat/account-slots-and-gaps`　**Review**：high

### 背景与根因

① `accounts.slots_json` 存在但 `SchedAccount` 无该字段，排期只用全局时段——需求
§4.1「账号档案：可用时段」双侧未闭环；② SKU 应发但该平台无账号时展开为 0 行且
不进缺料清单，任务单静默变空无解释；③ 设置里 hotDaily 可调 0–5 但引擎 `hot_daily>=1`
即每日一次，>1 无效（误导）。

### 改动清单

1. `SchedAccount` 加 `slots: Vec<Slot>`（空 = 跟随全局）；`generate_sheet` 解析
   `slots_json`（格式与全局 time\_slots 相同的字符串数组）。分配时该账号的行只落在
   「账号时段 ∩ 全局时段」（账号有时段时以账号为准，实现选简单版：账号 slots 非空则
   用账号 slots）。proptest 增不变量：行时间落在其账号 slots 内（若非空）。
2. `generate_sheet`：对每个 chosen SKU，`platforms` 里没有任何 active 账号的平台 →
   shortage\_json 追加 `{reason:"no_account", code, platform}`；全平台都无账号 →
   整 SKU 进缺料。前端缺料横幅按 reason 显示中文（无可用素材包/无可用标题/无正文/
   无账号(平台)/查重窗口冲突/超时补排——与 C1/B3 的新 reason 一起做个
   `shortageLabel()` 映射放 `src/lib/status.ts`）。
3. hotDaily 语义二选一（**推荐 a**）：（a）设置项改布尔「热款每日发布」，Stepper 删除,
   迁移设置 JSON 时 >0 视为 true；（b）实现每日多次（同 SKU 同日多套装）——改动大，
   V2 再说。选 a，并在 `FreqRules` 注释里写明。

### 验收标准

- 账号时段测试 + 缺料 reason 测试绿；策略页与设置页文案同步更新。`pnpm check` 全绿。

---

# 批次 D — 需求缺口 UI 闭环（D7 先做，其余依赖其刷新机制）

## D7 — 事件驱动刷新：订阅 SheetChanged / 收件箱联动 / 错误处理

**分支**：`feat/publish-event-wiring`　**Review**：medium

### 背景与根因

后端三事件齐备，前端只接了 badges 与 inboxIngest 的 toast：`SheetChangedEvent`
无人订阅（`src/stores/publish.ts:35` 未传 `onSheetChanged`），看板/工作台/任务单列表
不随 watcher 对账更新；InboxPanel 的 rescan/discard/retry 无 catch
（`AssetsPage.tsx:527-541`），失败无提示；收到收录事件时资产库列表/收件箱面板不刷新。

### 改动清单

1. publish store 增加 `sheetRev: number`（自增计数）与 `inboxRev: number`：
   `onSheetChanged` → `sheetRev++`；`onInboxIngest` → `inboxRev++`。
2. `PlanPage` 的 Board/SheetList/Workbench 各自 `useEffect` 依赖对应 rev 重新 load
   （Workbench 只在 `e.sheetId === 当前` 时刷，事件 payload 已带 sheet\_id——
   store 里存 `lastSheetChanged: SheetChangedEvent | null` 供判断）。
3. `AssetsPage` 的 InboxPanel 与 SKU 列表依赖 `inboxRev` 刷新。
4. InboxPanel 三个操作包 try/catch + `toast.error`；与 store 自维护的 toasts 数组
   二选一统一到 sonner（删 store 的 toasts/IngestToast，`onInboxIngest` 直接调
   sonner `toast()`，减一套自绘 toast UI——先 grep 确认自绘渲染点后一并删除）。
5. 遵守铁律：不加任何轮询。

### 验收标准

- 手工验证：开着工作台，模拟回执落盘（写 xlsx）→ 2s 防抖后页面自动更新状态徽章；
  收件箱放文件 → SKU 余量自动变。`pnpm typecheck && pnpm test` 绿 + `pnpm check`。

## D1 — 账号档案编辑 UI（日限/时段/改名/删除）

**分支**：`feat/account-edit-ui`　**Review**：medium

### 改动清单

1. `StrategyTab` 账号行加「编辑」按钮 → `EditAccountModal`：改名、日限 Stepper、
   可用时段（chips 输入，格式 `HH:MM-HH:MM`，前端用与后端一致的解析校验），调
   `commands.updateAccount`（后端已有，确认 patch 字段含 slots；缺则补齐 + 重生成绑定）。
2. 删除账号：后端新增 `delete_account`（有历史任务引用时拒绝删除、提示改停用；
   `publish_tasks.account_id` 引用检查），前端按钮 + ConfirmModal。
3. 账号时段的消费端在 C5；本任务先落 UI 与存储。

### 验收标准

- 编辑各字段落库回显；被引用账号删除被拒且文案清晰。`pnpm check` 全绿。

## D2 — 时段模板编辑（含跨午夜校验反馈）

**分支**：`feat/timeslot-editor`　**Review**：medium

### 改动清单

1. `StrategyTab` 时段卡片改可编辑：chips 可删；输入框回车新增，前端校验
   `HH:MM-HH:MM` 且 start\<end（与 `parse_slot` 一致；跨午夜暂不支持——输入
   `21:00-01:00` 给出明确错误文案「暂不支持跨午夜时段，请拆成 21:00-23:59」），
   调 `updatePublishSettings({timeSlots})`。
2. 后端 `sanitize()` 对非法段拒绝保存（B4 已做则复用）。
3. 空时段列表时排期全部「立即发」——保存时给 warning toast 提示该后果。

### 验收标准

- 编辑后 `generate_sheet` 使用新时段（测试或手工验证）；非法输入不落库。门禁绿。

## D3 — 标题/正文条目编辑与删除

**分支**：`feat/text-item-edit`　**Review**：medium

### 改动清单

1. `TextList` 行加「编辑」→ 弹窗（textarea + 平台标签 select），调已有
   `commands.updateTextItem`。
2. 删除：后端 `delete_text_item`（被 daily\_sets 引用时拒绝——查
   `daily_sets.title_id/body_id`；建议已用条目只允许停用），前端按钮 + Confirm。
3. 显示来源（inbox/manual）小徽标，便于区分手动与收录。

### 验收标准

- 编辑落库；被引用条目删除被拒。`pnpm check` 全绿。

## D4 — SKU 平台覆盖 + 备注编辑

**分支**：`feat/sku-platform-override`　**Review**：medium

### 改动清单

1. `SkuEditModal` 加「发布平台」区块：默认「跟随全局矩阵」开关，关掉后显示五平台
   Toggle（复用 `publishPlatforms()`）；提交 `platforms: 覆盖数组 | null`
   （patch 语义 `Some(None)`=清除已支持，前端 bindings 传 null 即可——确认
   specta 生成的类型是 `platforms?: string[] | null`，按实际类型适配）。
2. 备注 note 输入框（多行），列表视图不展示、详情档案卡展示。
3. SKU 详情档案卡显示当前生效平台（覆盖或全局）。

### 验收标准

- 设「只发小红书」后 generate\_sheet 只展开 xhs（测试已有 platforms\_json 路径，
  补 UI 到端到端一条）。门禁绿。

## D5 — 素材池缩略图

**分支**：`feat/pack-thumbnails`　**Review**：medium

### 改动清单

1. `PackView` 增加 `thumb_path: Option<String>`（绝对路径）：封面优先，无封面取
   files 里首个图片成员；视频包无封面时为 None（V1 不抽帧）。后端在 `to_view` 拼
   `dir_rel + name`（参照 `publish_planning.rs:103` 的 cover\_path 做法）。
2. `PackGrid` 的 `.packimg` 用 `assetSrc(thumb_path)` 背景图（复用
   `src/lib/img.ts`）；视频包无图时保留 ▶ 占位。
3. `PackModal` 内加图片网格预览（包内全部图片成员，`convertFileSrc` 直读原图，
   `loading="lazy"`）。
4. 注意 Tauri asset scope：确认 `assetSrc` 对资产库目录可读（生成页/作品库已在读
   根外目录的先例，如 scope 不含发布根目录，需在 `tauri.conf.json`/capability 里
   扩 assetProtocol scope——改动最小方式是允许 `$HOME/**` 已有配置则不动，实测为准）。

### 验收标准

- 图集包网格显示首图/封面，弹窗能看全部图。滚动流畅（懒加载）。门禁绿。

## D6 — 选择器搜索与列表防抖

**分支**：`feat/picker-search`　**Review**：medium

### 改动清单

1. `AddRowModal`（`PlanPage.tsx:815`）与 `ClaimModal`（`AssetsPage.tsx:639`）顶部
   加搜索框（前端内存过滤 code/styleName，`Intl.Collator` 无需拼音——直接子串匹配），
   列表高度放宽到 260px。
2. SKU 主列表搜索输入 300ms 防抖（自定义 `useDebouncedValue` 放
   `src/features/_shared/`，或项目已有则复用）。
3. ClaimModal 顺手显示 tier 徽章已有，补显示三池余量帮助判断（数据已在 SkuView）。

### 验收标准

- 100+ SKU 下两弹窗可搜索定位；主列表打字不再每键全量查询。门禁绿。

---

# 批次 E — 稳健性与性能

## E1 — 重 IO 移出 async 线程 + 导出进度

**分支**：`fix/blocking-io`　**Review**：high

### 背景与根因

`export_package`（复制视频可达数百 MB×多 SKU）、ingest/build\_pack 的文件搬运全是
`std::fs` 在 async 上下文同步执行——导出期间 UI 卡死、其它 IPC 排队。

### 改动清单

1. `export_package` 拆两段：DB 读（async）→ 文件复制 + xlsx 写（打包成纯数据后
   `tauri::async_runtime::spawn_blocking` 执行）→ DB 状态更新（async）。
2. 导出进度事件：新 `ExportProgressEvent { sheet_id, done, total }`（specta 登记 +
   重生成绑定），每复制完一个文件 emit；前端导出按钮转 spinner + 进度文本。
   节流可省（文件数 ≤ 数百）。
3. `ingest::rescan`/`collect_media` 的文件枚举与搬运同样包 `spawn_blocking`
   （watcher worker 里已是独立 task，优先级低于导出；若改动面大可只做枚举部分，
   在 PR 里说明取舍）。

### 验收标准

- 手测 500MB 级素材导出期间 UI 可交互、进度推进；测试保绿。`pnpm check` 全绿。

## E2 — 视图查询 N+1 治理

**分支**：`perf/publish-queries`　**Review**：high

### 改动清单

1. `list_sheets`：一条 `GROUP BY sheet_id, status` 聚合出各状态计数（新 repo 查询
   `planning::sheet_status_counts`），替代每单全量拉行。
2. `list_asset_packs`：`ledger.pack_platform_last` 改一条
   `SELECT pack_id, platform, MAX(published_at) ... WHERE pack_id IN (...) GROUP BY`
   批量；`is_locked` 改一条 IN 查询；`skus::get` 提到循环外（同一 SKU）。
3. `generate_sheet` 的逐包 `pack_platform_last` 同款批量化。
4. 保持 repo 层签名清晰（批量函数返回 HashMap）。行为不变，纯性能——现有测试即回归。

### 验收标准

- 功能测试全绿；用 300 SKU/1000 包的临时库手测列表 <100ms（可写一个 ignored 压测）。
  `pnpm check` 全绿。

## E3 — watcher 文件大小稳定探测

**分支**：`fix/watcher-size-stable`　**Review**：medium

### 背景与根因

`watcher.rs` 注释声称「文件大小/事件连续 2 秒稳定」，实现只有事件静默。写入方暂停

> 2s（人工分批拷贝、同步软件限速）会收录半截文件。

### 改动清单

1. `rescan` 收录 TXT/归集媒体前，对每个候选文件做两次 `metadata().len()` 采样
   （间隔 500ms，`tokio::time::sleep`），不等 → 本轮跳过该文件（下轮事件自然重试）。
   抽 `is_size_stable(path) -> bool` 放 ingest，媒体与 TXT 共用。
2. 同步软件临时文件过滤补全：跳过 `.syncthing.` 前缀、`.tmp`/`.part`/`.crdownload`
   后缀（`collect_dir_media` 与 rescan 的文件名过滤处）。

### 验收标准

- 测试：mock 一个两次 size 不同的文件被跳过、稳定后下轮收录。`pnpm check` 全绿。

## E4 — 入库原子性 + 删除引用校验 + 孤儿对账

**分支**：`fix/pack-integrity`　**Review**：high

### 改动清单

1. `build_pack`（`ingest.rs:435`）：`assets::insert` 失败时把已 rename 的文件移回
   原位（记录 moved 清单，Err 路径回滚；回滚失败 warn 并留 `.orphan` 标记文件）。
2. `delete_pack`：删除前查 `daily_sets.pack_id` 与 `usage_ledger.pack_id` 引用——
   有台账记录的包**不允许物理删除**（提示走退役）；无引用才删目录+记录。
3. 启动期孤儿对账（挂在 ticker 首轮）：遍历 `资产库/*/*` 目录 vs asset\_packs.dir\_rel，
   目录无记录 → `tracing::warn` 汇总一条（V1 只报不自动收编）；记录无目录 →
   pack 标 note「目录缺失」。
4. 迁移：`usage_ledger.pack_id`/`daily_sets.pack_id` 若缺索引则补（`ls migrations/`
   确认 0010 现状：ledger 有 idx\_ledger\_pack，daily\_sets 无索引 → 补
   `CREATE INDEX idx_daily_sets_date_sku ON daily_sets(date, sku_id)`，并评估加
   `UNIQUE(date, sku_id)`——先查存量是否有重复，有则迁移里去重保留最新被引用者，
   谨慎：publish\_tasks.set\_id 外键需要跟随更新；如复杂度高，UNIQUE 降级为仅索引 +
   代码层查重，PR 里说明）。

### 验收标准

- 测试：插包失败文件回滚；有台账的包删除被拒；索引迁移幂等。`pnpm check` 全绿。

## E5 — 归档保留期清理

**分支**：`feat/archive-retention`　**Review**：medium

### 改动清单

1. 设置项 `archive_retention_days`（默认 90，0=永久）加入 PublishSettings +
   设置区块 Stepper。
2. ticker 每日首轮（复用 `run_catchup` 已有节奏，日期变更时执行一次）：删除
   `收件箱/已收录/`、`收件箱/已丢弃/` 下超期日期目录；`任务包/` 下**已关闭**且超期的
   包目录（查 task\_sheets：date 超期且 status=closed 才删——快照目录随包删除）。
   inbox\_items 对应记录保留（历史可查）。
3. 删除前 dry-run 统计 + `tracing::info`；物理删用 `remove_dir_all`，任何失败跳过
   继续。

### 验收标准

- 测试：造过期目录与未关单目录，前者删后者留。`pnpm check` 全绿。

## E6 — 草稿保护：重生成确认 + 到料提示

**分支**：`feat/draft-guards`　**Review**：medium

### 背景与根因

① 草稿重生成会 `clear_sheet_children` 清掉人工改动（改时间/增补行/换套装）无任何确认；
② 22:00 生成明日草稿后新素材到库，草稿不感知，缺料的 SKU 白白缺一天。

### 改动清单

1. **后端**：`SheetDetail` 增加 `edited: bool`——实现：publish\_tasks 里存在
   `updated_at > created_at` 的行或行数≠初次生成数（简化：daily\_sets/tasks 任一
   `updated_at` 晚于 sheet 的 `generated_at`；给 task\_sheets 加 `generated_at`
   列，生成/重生成时刷新）。
2. **前端**：SheetList「生成明日任务单」与（新增的）「重新生成」按钮在目标日已有
   `edited` 草稿时弹 ConfirmModal「将丢弃 N 处人工调整」。Workbench 顶栏为草稿态
   补「重新生成」按钮（当前 UI 只能生成明日，今日草稿想重算没有入口）。
3. **到料提示**：`rescan_and_emit` 收录成功后，查明日/今日草稿的 shortage\_json 里
   是否含本次入库的 SKU；命中则 emit 一条 `SheetChangedEvent`（状态不变，前端 D7 的
   rev 机制自动刷新），并在 InboxIngest toast 文案追加「该 SKU 在 {date} 缺料，
   可到任务单重新生成」。不自动重算（尊重人工改动）。

### 验收标准

- 已编辑草稿重生成有确认；缺料 SKU 到料后 toast 有提示。门禁绿。

## E7 — 暂停排期（节假日跳过）

**分支**：`feat/schedule-pause`　**Review**：medium

### 改动清单

1. 设置项 `schedule_paused: bool`（默认 false）+ 发布计划页顶栏显式开关
   （类比引擎队列暂停的既有交互）。
2. `run_catchup` 开头 `schedule_paused` 为 true 时跳过①②（生成），**保留**③
   超时扫描与对账（回收闭环不能停）。
3. 手动 `generate_sheet` 不受影响（想生成随时能生成）。
4. 看板/任务单列表在暂停时显示「排期已暂停」横幅。

### 验收标准

- 暂停后 ticker 不生成新草稿、对账照常；开关状态重启保持。门禁绿。

## E8 — 杂项修正打包（小而确定）

**分支**：`fix/publish-misc`　**Review**：medium

### 改动清单

1. `classify_fail`（`reconcile.rs:16`）关键字优先级重排：timeout（超时/网络）提到
   content（素材/审核）与 login 之前——「上传素材超时」应归 timeout；单测补歧义样例。
2. `use_local_as_exec_root` 顺手把 `path_style` 设为本机风格（cfg!(windows) ?
   windows : unix）。
3. `exceeds_path_limit` 校验加上**文件名**层（当前只到 SKU 目录层，
   `exporter.rs:115`）：用包内最长文件名拼接后判断。
4. `lastPublishLabel`（`AssetsPage.tsx:442`）跨年显示补年份（>300 天或跨年时
   显示 `2025年12月3日`）。
5. ⌘K 命令面板注册发布模块动作：生成明日任务单、打开今日看板、重扫收件箱、
   导入回执（跳对应页并触发）——按现有 command palette 的 action 注册模式
   （`grep -rn "⌘K\|CommandPalette" src/` 找注册点）。
6. 标题入库查重：`ingest_txt` 与 `add_text_item` 插入前查同 SKU 同 kind 同 text
   （精确匹配）已存在则跳过并计入报告（`Ingested.titles` 计数区分 新增/跳过，
   detail 里带 `duplicates: n`）。

### 验收标准

- 各点单测/手测覆盖；`pnpm check` 全绿。

---

# 批次 F — 新功能（体验跃升，按价值排序）

## F1 — 补料提示词一键生成（价值最高，先做）

**分支**：`feat/restock-prompts`　**Review**：medium

### 设计

缺料清单/低余量 SKU → 生成可直接粘贴给 Claude/Codex 的补料 prompt，闭环内容生产。

### 改动清单

1. 后端 `build_restock_prompt(sku_ids, kind) -> String` 纯函数 + 命令
   `restock_prompt(skuIds, kind)`：按 `docs/收件箱收录格式规范.md` 的输出契约拼模板——
   含 SKU 编码、款式名、商品名、固定话题、目标条数（默认标题 10/正文 5）、落盘文件名
   规范与【SKU】头示例。模板常量放 `publish/` 新文件 `restock.rs`，格式规范文档路径
   写进注释保持同步。
2. 前端两个入口：（a）资产库列表勾选/单个 SKU 详情 →「生成补料提示词」按钮；
   （b）任务单缺料横幅 →「一键生成补料提示词」。弹窗展示文本 + 「复制」按钮
   （`navigator.clipboard`）。
3. 支持多 SKU 合并为一段 prompt（Claude 一次生成多款）。

### 验收标准

- 生成的 TXT 按 prompt 写出后能被收件箱正确收录（用 parser 测试反向验证模板正确性：
  单测里让 `parser::parse` 消化模板示例）。门禁绿。

## F2 — 回执截图内嵌核对弹窗

**分支**：`feat/receipt-screenshot-view`　**Review**：medium

### 改动清单

1. `TaskRowView` 增加 `screenshot_path: Option<String>`：任务 `screenshot` 字段非空时
   拼 `任务包/{date}/回执截图/{name}` 绝对路径（`build_detail` 里，参照 cover\_path）。
   suspect 任务无回执截图，但**同账号同平台邻近任务**的截图对核对有参考价值——V1 先
   只显示自身有的。
2. `VerifyModal` 与已发布/失败行的详情（点行展开或复用 Modal）内嵌
   `<img src={assetSrc(screenshotPath)}>`，点击放大（复用现有 zoom 组件如有）。
3. 无截图显示占位文案「执行器未回传截图」。

### 验收标准

- 有截图的任务能看图核对；asset scope 可读任务包目录（同 D5 的 scope 确认）。门禁绿。

## F3 — 资产跑道预测（余量 → 还能撑几天）

**分支**：`feat/asset-runway`　**Review**：high

### 设计

按分层频率 + 查重窗口推演每 SKU 素材/标题耗尽日期，把静态阈值预警升级为倒计时。

### 改动清单

1. 纯函数 `publish/planner/runway.rs`：输入（tier、启用平台数、可用包数与各包窗口
   状态、标题/正文数、FreqRules、dedup\_days），输出
   `RunwayView { material_days: Option<i64>, title_days, body_days }`——
   模拟未来 N 天（上限 60）：每个应发日消耗一个出窗包 + 计标题轮换（标题不受窗口限制,
   以「可用包数 × 每包窗口内可用平台次数」近似素材容量即可，精确模拟 due\_skus 更好，
   函数已可复用）。单测覆盖热/温/冷三档。
2. `SkuView` 增加三字段（`list_agg` 数据已够，批量算，注意别引入 N+1——
   包窗口状态复用 E2 的批量查询）。
3. 前端 SKU 列表「预警/状态」列显示「素材还剩 N 天」（≤7 琥珀、≤3 红）；
   支持按跑道排序（默认警示置顶开关）。
4. 与 F1 联动：跑道 ≤7 天的 SKU 出现在「生成补料提示词」默认勾选集。

### 验收标准

- 三档 SKU 的推演单测；列表展示与排序正确；无明显性能回退。`pnpm check` 全绿。

## F4 — 排期预演（未来 7 天沙盘）

**分支**：`feat/schedule-preview`　**Review**：medium

### 改动清单

1. 命令 `preview_schedule(days: i64) -> Vec<PreviewDay>`：对今起 N 天（≤14）逐日跑
   `due_skus` + 轻量展开（不选套装、不落库——只算「哪 SKU × 哪平台 × 几行」；
   PreviewDay { date, entries: Vec<{skuCode, platforms, rows}> , trimmed }）。
   复用 frequency/scheduler 纯函数，零副作用。
2. 策略页底部「未来 7 天预演」卡片：改动频率/矩阵/账号后点「预演」（或 500ms 防抖
   自动刷新）渲染表格；冷款首次轮到日高亮。
3. 无套装选取 → 不受缺料影响，卡片注明「按频率与账号推演，不含缺料过滤」。

### 验收标准

- 改 warmWeekly 立即反映在预演分布；纯函数无 DB 写（代码评审确认）。门禁绿。

## F5 — 发布月历视图

**分支**：`feat/publish-calendar`　**Review**：medium

### 改动清单

1. 命令 `calendar_month(yyyy_mm) -> Vec<CalendarDay>`：按月聚合 usage\_ledger
   （历史实发）+ task\_sheets/publish\_tasks（当月计划），
   `CalendarDay { date, published, planned, failed, skus: Vec<String>(去重前若干) }`。
   一条 GROUP BY date 查询 + 一条计划查询。
2. 发布计划页新分页签「月历」（Tab 加一项）：7×5 网格，格子密度色块
   （tokens 用现有 ok/er/wr 变量），点格子跳该日任务单（有 sheet 时）。
3. 顶部月份切换 ‹ ›。

### 验收标准

- 月历与台账/任务单数据一致；点击跳转正确。门禁绿。

## F6 — 开屏晨报 + 系统通知

**分支**：`feat/daily-brief`　**Review**：medium

### 改动清单

1. 命令 `daily_brief() -> BriefView`：昨日（成功率/失败数/日报有无）+ 今日
   （计划/待核对/缺料）+ 待认领 + 跑道告警数（F3 依赖，未合入则先留 0）。全部现有查询拼装。
2. 前端：App 启动后（发布模块已配置根目录时）首次进入任一页面弹一次晨报卡
   （Modal，本日只弹一次——localStorage 记 `briefShown:{date}`），各数字可点击跳转。
3. 系统通知：`tauri-plugin-notification`（查 `Cargo.toml`/capabilities 是否已引，
   未引则加插件 + 权限）。两个触发点：`timeout_scan` 标记数 >0、
   `apply_receipts` 出现 risk 熔断——App 非前台时发系统通知（前台只走既有事件/toast）。
4. 通知开关进设置「发布与同步」（默认开）。

### 验收标准

- 晨报数字与各页一致、每日一弹；后台时疑似已发触发系统通知。门禁绿。

## F7 — 拖放直投（文件拖到 SKU 即入池）

**分支**：`feat/drag-drop-ingest`　**Review**：medium

### 改动清单

1. Tauri v2 窗口拖放：监听 `onDragDropEvent`（webview 事件，确认
   `dragDropEnabled` 配置；guardrails 要求经 `src/lib/ipc/` 出入——把监听封装进
   `src/lib/ipc/index.ts` 的薄封装导出）。
2. `AssetsPage` SKU 行 / SKU 详情作为 drop target：拖入时行高亮；drop 后按扩展名
   分流——图片/视频调 `importMediaFiles(skuId, paths)`（已有），`.txt` 新命令
   `import_text_file(skuId, path)`（复制进 `收件箱/{code}/` 后走 `ingest_txt`
   forced\_sku 管线）。
3. drop 到页面空白处（非某 SKU）→ 复制进 `收件箱/` 根，走三冗余识别（等价于放进
   收件箱文件夹）。
4. 成功后 toast + 依赖 D7 的 rev 自动刷新。

### 验收标准

- 拖图到 SKU 行成包入池；拖 TXT 正确入文本池；拖到空白走自动识别。门禁绿。

## F8 — 看板日期切换 + 昨日对比

**分支**：`feat/board-date-nav`　**Review**：medium

### 改动清单

1. Board 顶部日期 ‹ 今天 ›（`getDashboard(date)` 已支持任意日期）；非今日时显示
   「回到今天」。
2. 今日卡片补一行「昨日成功率 N%（日报）」：读昨日 sheet 的 report\_json
   （`getReport`），无则「—」。
3. 历史日期看板隐藏「去核对」等仅今日有意义的动作按钮（suspect 仍可跳）。

### 验收标准

- 前后翻日数据正确；今日视图不回归。门禁绿。

## F9 — 同步链路健康监测

**分支**：`feat/sync-health`　**Review**：medium

### 改动清单

1. `task_sheets` 加列（迁移）：`first_receipt_at INTEGER`、`last_receipt_at INTEGER`
   ——`apply_receipts` 首次 matched>0 时写 first（仅一次），每次写 last。
2. `DashboardView` 增加 `exportedAt / firstReceiptAt / lastReceiptAt`；看板渲染一行
   「已导出 12:03 · 执行器 12:40 开始回写 · 最后回写 14:02」；
   `exported 且超 1 小时无 first_receipt` → 琥珀提示「执行器尚未开始处理，请检查
   同步软件与执行机」。阈值先写死 1h（注释说明，后续可进设置）。
3. 疑似已发 banner 文案在「无任何回执」与「有回执但部分超时」两种场景区分提示
   （前者优先怀疑同步链路）。

### 验收标准

- 模拟回写后时间戳落库并展示；无回写时提示出现。门禁绿。

## F10 — 素材包发布次数与复用视图

**分支**：`feat/pack-usage-stats`　**Review**：medium

### 改动清单

1. `PackView` 增加 `publish_count: i64` 与 `last_published: Option<i64>`
   （E2 的批量台账查询顺手 COUNT/MAX）。
2. PackGrid 卡片角标「发过 N 次」；PackModal 内列该包台账明细（日期/平台/账号/链接，
   新命令 `pack_history(packId)` 或扩展现有 ledger repo）。
3. 素材池支持按「发布次数/最近发布」排序切换，辅助人工退役决策。

### 验收标准

- 计数与台账一致；排序正确。门禁绿。

---

## 附：来源对照（审查发现 → 任务）

| 来源                                                                                                                                                                                                                                          | 任务 |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -- |
| 第一轮 30 项：#1→A1 · #2→A2 · #3/#7→B1 · #4/#5→B3 · #6→A4 · #8→C2 · #9→C3 · #10/#22→C1 · #11/#12→B2 · #13/#16→B4 · #14/#24→A3 · #15→E4 · #17→C5+D1 · #18→D2 · #19→D3 · #20→D4 · #21→D5 · #23/#27→D7 · #25→E1 · #26→D6 · #28→C4 · #29→E2 · #30→F8 |    |
| 第二轮 10 功能：跑道→F3 · 补料 prompt→F1 · 预演→F4 · 月历→F5 · 截图→F2 · 预检→B1 · 同步健康→F9 · 晨报/通知→F6 · 拖放→F7 · 快照→B5                                                                                                                                         |    |
| 第三轮 17 新发现：丢弃复活/逐文件容错→A3 · 路径穿越/保留名/大小写→A5 · daily\_sets 约束→E4 · watcher 大小稳定→E3 · 无账号缺料/hotDaily→C5 · 重生成覆盖/到料提示→E6 · classify 顺序/exec 风格联动/路径上限含文件名/年份/⌘K/标题查重→E8 · autogen 校验→B4 · 归档清理→E5 · 暂停排期→E7 · toast 体系合并→D7                     |    |

## 附：建议里程碑切分

- **v0.8.1（缺陷修复版）**：批次 A + B（10 项）。A1 单独可先出 hotfix。
- **v0.8.2（算法与闭环版）**：批次 C + D（12 项）。
- **v0.9.0（体验版）**：批次 E + F 按价值裁剪（F1/F2/F3 优先）。
- 每批合入后按审查协议跑全新上下文 `/code-review`（A/B/C/E1/E2/E4/F3 用 high，
  其余 medium）；v0.9.0 前触发一次 `/code-review ultra`。

