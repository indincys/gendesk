# 内部图片生产工具（GenDesk）发布与资产管理模块 开发执行计划

> 生成于 2026-07-14。执行者：Claude Code（代码 100% 由 AI 编写）。
> 权威输入的边界：
>
> - **功能与业务规则** → 《内部图片生产工具_发布与资产管理需求文档.md》（下称「需求文档」）
> - **落盘格式契约** → 《docs/收件箱收录格式规范.md》（生成端 Skill 已按此落盘）
> - **视觉与交互** → `需求文档原型设计-handoff.zip` 内 `发布与资产管理原型.dc.html`
>   （Claude Design 原型，像素级基准；P0 任务将其解包入库为 `docs/prototype/publish.dc.html`）
> - **工程规范** → `CLAUDE.md`（铁律、门禁、IPC 约定、审查协议全部沿用，本计划不重复）
>
> 冲突裁决顺序：需求文档 > 原型 > 本计划。§0/§1 已消化 2026-07-14 与用户确认的全部决策，
> 执行时无需再回读讨论过程。

---

## 0. 已拍板决策（2026-07-14 用户确认，勿再询问）

1. **视觉基准 = 原型 Zip 1:1 复刻**（同 v0.5.0 生成页模式）：读原型 HTML 源码复刻，不截图猜测。
   原型已确认覆盖：资产库页（SKU 列表 + 详情 + 收件箱状态区）、发布计划页三页签
   （看板/任务单工作台/策略与账号）、设置「发布与同步」区块、疑似已发处置交互、日报。
   原型 tokens 与现有 `docs/prototype/Design-Tokens.md` 完全一致（已 diff 验证），无新增 token。
2. **定时机制 = 应用内定时 + 启动补跑**：应用运行时到点触发；每次启动扫描补齐
   「该生成未生成的任务单 / 该判超时未判的任务」。不做系统级定时（launchd/任务计划）。
3. **发版节奏 = 三阶段三 PR，一次发版**：P1/P2/P3 各一个分支一个 PR（小步提交保 bisect），
   分阶段合入受保护的 main；**全程不打 tag**，P3 完成 + 收尾质量关后一次性 bump `0.7.0` + tag
   `v0.7.0` 触发 release。P2 合入后的「陪跑一周」用本地构建（`pnpm tauri build`），不发正式版。
4. **平台 = 固定五平台枚举**：`douyin / xhs / kuaishou / shipinhao / bilibili`
   （抖音/小红书/快手/视频号/B站）+ 全局启用开关（平台矩阵）。中文名↔枚举映射在 Rust 单点定义，
   xlsx 单元格与 UI 均显示中文。文本条目的平台标签 = 五平台 + `general`（通用）。

---

## 1. 开工前置事实（执行时直接依赖，勿再推导）

1. **相对路径是真相**：库内与包内只存根目录内相对路径（`RelPath` newtype，repo 层只收该类型，
   类型强制而非散文约定）。绝对路径只在两处出现：本机 IO 时 `本机根 + rel`，
   导出写 xlsx 时 `执行机根 + rel`（纯字符串拼接，分隔符按执行机风格，默认 Windows `\`）。
2. **ASCII 规则只约束「变量名」**：SKU 编码、任务 ID、素材文件名（`video.mp4`/`img_01.jpg`/
   `cover.jpg`/`body.txt`）必须 ASCII 小写、无空格、无 `<>:"|?*`。四分区目录名（`资产库/收件箱/
   任务包/已收录`）与包内固定名（`任务单.xlsx`/`执行说明.md`/`回执截图/`/`素材/`/`READY.txt`）
   按需求文档 §4.6/§7.2 保持中文/固定拼写——它们是常量，全部集中在 `publish/paths.rs` 单点定义，
   代码中禁止字面量散写。
3. **任务 ID** `T{YYMMDD}-{NNN}`：按任务单内行序每日从 001 递增，全局唯一（UNIQUE 约束）；
   删行**不回收**编号（对账唯一性优先，与提示词号池的回收策略不同）。
4. **内容类型由素材包类型决定**：视频型包 → `视频` 任务（只消费标题）；图集型包 → `图文` 任务
   （消费标题 + 正文）。SKU 当天发什么类型，取决于套装选中的包。
5. **素材包生命周期存储态只有三个**：`new`（新入库，不参与排期）/ `active`（可用）/
   `retired`（退役，人工终态）。「已用尽/冷却中/回可用日期」全部是**派生态**：
   由使用台账 + 查重窗口在查询时计算，不落库、不需要定时翻转——窗口期满自然「回可用」。
6. **查重占用 = 台账窗口内记录 + 未关闭任务单的在途引用**：套装选取过滤「同素材包×同平台」时，
   既查 usage_ledger（近 N 天已发布），也查未关闭 task_sheets 中该包的待执行/疑似行，
   防止回执未回收时次日重复选中同一包。
7. **素材包锁定**：被状态 ≠ closed 的任务单引用的包，禁止删除/退役/移动（Rust 业务层校验，
   返回明确错误文案）。
8. **单写者纪律的发布版**：收件箱收录、对账回写、定时生成三条异步链路各自汇入**一个**
   串行工作者（tokio mpsc 单消费者），所有 DB 写仍在单事务内完成；与现有引擎调度器互不干扰。
9. **收录后的文件去向**：TXT 内容入库后原文件移入 `收件箱/已收录/{YYYYMMDD}/`；
   媒体文件重命名（ASCII）后**移入** `资产库/{SKU}/{pack_dir}/`，不留副本；
   未知 SKU / 解析失败的文件**留原位**，只记 inbox_items 记录（待认领/待人工确认）。
10. **图集归集时机**：SKU 文件夹内全部媒体文件大小连续 2 秒稳定后归集一次——
    `jpg/jpeg/png/webp` 整批 → 1 个图集包（文件名排序定图序，`cover.jpg` 识别为封面）；
    每个 `mp4/mov` → 1 个视频包（`<视频名>_cover.jpg` 识别为其封面）。后到的文件成新包，V1 不做合并。
11. **【话题】规则**：SKU 尚无固定话题 → 采纳前 5 个；已有 → 忽略并在收录报告记差异，绝不静默覆盖。
12. **任务单一天一张**（date UNIQUE）。草稿可整单重生成（覆盖）；已确认后锁定，
    提供「退回草稿」人工动作；已导出后不可编辑，重新导出=整包覆盖（先删 READY.txt 再写，
    UI 明确警告「确保执行器未在运行」）。
13. **超时判定基准**：`任务日期+定时发布时间 + 超时小时数`；定时列留空的任务以
    `导出时间 + 超时小时数` 为基准。只对已导出任务单中 `待执行` 的任务判定。
14. **疑似已发绝不自动重发**（硬性，需求 §6.4）：任何自动路径（重排、补排、重生成）都必须排除
    疑似任务；只能人工核实后定态为已发布或失败。此规则必须有直接的负向测试断言。
15. **失败六类处置**：`timeout`（网络超时）→ 次日生成草稿时自动补排该 SKU×账号；
    `risk`（风控）→ 该账号当日剩余待执行任务置 `已取消`，日报建议明日补排（不自动生成明日行）；
    `content`（素材不合规）→ 标记素材包（退役候选）+ 转人工；`login`/`page`/`other` → 转人工。
16. **任务单关闭条件**：全部行到达终态 `已发布/失败/已取消`。**疑似已发不是终态**，
    有疑似行的任务单停在「回收中」，直到人工定态。关闭时生成日报（成功率/失败清单/缺料/明日建议）。
17. **启动补跑三查**：① 今日任务单不存在 → 生成今日草稿；② 已过每日生成时间且明日任务单不存在
    → 生成明日草稿；③ 跑一轮超时扫描。运行期 ticker：到点生成明日 + 每 10 分钟超时扫描。
18. **5 视觉组映射沿用**（`src/lib/status.ts` 扩展）：待执行=灰、已发布=绿、失败=红、
    疑似已发=琥珀、已取消=灰弱化；任务单级：草稿=灰、已确认/已导出/回收中=蓝、已关闭=绿。
19. **「现有八页不受改动」的唯一例外**：作品库加「入资产库」入口（§3.7 联动，纯增量）。
20. **数据模型偏离需求 §9 草案一处**：标题池/正文池合并为一张 `text_items(kind)` 表
    （字段完全同构，代码减半；§9 自注为「实现参考」，允许偏离）。

---

## 2. 技术选型增量（新依赖，均为 Rust 侧）

| 依赖                | 用途                              | 说明                                                         |
| ----------------- | ------------------------------- | ---------------------------------------------------------- |
| `notify` v8       | 收件箱 + 任务包 xlsx 文件监听             | 防抖不用现成 debouncer：按规范 §4「文件大小连续 2 秒稳定」自实现（虚拟时钟可测）             |
| `rust_xlsxwriter` | 写任务单 xlsx（22 列）                 | 只写值与列宽，不做花哨样式（执行器只读值）                                      |
| `calamine`        | 读回执 xlsx（第 20–22 列）             | 按表头行定位列，容忍执行器整文件重写/转存                                      |
| `chrono`          | 日期/时段/每周频率计算                    | 全部本地时区；库内时间戳仍存 Unix 秒                                      |

前端**零新依赖**（虚拟滚动、Intl.Collator 拼音排序、Motion 均已有）。无新 IPC 模式（沿用
specta 命令 + 事件推送 + 250ms 节流）。

---

## 3. 数据模型定稿（migration `0010_publish.sql`，forward-only）

```sql
-- SKU 档案。内置一行「通用」分组（is_general=1，code='GENERAL'），收纳不挂具体 SKU 的文本。
CREATE TABLE skus (
  id           INTEGER PRIMARY KEY,
  code         TEXT NOT NULL UNIQUE,           -- ASCII，如 SF-YD-201
  style_name   TEXT NOT NULL,                  -- 款式名
  product_name TEXT NOT NULL DEFAULT '',       -- 商品名
  tier         TEXT NOT NULL DEFAULT 'warm',   -- hot|warm|cold
  topics_json  TEXT NOT NULL DEFAULT '[]',     -- 固定话题标签，有序，导出取前 5
  platforms_json TEXT,                         -- 平台覆盖（NULL=跟随全局矩阵）
  status       TEXT NOT NULL DEFAULT 'active', -- active|paused（停发）
  is_general   INTEGER NOT NULL DEFAULT 0,
  note         TEXT NOT NULL DEFAULT '',
  created_at   INTEGER NOT NULL,
  updated_at   INTEGER NOT NULL
);

-- 素材包。dir_rel 指向 资产库/{SKU}/{pack} 目录；files_json=[{name,origName,bytes}]。
CREATE TABLE asset_packs (
  id         INTEGER PRIMARY KEY,
  sku_id     INTEGER NOT NULL REFERENCES skus(id),
  kind       TEXT NOT NULL,                    -- video|gallery
  dir_rel    TEXT NOT NULL UNIQUE,
  files_json TEXT NOT NULL,
  cover      TEXT,                             -- 包内封面文件名，可空
  lifecycle  TEXT NOT NULL DEFAULT 'new',      -- new|active|retired（exhausted 为派生态）
  source     TEXT NOT NULL DEFAULT 'inbox',    -- inbox|works|manual
  note       TEXT NOT NULL DEFAULT '',
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

-- 标题池 + 正文池（合表，见前置事实 20）
CREATE TABLE text_items (
  id         INTEGER PRIMARY KEY,
  sku_id     INTEGER NOT NULL REFERENCES skus(id),
  kind       TEXT NOT NULL,                    -- title|body
  text       TEXT NOT NULL,
  platform   TEXT NOT NULL DEFAULT 'general',  -- douyin|xhs|kuaishou|shipinhao|bilibili|general
  source     TEXT NOT NULL DEFAULT 'manual',   -- inbox|manual（V2 预留 'ai'）
  enabled    INTEGER NOT NULL DEFAULT 1,
  use_count  INTEGER NOT NULL DEFAULT 0,
  created_at INTEGER NOT NULL
);

CREATE TABLE accounts (
  id          INTEGER PRIMARY KEY,
  platform    TEXT NOT NULL,
  name        TEXT NOT NULL,                   -- 任务单「平台账号名称」列的值
  daily_limit INTEGER NOT NULL DEFAULT 3,
  slots_json  TEXT,                            -- 可用时段（NULL=用全局时段模板）
  status      TEXT NOT NULL DEFAULT 'active',  -- active|disabled
  created_at  INTEGER NOT NULL,
  UNIQUE(platform, name)
);

-- 日内容套装：一天一 SKU 一套。account_id 列为 V2「每账号独立变体」预留，V1 恒 NULL。
CREATE TABLE daily_sets (
  id       INTEGER PRIMARY KEY,
  date     TEXT NOT NULL,                      -- '2026-07-15'
  sku_id   INTEGER NOT NULL REFERENCES skus(id),
  pack_id  INTEGER NOT NULL REFERENCES asset_packs(id),
  title_id INTEGER NOT NULL REFERENCES text_items(id),
  body_id  INTEGER REFERENCES text_items(id),  -- 图文才有
  account_id INTEGER,                          -- V2 预留
  UNIQUE(date, sku_id)
);

CREATE TABLE task_sheets (
  id            INTEGER PRIMARY KEY,
  date          TEXT NOT NULL UNIQUE,
  status        TEXT NOT NULL DEFAULT 'draft', -- draft|confirmed|exported|reconciling|closed
  shortage_json TEXT NOT NULL DEFAULT '[]',    -- 缺料清单（生成副产物）
  report_json   TEXT,                          -- 日报，关闭时写入
  exported_at   INTEGER,
  closed_at     INTEGER,
  created_at    INTEGER NOT NULL,
  updated_at    INTEGER NOT NULL
);

CREATE TABLE publish_tasks (
  id           INTEGER PRIMARY KEY,
  sheet_id     INTEGER NOT NULL REFERENCES task_sheets(id),
  task_code    TEXT NOT NULL UNIQUE,           -- T260715-001
  set_id       INTEGER NOT NULL REFERENCES daily_sets(id),
  account_id   INTEGER NOT NULL REFERENCES accounts(id),
  platform     TEXT NOT NULL,
  content_kind TEXT NOT NULL,                  -- video|gallery
  planned_time TEXT,                           -- 'HH:MM'；NULL=立即发布
  status       TEXT NOT NULL DEFAULT 'pending',-- pending|published|failed|suspect|canceled
  fail_kind    TEXT,                           -- login|risk|content|page|timeout|other
  result_url   TEXT,
  result_msg   TEXT,
  result_time  INTEGER,
  screenshot   TEXT,                           -- 回执截图文件名
  updated_at   INTEGER NOT NULL
);
CREATE INDEX idx_ptasks_sheet ON publish_tasks(sheet_id);

-- 使用台账：套装粒度，冗余展开列（查重窗口判定不 join）。与对账写入同事务。
CREATE TABLE usage_ledger (
  id           INTEGER PRIMARY KEY,
  date         TEXT NOT NULL,
  sku_id       INTEGER NOT NULL,
  pack_id      INTEGER NOT NULL,
  title_id     INTEGER NOT NULL,
  body_id      INTEGER,
  platform     TEXT NOT NULL,
  account_id   INTEGER NOT NULL,
  task_code    TEXT NOT NULL,
  published_at INTEGER NOT NULL,               -- 实际发布时间（回执解析）
  url          TEXT
);
CREATE INDEX idx_ledger_pack ON usage_ledger(pack_id, platform, published_at);

-- 收件箱收录记录：成功收录留报告，未知 SKU=待认领，解析失败=待人工确认
CREATE TABLE inbox_items (
  id          INTEGER PRIMARY KEY,
  file_rel    TEXT NOT NULL,                   -- 收件箱内相对路径（收录成功后=归档后路径）
  kind        TEXT,                            -- title|body|combo|media
  sku_code    TEXT,                            -- 识别出的 SKU 编码（可空）
  state       TEXT NOT NULL,                   -- ingested|unclaimed|failed
  detail_json TEXT,                            -- 收录报告：条数/话题差异/错误信息
  created_at  INTEGER NOT NULL
);
```

**设置项**（沿用 settings key/value_json 表，前缀 `publish.`）：`root_local`、`root_exec`、
`path_style`（默认 `windows`）、`dedup_days`（30）、`receipt_timeout_hours`（4）、
`autogen_time`（'22:00'）、`warn_material`（2）、`warn_title`（3）、`warn_body`（1，仅对有图集包的
SKU 生效）、`account_daily_limit_default`（3）、`min_gap_minutes`（60）、
`platform_matrix`（`{douyin:true,...}`）、`tier_rules`（`{hot:{daily:1}, warm:{weekly:3},
cold:{weeklyRotate:5}}`）、`time_slots`（`["11:30-13:00","18:00-20:00","21:00-22:30"]`）。

---

## 4. IPC 契约总表（命名即规范，全部经 specta 登记 + 重新生成 bindings）

### 4.1 命令（`src-tauri/src/commands/` 新增模块）

**publish_settings（并入 settings 域）**
- `get_publish_settings() -> PublishSettings` / `update_publish_settings(patch) -> PublishSettings`
- `pick_publish_root() -> Option<String>`（dialog）；保存时校验/创建四分区目录
- `use_local_as_exec_root()`（拓扑 B「同本机」一键）

**skus 域**
- `list_skus(filter: {tier?, warnOnly?, status?, query?}) -> Vec<SkuView>`
  （含三池余量、预警标记、最近发布时间——聚合查询一次出）
- `create_sku(input)` / `update_sku(id, patch)` / `set_sku_status(id, status)`
- `get_sku_detail(id) -> SkuDetail`（档案 + 池计数 + 发布历史分页读台账）

**assets 域**
- `list_asset_packs(skuId) -> Vec<PackView>`（含派生生命周期 + 回可用日期 + 锁定标记）
- `import_media_files(skuId, paths) -> PackView`（手动入素材池，走与收件箱相同的归集/命名管线）
- `pack_from_works(skuId, workIds) -> PackView`（作品库联动：复制输出图为图集包）
- `retire_pack(id)` / `restore_pack(id)` / `delete_pack(id)`（均校验锁定规则）
- `update_pack(id, patch: {note?, cover?})`

**texts 域**
- `list_text_items(skuId, kind) -> Vec<TextItemView>`
- `add_text_item(input)` / `update_text_item(id, patch)` / `set_text_item_enabled(id, bool)`

**inbox 域**
- `list_inbox_items(state) -> Vec<InboxItemView>`
- `claim_inbox_item(id, skuCode)`（指认后走正常收录管线）/ `discard_inbox_item(id)`
- `retry_inbox_item(id)`（解析失败重试）/ `rescan_inbox()`（手动全量扫描）

**accounts 域**
- `list_accounts()` / `create_account(input)` / `update_account(id, patch)` /
  `set_account_status(id, status)`

**planning 域**
- `generate_sheet(date) -> SheetView`（套装选取 + 排期展开 + 缺料清单，单事务；
  草稿存在则重算覆盖，已确认及之后报错）
- `list_sheets(page) -> Page<SheetSummary>` / `get_sheet(id) -> SheetDetail`（行明细 + 缺料）
- `reroll_set(sheetId, skuId) -> SheetDetail`（整包换该 SKU 当日套装）
- `update_task_row(id, patch: {plannedTime?})` / `cancel_task_row(id)` /
  `add_task_row(sheetId, {skuId, accountId, plannedTime?})` / `delete_task_row(id)`
- `confirm_sheet(id)` / `unlock_sheet(id)`（已确认退回草稿）
- `export_package(sheetId) -> ExportResult`（含包路径、文件数；完成置 exported）
- `open_package_dir(sheetId)`（opener）

**reconcile 域**
- `import_receipts(sheetId) -> ReconcileResult`（手动兜底，与 watcher 走同一管线）
- `resolve_suspect(taskId, outcome: {published: {url?}} | {failed: {kind}})`
- `get_dashboard(date) -> DashboardView`（计划/已发布/失败/待核对 + 平台账号完成率 + 账号健康）
- `get_report(sheetId) -> ReportView`

### 4.2 事件（`publish/events.rs`，沿用 engine/events.rs 的 Event derive 模式，250ms 节流）

- `PublishBadgesEvent`：资产库徽章（待认领 + 预警数）、发布计划徽章（待确认任务单 + 待核对数）
- `InboxIngestEvent`：单文件收录结果（成功/待认领/失败 + 报告摘要，前端 toast + 列表刷新）
- `SheetChangedEvent`：任务单状态/行状态变化（工作台与看板刷新，携 sheetId + 汇总计数）

---

## 5. 模块结构

### 5.1 Rust（`src-tauri/src/publish/`，新顶层模块）

```
publish/
├── mod.rs            模块出口 + PublishState（watcher 句柄、ingest/reconcile 工作者）
├── paths.rs          四分区常量、RelPath newtype、ASCII 规范化、导出路径拼接（win/mac 风格）
├── platform.rs       五平台枚举 + 中文名映射 + platform 标签匹配规则
├── inbox/
│   ├── parser.rs     TXT 三类解析 + 【话题】+ SKU 三冗余识别（纯函数）
│   ├── watcher.rs    notify 监听 + 大小稳定防抖（虚拟时钟可测）
│   └── ingest.rs     收录事务：入库/归档/移资产库/待认领/失败 + 事件
├── planner/
│   ├── set_picker.rs 套装选取（查重过滤→最少使用→随机；平台标签匹配回退通用）（纯函数）
│   ├── scheduler.rs  当日应发清单（分层频率+轮播游标）→ 展开 → 约束过滤 → 时段分配（纯函数）
│   └── mod.rs        generate_sheet 事务编排 + 缺料清单
├── xlsx/
│   ├── writer.rs     22 列任务单写入（表头中文常量与 §5 逐字一致）
│   └── reader.rs     回执读取（按表头定位列，只取 20–22 列 + 任务 ID）
├── exporter.rs       任务包导出：素材复制 + body.txt + 执行说明.md 模板 + READY.txt 最后写
├── reconcile.rs      对账三分支 + 失败六类处置 + 疑似标记 + 关单与日报（串行工作者）
└── ticker.rs         应用内定时（生成明日 + 超时扫描）+ 启动补跑三查
```

`db/repo/` 新增：`skus.rs`、`assets.rs`、`texts.rs`、`accounts.rs`、`planning.rs`、
`ledger.rs`、`inbox.rs`。`commands/` 新增：`publish_settings.rs`、`skus.rs`（注意与现有文件
命名冲突则用 `publish_` 前缀统一）、`publish_assets.rs`、`publish_inbox.rs`、
`publish_accounts.rs`、`publish_planning.rs`、`publish_reconcile.rs`。
全部命令与 3 个事件在 `lib.rs` 的 `specta_builder()` 登记后
`cargo test --lib export_bindings` 重新生成并提交 `bindings.ts`。

### 5.2 前端

```
src/features/assets/    资产库页：SKU 列表、SKU 详情（档案+三池+发布历史）、收件箱状态区
src/features/plan/      发布计划页：看板 / 任务单列表+工作台 / 策略与账号 三页签
src/features/settings/  追加「发布与同步」区块
src/features/works/     追加「入资产库」入口（唯一触碰现有页的点）
src/stores/publish.ts   事件镜像 + 徽章 + 页面 UI 态（Zustand，不持有可变业务状态）
```

路由 + 导航加两项（资产库/发布计划）+ 徽章；⌘K 补全：生成明日任务单、重扫收件箱、
打开资产库/发布计划。状态色新增映射进 `src/lib/status.ts`；新样式 token 全部走
`globals.css` CSS 变量（guardrails 既有 oklch 检查自动覆盖）。

---

## 6. 质量体系增量（在 CLAUDE.md 既有门禁之上）

1. **覆盖率闸门扩展**（check.yml llvm-cov）：`publish/planner/`、`publish/inbox/parser`、
   `publish/xlsx/` 加入 ≥ 85% 行覆盖闸门（与 engine/ids/importer 并列）。
2. **proptest 三处**（不变量驱动，专治自证测试）：
   - 排期器：任意策略/账号/SKU 输入 → 产出任务行**必然**满足：账号不超日限、同平台多账号
     最小间隔 ≥ 设置值、时间落在时段模板内、行数 = 期望展开数 − 约束裁剪数、疑似任务永不出现在产出中。
   - inbox parser：任意字节输入不 panic；规范样例 round-trip（解析→重序列化→再解析一致）。
   - 任务 ID 发放：任意生成/重生成序列不产生重复 task_code。
3. **xlsx round-trip 测试**：writer 写出 → calamine 读回 22 列逐格一致；
   另有「模拟执行器」测试：整文件重写（模拟影刀/Excel 转存丢样式）只改 20–22 列 → 对账结果正确。
4. **确定性异步测试**：防抖、ticker、超时扫描全部 `tokio::time::pause` 虚拟时钟。
5. **端到端集成测试**（tempdir 全链路）：造根目录 → 放规范 TXT/图片 → 收录 → 生成任务单 →
   确认导出 → 脚本改写 xlsx 模拟回执 → 对账 → 台账/关单/日报断言。P3 出口必备。
6. **变异测试**：P3 出口对 `publish/planner/` + `publish/reconcile.rs` 跑 cargo-mutants，
   存活体补测试或书面豁免（追加进 docs/mutants-exemptions.md）。
7. **审查协议沿用 §1.4**：每阶段 PR 由全新上下文 `/code-review high`（本模块全部涉及数据层）；
   发现项修复后才 merge。tag v0.7.0 前由用户触发 `/code-review ultra`（本模块属「发布链级」深审对象）。
8. **测试完整性规则沿用**：禁止为过 CI 修改/删除/放宽既有断言。

---

## 7. 任务拆解（一个任务 ≈ 一个或一组小步提交；每阶段一分支一 PR）

### P0 开工准备（并入 P1 分支首个提交组）

| #   | 任务 | DoD |
| --- | --- | --- |
| 0.1 | 解包 handoff zip：`发布与资产管理原型.dc.html` → `docs/prototype/publish.dc.html` 入库；zip 本体不入库（根目录加 .gitignore 条目） | 原型可离线打开；git status 干净 |
| 0.2 | Cargo 新依赖（notify/rust_xlsxwriter/calamine/chrono）+ `cargo audit` 过 | `pnpm check` 绿 |
| 0.3 | check.yml 覆盖率闸门扩展（§6.1，先注册目录，阈值随实现生效） | CI 配置审查通过 |

### P1 资产管理（分支 `feat/publish-p1-assets`）

| #    | 任务 | DoD |
| ---- | --- | --- |
| 1.1  | migration 0010 全 schema + repo 七模块骨架 + 域类型（SkuView/PackView/TextItemView/InboxItemView…）+ specta 登记 + bindings 再生成 | migration 幂等应用；`cargo test` 含 bindings 同步绿 |
| 1.2  | `paths.rs`：四分区常量、RelPath newtype、ASCII 规范化（转小写/去空格/非法字符替换/重名加序）、win/mac 路径拼接纯函数 | 单测覆盖规范化与拼接全分支（含 260 字符长度告警） |
| 1.3  | publish_settings：get/update/pick/同本机；保存校验并创建四分区 | 设置往返 + 目录创建集成测试 |
| 1.4  | skus 域五命令 + 内置「通用」行 + 列表聚合查询（余量/预警/最近发布） | 临时库集成测试；预警阈值边界用例 |
| 1.5  | texts 域四命令 | 集成测试 |
| 1.6  | assets 域：包 CRUD + 生命周期派生查询（此时无台账，派生分支以固定用例测）+ 锁定校验（P1 恒不锁，逻辑先入位）+ `import_media_files` 归集/命名管线 | 集成测试：导入→重命名→files_json 正确 |
| 1.7  | inbox parser：三类 TXT + 【话题】+ SKU 三冗余识别 | 规范文档全部样例通过 + proptest 不 panic + 覆盖 ≥85% |
| 1.8  | inbox watcher + ingest 串行工作者：防抖→解析→事务（入库/归档已收录/移资产库/待认领/失败）→ `InboxIngestEvent`；`rescan_inbox` 启动全量扫描 | tempdir 集成测试三分支（成功/未知 SKU/坏文件）；防抖虚拟时钟测试 |
| 1.9  | inbox 域命令（认领/丢弃/重试/重扫） | 认领后走完整收录管线断言 |
| 1.10 | `pack_from_works` 作品库联动 | 选 N 张输出图 → 图集包落盘 + 记录正确 |
| 1.11 | 徽章计算 + `PublishBadgesEvent`（待认领 + 预警） | store 测试（mockIPC） |
| 1.12 | 资产库页 UI：SKU 列表（筛选/搜索/预警标记，1:1 原型） | 与原型逐区块对照；空态齐全 |
| 1.13 | SKU 详情：档案编辑（话题标签有序维护）+ 三池页签（素材网格含生命周期色/标题/正文列表）+ 发布历史页签（P1 空态） | 同上 |
| 1.14 | 收件箱状态区 UI（待认领指认/丢弃、失败查看/重试）+ 作品库「入资产库」弹窗 | 同上 |
| 1.15 | 设置页「发布与同步」区块 UI + 导航两项 + 徽章接线 + ⌘K 补全 | 冒烟走查 |

**P1 DoD（需求 §11）**：收件箱放入规范 TXT/图片自动入库；SKU 列表展示余量与预警；
门禁全绿；全新上下文 `/code-review high` 通过后 merge。

### P2 编排与导出（分支 `feat/publish-p2-planning`）

| #    | 任务 | DoD |
| ---- | --- | --- |
| 2.1  | accounts 域四命令 + 策略设置（tier_rules/matrix/time_slots）读写 | 集成测试 |
| 2.2  | `set_picker.rs`：查重过滤（台账 + 在途引用，前置事实 6）→ 最少使用 → 同分随机；标题/正文平台标签匹配回退通用 | 单测全分支 + 固定 seed 随机可复现 |
| 2.3  | `scheduler.rs`：当日应发清单（热=每日×平台集；温=每周 N 次；冷=轮播池每周轮出 N，游标存 skus 表新列或 settings）→ 套装 × 平台 × 账号展开 → 日限/最小间隔过滤 → 时段分配 + 抖动；缺料清单副产物 | proptest 不变量全套（§6.2）+ 覆盖 ≥85% |
| 2.4  | `generate_sheet` 事务：套装 + 行 + task_code 发放 + 缺料入 sheet；草稿重生成覆盖语义 | 集成测试：重生成幂等、确认后拒绝 |
| 2.5  | 工作台编辑命令：reroll_set / 改时 / 取消行 / 增行 / 删行 / confirm / unlock；素材包锁定自此生效 | 集成测试含锁定拒绝路径 |
| 2.6  | `xlsx/writer.rs` 22 列 + round-trip 测试 | §6.3 第一项绿 |
| 2.7  | `exporter.rs`：包目录（`任务包/{YYYYMMDD}/`）+ 素材按 SKU 复制 + body.txt + `执行说明.md`（include_str! 模板：只写 20–22 列、READY 语义、截图命名、六类失败文案枚举）+ READY.txt 最后写 + 路径拼接执行机风格 | tempdir 集成测试逐文件断言；READY 为最后 mtime |
| 2.8  | `ticker.rs`：autogen_time 生成明日 + 启动补跑查① ②（超时扫描 P3 接入） | 虚拟时钟测试：到点触发、错过补跑、不重复生成 |
| 2.9  | 发布计划页——任务单列表 + 工作台 UI（按 SKU 分组折叠、封面缩略图行、缺料提示条、换套装/改时/删增行、确认→导出，1:1 原型） | 原型对照；全动作走查 |
| 2.10 | 策略与账号页签 UI（分层频率/平台矩阵/账号卡/时段模板） | 原型对照 |
| 2.11 | 徽章追加「待确认任务单数」+ store 测试 | mockIPC 测试绿 |

**P2 DoD（需求 §11）**：一键生成明日任务单草稿 → 人工调整确认 → 导出的任务包可被影刀直接
消费（xlsx 22 列 + 素材 + READY 齐备）；排期约束 proptest 覆盖；门禁全绿；`/code-review high`。
**P2 合入后启动「陪跑一周」**（本地构建，人工执行影刀 + 核对回执质量）；P3 开发可并行启动，
陪跑发现的契约问题以小提交并入 P3 分支。

### P3 回执闭环（分支 `feat/publish-p3-reconcile`）

| #    | 任务 | DoD |
| ---- | --- | --- |
| 3.1  | `xlsx/reader.rs`：按表头定位，读任务 ID + 20–22 列；容忍整文件转存、忽略 `~$` 锁文件与冲突副本（检出即告警） | 模拟执行器测试（§6.3 第二项） |
| 3.2  | 任务包 watcher（复用防抖基建）+ `import_receipts` 手动兜底，汇入 reconcile 串行工作者 | tempdir 集成测试 |
| 3.3  | 对账三分支事务：已发布→台账+归档链接/截图/实发时间；失败→六类处置（前置事实 15，timeout 次日自动补排、risk 当日熔断）；超时→疑似已发。RPA 回写信息 `链接｜原因｜时间` 解析 | 各分支 + 处置全枚举集成测试 |
| 3.4  | 疑似已发：`resolve_suspect` 人工定态 + **负向断言**（重生成/补排/重排任何自动路径不含疑似任务） | 专项测试绿（前置事实 14） |
| 3.5  | 关单 + 日报：全行终态自动 closed + report_json（成功率/失败清单/缺料/明日建议） | 集成测试；疑似行阻塞关单断言 |
| 3.6  | ticker 接入超时扫描（每 10 分钟 + 启动补跑查③） | 虚拟时钟测试 |
| 3.7  | 素材生命周期完整生效：已用尽/回可用派生展示、SKU 详情发布历史填充 | 台账驱动的派生查询测试 |
| 3.8  | 看板页签 UI：四计数 + 平台/账号完成率 + 账号健康（正常/停用/当日熔断）+ 日报入口 + 疑似处置交互（1:1 原型，「禁止重发」约束感文案） | 原型对照 |
| 3.9  | 徽章追加「待核对数」；`SheetChangedEvent` 全链路接线 | mockIPC 测试 |
| 3.10 | 端到端集成测试（§6.5 全链路） | 一条测试跑通收录→出包→回执→关单 |

**P3 DoD（需求 §11）**：执行器回写后自动对账入台账；超时任务正确标琥珀且绝不被重发；门禁全绿；
`/code-review high`。

### 收尾（P3 分支尾部或独立小分支）

| #   | 任务 | DoD |
| --- | --- | --- |
| F.1 | cargo-mutants（planner + reconcile）存活体清零或豁免 | docs/mutants-exemptions.md 更新 |
| F.2 | 双端 UI 冒烟：两新页 + 设置区块 + reduced-motion 走查；pnpm/cargo audit 清零或豁免 | 人工清单打勾 |
| F.3 | CLAUDE.md 更新：新命令表、领域词汇（SKU/素材包/套装/任务单/任务包/回执/台账/查重窗口/收件箱/待认领/疑似已发）、里程碑进度 | 与实现一致 |
| F.4 | 用户触发 `/code-review ultra` 深审 → 修复 → bump 0.7.0 + tag `v0.7.0` 发版 | release 三 job 绿 + latest.json 正确 |

---

## 8. 残余风险与默认处置（执行中遇到时不再询问）

| 风险 | 默认处置 |
| --- | --- |
| 影刀/Excel 保存 xlsx 时整文件重写、丢样式、改单元格类型 | 对账只按**文本值**读取，任务 ID trim 后字符串匹配；对不上的行记警告不中断；表头行定位列而非按列号硬编码 |
| 同步软件产生冲突副本（`任务单 (conflicted copy).xlsx`） | 只认准确文件名 `任务单.xlsx`；扫描发现疑似冲突副本 → 看板告警转人工，绝不自动合并 |
| Excel 打开产生 `~$任务单.xlsx` 锁文件 | watcher 忽略 `~$` 前缀与隐藏文件 |
| 同步盘上 notify 事件风暴/丢事件 | 大小稳定防抖收敛 + `rescan_inbox`/手动导入回执双兜底；启动全量扫描 |
| 图集图片分批到达被拆成多包 | 按前置事实 10 处理；收录报告提示「该 SKU 今日已归集 N 包」，人工可退役多余包（V1 不做合并） |
| 管理端在 mac、执行机根路径拼接出错 | `paths.rs` 拼接纯函数 + win/mac 两风格单测矩阵；执行说明.md 内附一行自检路径供执行侧人工核对 |
| 已导出后人工改包内容 / 重复导出与执行器写冲突 | 已导出不可编辑；重新导出=先删 READY.txt 再整包覆盖 + UI 红色警告；单写方约定写入执行说明.md |
| 22:00 生成时应用休眠/时钟跳变 | ticker 用「下次触发时刻」比较而非固定 interval；唤醒后首个 tick 补查 |
| 大视频复制导致导出卡 UI | 导出在 tokio 阻塞线程池执行，前端 spinner；素材量级（每 SKU 一份）预期秒级，超时再优化（V2） |
| 根路径未配置/被移动 | 两新页显式空态引导去设置；启动校验四分区存在，缺失即重建（空目录）并 toast |
| `publish_tasks` 行随年积累 | V1 保留不清理（SQLite 体量可忽略），清理策略入 V2 backlog |

---

## 9. V2 预留自检（发版前逐项确认未被写死）

- `daily_sets.account_id` 预留列存在且 V1 恒 NULL（每账号独立变体）。
- `text_items.source` 为开放字符串（V2 加 `ai`：GenDesk 内直接生成文案变体，复用 provider 基建）。
- `usage_ledger` 可平滑加播放/点赞列（发布数据回收反哺冷热分层）。
- `skus.topics_json` 为 JSON（V2 话题按平台差异化时可迁移为对象形态）。
- 执行说明.md 为独立模板文件（V2 agent 深度定制不动导出器）。
- 平台枚举集中在 `platform.rs` 单点（加平台=改一处 + migration 无涉）。

---

## 10. 给执行会话的操作提醒

1. 开工前通读：需求文档全文、收件箱格式规范全文、`docs/prototype/publish.dc.html` 源码全文
   （READ IN FULL，不截图）、CLAUDE.md。
2. 每阶段分支命名与提交风格照 §7；每任务提交注明任务号（如 `feat(publish): 1.7 inbox parser`）。
3. 改动命令/事件后必跑 `cargo test --lib export_bindings` 并提交 bindings.ts；
   `pnpm check` 是提交前唯一可信门禁。
4. 遇到需求文档与原型冲突 → 按需求文档实现并在 PR 描述记录差异点；
   遇到本计划未覆盖的决策 → 按 §8 默认处置；两者都不覆盖且影响契约（xlsx 列/目录结构/状态机）
   → 停下来问用户，其余自行判断并在 PR 描述记录。
