# 内部图片生产工具 · 密钥存储迁移执行计划（Keychain → 本地加密文件）

> 目标：**彻底消灭 macOS Keychain 授权弹窗**（每次更新 app / dev 重编译后反复弹）。
> 决策背景：无 Apple 开发者证书（不考虑），CI 用自签名证书，签名锚点不受信任 →
> Keychain 按应用 ACL 的"始终允许"无法跨版本存活，此为系统固有限制，配置不可修复。
> 结论：放弃按应用 ACL，把 API Key 从系统钥匙串迁到本地加密文件。
>
> **安全水位（如实记录，写进代码注释与文档）**：无可信签名身份时，任何方案都收敛到
> "以当前用户身份运行的进程均可读取密钥"。本方案靠 文件权限 0600 + FileVault 全盘加密
> + 文件层 XChaCha20-Poly1305（**防误不防恶**：防备份/截图/grep 出现明文，密钥文件与
> 数据同目录，不构成独立安全边界）。泄露爆炸半径 = 可轮换的第三方生图 API Key。
> 若未来上 Apple 证书，回迁 Data Protection Keychain（见 S5 backlog 注记）。

## 现状（改动锚点）

- 抽象已就绪：`src-tauri/src/secrets.rs` 的 `SecretStore` trait（set/get/delete），
  生产实现 `KeyringStore`，测试实现 `MemoryStore`。
- 唯一注入点：`src-tauri/src/lib.rs` `setup_app()` 中
  `let secrets: Arc<dyn secrets::SecretStore> = Arc::new(secrets::KeyringStore);`
  该处 `data_dir`（`app.path().app_data_dir()`）与 DB 池均已就绪，可直接做迁移。
- 钥匙串条目：service `com.gendesk.app`，每个 API Key 一个 account；
  account 名单来源 = DB `api_keys.keyring_account` 列。
- 引擎启动时 `engine::load_key_configs()` 逐 Key 读密钥——迁移必须发生在
  `Engine::start` 之前，保证引擎首读即走文件、零弹窗。

## 任务拆解

### S1 `FileStore` 实现（secrets.rs）

- 新增 `FileStore`，实现 `SecretStore`，构造时传入存储目录（app data dir 根）。
- 磁盘布局：`secrets.key`（32 字节随机主密钥，首次创建时生成）+ `secrets.enc`
  （XChaCha20-Poly1305 密文；24 字节随机 nonce 前置存放，每次写入重新随机）。
  明文为 JSON：`{"version":1,"keys":{"<account>":"<secret>"}}`。
- 依赖：`chacha20poly1305`（RustCrypto，启用 `xchacha20` 相关默认特性）+ `rand`
  （生成主密钥/nonce；仓库已有则复用）。
- **原子写**：写临时文件（同目录）→ fsync → rename 覆盖。两文件创建时（Unix）
  设权限 `0o600`；Windows 无此语义，依赖用户 profile 目录 ACL，代码注释说明。
- 内部 `Mutex` 串行化读改写（single-instance 已禁双开，Mutex 防同进程并发即可）。
- **损坏自愈**：`secrets.enc` 解密/解析失败 → 重命名为 `secrets.enc.bak-<时间戳>`
  留证，从空 map 重建，`tracing::warn!` 记录（不含任何密钥内容）；用户可在设置页
  重填 Key。`secrets.key` 缺失但 `secrets.enc` 存在 → 同样走留证重建。
- 模块头注释改写：删除"Key 明文只进系统钥匙串"表述，替换为本计划开头的
  安全水位说明（含"混淆非安全边界"字样）。`mask()`、`MemoryStore` 原样保留。

### S2 注入替换 + 一次性迁移（lib.rs + secrets.rs）

- `setup_app()`：`KeyringStore` → `FileStore::new(&data_dir)`（在 `dirs.init()` 后、
  `Engine::start` 前）。
- 新增迁移函数（建议签名 `migrate_secrets(pool, source: &dyn SecretStore, dest: &dyn SecretStore)`，
  source = `KeyringStore`，便于用 `MemoryStore` 做双端测试）：
  1. 查 DB `api_keys` 全部行，取 `keyring_account` 名单；
  2. 逐 account：dest 已有 → 跳过（**幂等**，每次启动都跑）；dest 没有 → 从 source
     读（此处触发**最后一轮** Keychain 授权弹窗，用户点允许后永久安静）；
  3. 读到 → 先写 dest（落盘成功后）再删 source 条目；读失败/用户点拒绝 →
     记 warn、跳过该条、**不删源**，下次启动重试；整体绝不因单条失败中断启动。
- 迁移失败的兜底本来就存在：设置页 API Key 编辑功能可手动重填。
- `KeyringStore` 与 `keyring` 依赖**暂留**（仅迁移路径使用），移除时点见 S5。

### S3 双端统一

- macOS / Windows 均改用 `FileStore`（Windows 凭据管理器虽不弹窗，但统一实现
  消灭平台分支；迁移函数对 Windows 同样生效，keyring `windows-native` 特性照常读源）。
- Linux 不在支持矩阵，不处理。

### S4 测试

- `FileStore` 单测：set/get/delete 往返；重开实例后可读（持久化）；delete 不存在
  的 account 返回 Ok；损坏文件自愈（写垃圾字节 → get 报 warn 重建 → set 可用，
  且 `.bak-*` 留证存在）；Unix 下断言文件权限 0600。
- 迁移单测（`MemoryStore` 双端模拟）：全量迁移成功且源被清空；dest 已有的条目
  跳过且不动源（幂等）；source 读失败（可用一个注入错误的 mock store）→ 其余
  条目照迁、失败条目源保留。
- **测试密钥一律用明显假值**（如 `test-key-0001`）；**禁止** `sk-` 等仿真真实
  格式的前缀——避免 GitHub secret scanning / push protection 误报，也避免真假难辨。
- 测试内 `unwrap` 按现行规约：带说明注释的局部 `#[allow(...)]`。

### S5 文档同步

- `内部图片生产工具_技术选型定稿.md` §5.3：由"密钥只进系统钥匙串"改写为本方案
  （存储布局 + 安全水位 + 决策原因：自签名下 Keychain ACL 不可跨版本存活）。
- `CLAUDE.md`：错误处理/相关章节若提及 keyring 存储事实，同步更新（里程碑历史
  记录不改写）；本次完成后在里程碑清单追加一行。
- `docs/V2-backlog.md` 追加两条：
  1. 若未来取得 Apple 开发者证书 → 回迁 Data Protection Keychain
     （keychain-access-groups 使授权跨版本存活），`SecretStore` trait 使回迁仅为换实现；
  2. 迁移期结束（估计 1–2 个发版周期、确认团队全员升级）后移除 `keyring` 依赖
     与 `KeyringStore`，减 cargo audit 面。

### S6 门禁与验收

- `pnpm check` 全绿（guardrails / Biome / tsc / vitest / build / fmt / clippy -D /
  cargo test / cargo check）。新增 crate 过每周 cargo audit（提交前手动跑一次
  `cargo audit` 确认无告警）。
- 无 migration、无新 IPC、无前端改动（`bindings.ts` 不应有 diff）。
- **手工冒烟（macOS，AI 无法替代，合并前执行）**：
  1. 装旧版 + 已配 Key → 启动新构建 → 出现**最后一轮**授权弹窗，点允许 →
     「钥匙串访问.app」中确认 `com.gendesk.app` 条目已清空，设置页 Key 可用、能生成；
  2. 重新编译再启动（模拟更新）→ **零弹窗**；
  3. `pnpm tauri dev` 重复编译启动 → **零弹窗**；
  4. 全新数据目录首启 → 添加 Key → 重启 → 零弹窗、Key 存活。

## 流程约束（沿用仓库铁律）

- 一个分支（建议 `fix/secrets-file-store`）= 实现 + 测试 + 门禁全绿，小步提交。
- 非测试代码禁 unwrap/expect/panic；错误统一走 `error.rs` thiserror。
- **密钥永不进日志/DB/事件载荷**（现行纪律原样保留；warn 日志只写 account 名，
  不写密钥内容）。
- 合并前由**全新上下文**跑 `/code-review high`（涉密钥路径，按审查协议用 high）。
- 版本号 bump 与发版由用户另行决定，本计划不打 tag。

## GitHub 托管安全性说明（结论：零影响）

密钥是**运行时数据**，落在用户机器 app data 目录，与 git 仓库不相交，不会被
push、不会被 GitHub secret scanning 扫到、不随 fork/clone 传播。仓库侧唯一纪律
= S4 的"测试假值"规则。CI 全程不接触任何真实 API Key。
