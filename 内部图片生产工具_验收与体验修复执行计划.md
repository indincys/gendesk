# 验收页与体验修复执行计划（5 项）

> 交接文档：每个「任务」自成一体，可用 `/goal` 单独指派给全新上下文的执行 Claude。
> 权威约定见 `CLAUDE.md`。执行前请先读 `CLAUDE.md` 的架构铁律、IPC 约定、审查协议（§1.4）。
> 生成日期 2026-07-14。共 5 项：T1 验收页虚拟化 / T2 验收快捷键 / T3 按 Key 分类验收 /
> T4 keychain 免重复弹窗 / T5 弹层滚动限制。

---

## 通用约束（所有任务适用）

- **分支/提交/审查**：一个任务 = 一个分支 = 实现 + 测试 + 门禁全绿 + 全新上下文 `/code-review` 通过 = 一次 merge（`main` 受保护走 PR）。纯前端任务 review 用 medium；涉及 Rust/数据层用 high。
- **提交前门禁**：`pnpm check`（guardrails + Biome + tsc strict + vitest + vite build + cargo fmt/clippy -D warnings + cargo test + cargo check）必须全绿。纯前端任务至少 `pnpm typecheck && pnpm test && pnpm guardrails`；触及 Rust 需 `cd src-tauri && cargo test`。
- **架构铁律**：前端只经 `src/lib/ipc/` 出入；`oklch(` 硬编码仅限 `src/styles/globals.css`；不新增业务轮询（事件驱动）。
- **测试完整性**：禁止为让 CI 变绿而改/删/放宽既有测试断言。
- **落地顺序（固定）**：T5 → T1 → T2 → T3 → T4。T1/T2/T3 均改 `ReviewPage.tsx`，须按此序落地并逐个 rebase，避免冲突；T4、T5 与验收页无耦合，可并行推进。

---

## T1 — 验收页虚拟化 + 等高网格（重点，性能）

**分支**：`fix/review-virtualize`　**Review**：medium

### 背景与根因
验收页 `src/features/review/ReviewPage.tsx` 用 CSS `column-count` 瀑布流渲染**全部** `displayed` 项，缩略图 `.thumbimg` 为 `height:auto`（高度未知），且 `.rclhead` 分组头用 `column-span:all`。三者叠加导致：每张 lazy 图加载 / 每次滚动都触发**整个多列流的跨片段重排**（默认 `group` 排序下有几十个 `column-span:all` 分组头，最坏情况）。加之卡片 JSX 内联、无 `React.memo`，任一状态变化（焦点/选中/待定）都重渲全部 N 张卡。几百张时主线程被布局与重渲打满 → 页面卡顿，且几百个 DOM 节点使切换到其它页面时的卸载也卡。

### 目标
只渲染视口可见行，DOM 节点由几百降至几十；等高单元格消除多列重排。使用**已安装但未使用**的 `@tanstack/react-virtual`（`package.json` 已有 `^3.13.0`）。

### 改动清单
1. **拆出 `ReviewCard`（`React.memo`）**
   将 `ReviewPage.tsx:392-469` 的单卡 JSX 抽为独立组件。props：`item / idx / selected / focused / pending`（布尔）+ 稳定回调 `onCardClick(idx, shift) / onAccept(id) / onReject(id) / onRetry(item) / onTogglePending(id) / onZoom(idx)`。父级所有回调 `useCallback` 化（`accept/reject/openRetry` 已是；`onCardClick/togglePending/setZoom` 需包稳定）。memo 后按键/选中只重渲变化的 1–2 张。
2. **扁平行模型**（`useMemo`，依赖 `displayed / cols / sortMode / clusterCounts`）
   ```ts
   type Row =
     | { kind: "header"; key: string; count: number }
     | { kind: "cards"; items: ReviewItemView[] };
   ```
   遍历 `displayed`：聚类键（复用现有 `clusterKey`）变化且非 time 模式时，先 flush 当前不满行、再 push 一个 header 行；卡片按 `cols` 个攒成 cards 行。另建 `cardRow: number[]`（卡片全局 index → 所在行号），供焦点滚动。
3. **虚拟化容器**（`.pbody` 作为 scrollElement）
   ```ts
   const parentRef = useRef<HTMLDivElement>(null);
   const virt = useVirtualizer({
     count: rows.length,
     getScrollElement: () => parentRef.current,
     estimateSize: (i) => (rows[i].kind === "header" ? 56 : 220),
     overscan: 8,
   });
   ```
   渲染 `virt.getVirtualItems()`：外层 `position:absolute; left:0; width:100%; transform:translateY(start)`，`data-index={v.index}`，`ref={virt.measureElement}`（动态测高，随容器宽/cols 自适应）；滚动区外层高度 `virt.getTotalSize()`。
4. **等高网格行**：cards 行用 `display:grid; grid-template-columns: repeat(cols,1fr); gap:12px`。缩略图框改**固定比例 + cover**：新增类 `.rcsq{aspect-ratio:1; overflow:hidden}`，内部 `.thumbimg{height:100%; object-fit:cover}`。高度已知 → 零重排。保留 sel/focus/pend/悬浮操作/meta 视觉钩子。
5. **移除旧多列 CSS**（`src/styles/globals.css`）
   - `.rgrid`（946 起）去掉 `column-gap`/由内联 `columnCount` 驱动的多列，改为承载虚拟化滚动区所需样式或直接由 `.pbody` 承担。
   - `.rclhead`（1127 起）去掉 `column-span:all`（1142），改普通整宽块；相关 `.rcard`/`.rgrid>.rcard` 的 `break-inside:avoid` 不再需要。
6. **焦点滚动**：把 `ReviewPage.tsx:276-278` 的 `document.querySelector('[data-ridx]').scrollIntoView` 换成 `virt.scrollToIndex(cardRow[focus], { align: "auto" })`。
7. 保留 `每行` 滑块（3–8）控制 `cols`；行模型 chunk 大小 = `cols`。

### 决策（已确认）
验收网格由自然比例瀑布流改为**统一等高方格**。作品库 / 参考图库网格本次**不动**。

### 验收标准
- 造 500+ 待验收任务（可临时放宽或 mock 数据源），滚动、方向键移动焦点、切换到其它页面均**无可感卡顿**；DevTools 中 DOM 仅保留可见行+overscan。
- 分组头随聚类正确出现、计数正确；`每行` 调整即时生效不卡。
- `pnpm check` 全绿。

---

## T2 — 验收页快捷键修正

**分支**：`fix/review-keyboard`　**Review**：medium

### 背景与根因
`ReviewPage.tsx:227-270` 的网格 `keydown` 挂在 `window`。问题：
- 悬浮操作按钮 / `每行` 滑块是真实 `<button>`/`<input>`，点击后**保留焦点**：原生 Space/Enter 会再触发一次该控件，同时 window 处理器也触发 → 双重 / 错位。
- 滑块聚焦时，处理器因 target 是 `INPUT` 直接 `return`，方向键改动滑块值、网格导航「失灵」。
- 批次弹层（`showBatchPicker`，`ReviewPage.tsx:607-649`）打开时，处理器仅由 `zoom/retryTarget` 守卫，按键仍作用于背后网格；且该自定义 `.ovl` 无 Esc 关闭。

### 改动清单（均在 `ReviewPage.tsx`）
1. **扩大让位守卫**：网格 keydown 处理器开头加
   ```ts
   const el = e.target as HTMLElement | null;
   if (el?.closest("button, input, textarea, select, [contenteditable=true]")) return;
   ```
   并把 `showBatchPicker` 纳入 early-return 守卫与 effect 依赖数组（弹层打开时不作用于背后网格）。
2. **点击后失焦**：悬浮操作按钮（`408-463`，尤其 `待定`/`大图` 这类点击后不移除卡片的）onClick 末尾加 `(e.currentTarget as HTMLElement).blur()`，键盘焦点回网格，消除「再按空格/回车双触发」。
3. **`每行` 滑块**（`358-366`）加 `onMouseUp={(e)=>e.currentTarget.blur()}`（`onKeyUp` 同理），拖动/调整后方向键网格导航恢复。
4. **批次弹层**（`607-649`）迁到共享 `<Modal>`（`src/components/ui/Modal.tsx`，自带 Esc + 捕获阶段拦截），并依赖第 1 条守卫杜绝穿透。
5. **指针跟随焦点**：卡片 `onMouseEnter` 时 `setFocus(idx)`，令空格/回车作用于**鼠标悬停**那张而非默认第 0 张。（本任务在 T1 之后落地，卡片已 `React.memo`，hover 改焦点仅重渲 1–2 张，成本可忽略。）

### 验收标准
- 逐一验证：空格选中、⏎ 通过焦点/所选、⌫ 不通过、S 待定、Z 大图、⌘A 全选、⇧ 范围点选，均按提示栏语义工作。
- 点过悬浮按钮 / 拖过 `每行` 滑块后，方向键网格导航仍正常。
- 批次弹层打开时背后网格不响应键盘；Esc 可关闭弹层。
- `pnpm check` 全绿。

---

## T3 — 验收页增加「按 Key 分类验收」

**分支**：`feat/review-by-key`　**Review**：medium

### 背景
后端 `ReviewItemView.keyAlias` 已提供（`src-tauri/src/commands/review.rs`，无需改后端）。前端 `sortMode` 目前仅 time/ref/group。

### 改动清单（均在 `ReviewPage.tsx`）
1. `sortMode` 类型加 `"key"`（第 22 行）；分段控件（`338-357`）加「按 Key」项。
2. `displayed`（`140-151`）加分支：`arr.sort((a,b)=>(a.keyAlias??"~").localeCompare(b.keyAlias??"~")||a.id-b.id)`（`"~"` 使无 Key 排末）。
3. `clusterKey`（`154-155`）：key 模式返回 `it.keyAlias ?? "未标注 Key"`；`clusterCounts`（`158-166`）纳入 key 模式；分组头标签（`385`）key 模式显示「Key」。
4. **分类验收核心**：分组头 `.rclhead` 增加两个操作「通过本组 / 本组移废纸篓」，对该 clusterKey 下全部 `displayed` 项批量 `accept`/`reject`。对 ref/group/key **三种聚类都生效**，真正实现「按类验收」。（复用现有 `accept([...ids])`/`reject([...ids])`，含 inFlight 幂等守卫。）

### 验收标准
- 多 Key 批次下切「按 Key」：正确按 Key 聚类、计数正确、无 Key 项归入「未标注 Key」并排末。
- 分组头「通过本组」批量对该类全部生效（含自动转正/写回等既有副作用）。
- `pnpm check` 全绿。

---

## T4 — 更新后 keychain 反复弹窗（自签名证书根治）

**分支**：`chore/macos-selfsigned`　**Review**：high（涉及构建/密钥）

### 背景与根因
App **无稳定代码签名**：`.github/workflows/release.yml` 的 `tauri-apps/tauri-action` 步骤（47-61）未传签名证书 → macOS 产物 ad-hoc/未签名，**每次构建指纹（cdhash）都变**。macOS keychain 的「始终允许」按 App 的 designated requirement（DR）匹配 → 每次更新失配、重新弹窗。且 `add_api_key`（`src-tauri/src/commands/api_keys.rs:115`）**每把 Key 各存一条钥匙串项**（account 唯一），引擎启动 `reload_keys` 一次读全部 Key → 更新后启动即连弹 N 次。

### 方案（确定）
用免费**自签名代码签名证书**（非 Apple Developer ID / 公证方案，无需 $99 账号、无需联网公证）。每次构建用**同一张**证书 → 代码签名 leaf 指纹不变 → DR 稳定 → 一次「始终允许」永久生效。

分发与更新走 CI（tag 推送触发 `release.yml`），故签名只在 CI 落实：给 tauri-action 注入证书与签名身份，**不改** `tauri.conf.json`（保持本地 `tauri dev`/`build` 无证书也能跑）。

### 用户手动操作流程（一次性，必须你本人执行；私钥不交给 AI）

**第 1 步 · 生成自签名代码签名证书（本机 GUI）**
1. 打开「钥匙串访问」→ 菜单「钥匙串访问 › 证书助理 › 创建证书…」。
2. 名称填 `GenDesk Self-Signed`（此名即后面的签名身份，记牢，别带特殊字符/空格外的符号）。
3. 身份类型选「自签名根」；证书类型选「代码签名」；勾选「让我覆盖这些默认值」，一路「继续」到底（有效期可设 3650 天），完成。
4. 生成后它出现在「登录」钥匙串的「我的证书」里，展开应能看到配套私钥。

**第 2 步 · 导出为 .p12**
1. 在「我的证书」右键 `GenDesk Self-Signed` → 「导出…」，格式选「个人信息交换 (.p12)」，存为 `GenDesk-SelfSigned.p12`。
2. 设一个导出密码（记牢，后面作为 secret 用）。

**第 3 步 · 确认签名身份名**
终端执行 `security find-identity -v -p codesigning`，输出应含一行 `GenDesk Self-Signed`，该名称即 `APPLE_SIGNING_IDENTITY` 的值。

**第 4 步 · 生成 base64 并建 GitHub Secrets**
1. 终端执行 `base64 -i GenDesk-SelfSigned.p12 | pbcopy`（把 .p12 转 base64 到剪贴板）。
2. GitHub 仓库 → Settings → Secrets and variables → Actions → New repository secret，建三条：
   - `APPLE_CERTIFICATE`：粘贴上一步 base64 内容。
   - `APPLE_CERTIFICATE_PASSWORD`：第 2 步设的导出密码。
   - `APPLE_SIGNING_IDENTITY`：`GenDesk Self-Signed`。

**第 5 步 · 离线备份**
把 `GenDesk-SelfSigned.p12` 与导出密码存到离线安全处（如密码管理器）。丢失后无法复现同一指纹，用户需重新点一次「始终允许」。

**第 6 步 · 告知执行 Claude**
把「三条 Secret 已建好」告诉执行 Claude，它再改 `release.yml`。

### Claude 执行改动（`.github/workflows/release.yml`）
给 `tauri-apps/tauri-action` 步骤的 `env`（现有 `TAURI_SIGNING_*` 处）并列增三行：
```yaml
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
```
**不设** `APPLE_ID`/`APPLE_PASSWORD`/`APPLE_TEAM_ID` → tauri-action 只签名不公证；它会自动把证书导入临时钥匙串并授权 codesign，无需其它步骤。不动 `tauri.conf.json`。

### 验证（交付前手工回归，CI 绿不足以证明）
1. 推一个测试 tag 触发 `release.yml`，确认 macOS 构建通过、`verify-updater` 绿、产物可下载。
2. 取 CI 产物执行 `codesign -d -r- <App>.app` 看 DR；再发一个 tag，确认两次 DR **一致**（都指向该自签名 leaf）。
3. 端到端：装第一个签名版 → 启动触发读 Key → 弹窗点「始终允许」→ 安装第二个签名版（模拟更新）→ 确认**不再弹窗**。
4. 确认 Gatekeeper 首启放行流程与现状一致（自签名≠公证，无回归）。
   （本机自查可临时 `export APPLE_SIGNING_IDENTITY="GenDesk Self-Signed"` 后 `pnpm tauri build`，证书在登录钥匙串即可签。）

### 注意
- 由现状（ad-hoc）切到自签名后**第一次**更新会因 DR 变化再弹一次，此后永久不弹。
- 每个 release 都须由配好证书的 CI 产出；本地未带该证书签名的包不要拿去分发。

### 验收标准
- 两次 CI 构建 DR 一致；模拟更新后 keychain 不再弹窗（除首次切换那一次）；Gatekeeper 首启无回归；`release` 工作流产物可下载、`verify-updater` 绿。

---

## T5 — 选提示词组弹层滚动限制在列表内（纯 CSS）

**分支**：`fix/apop-scroll`　**Review**：low/medium

### 背景与根因
生成页参考图上的「指定提示词组」弹层 `.apop`（`src/features/generate/GeneratePage.tsx:526-555`）在 `src/styles/globals.css:2525` **无 `max-height`、无 `overflow`**。词组多时列表溢出，滚轮直接滚动最近可滚祖先 `.pbody`（参考图区），表现为「滚列表时背后图片区跟着滚」。

### 改动清单（`src/styles/globals.css`）
1. `.apop`（2525）补：
   ```css
   max-height: min(320px, 60vh);
   overflow-y: auto;
   overscroll-behavior: contain;
   ```
   `overscroll-behavior: contain` 防止滚到顶/底时链式滚动背后区域。
2. 顺带给共享弹窗体 `.mbody`（1828）/ `.mlist`（1833）加 `overscroll-behavior: contain`，让「选择提示词组」大弹窗也不外溢。

### 验收标准
- 左侧选多个词组使 `.apop` 列表变长，在弹层内滚轮 → 仅列表滚动，背后参考图区不动。
- 视觉与原型一致，无溢出/截断异常；`pnpm check` 全绿。
