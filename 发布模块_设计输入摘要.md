# 发布与资产管理模块 设计输入摘要

> 本文档是「发布与资产管理模块」原型设计的输入，配套阅读
> `内部图片生产工具_发布与资产管理需求文档.md`（功能与业务规则的权威来源，本摘要不重复其内容）。
> 视觉方向、技术基座、token 交付格式**完全沿用** `设计输入技术摘要.md` 第 1、2、5 节的既有约束
> （浅色优先、Linear 式克制蓝、信息密度高但有呼吸感、shadcn/ui + Tailwind + Motion 承接、
> 颜色/圆角/间距可提炼为 token），本文不再赘述。

## 0. 任务定位说明

你的任务是为两个**新增页面**（资产库、发布计划）和设置页一个新增区块做原型设计
（视觉与交互判断），与现有八页并列，风格必须与现有原型 `prototype.dc.html` 一脉相承。
这不是代码实现，也不需要遵守开发流程规范。

## 1. 一句话理解这个模块

用户每天要在多个平台发布上百个 SKU 的短视频和图文。GenDesk 负责管资产（素材/标题/正文按
SKU 分池）、每天自动生成一张发布任务单（人工审核 5–10 分钟后导出）、回收执行结果对账。
真正的发布由外部 RPA 在另一台电脑执行，**不在界面范围内**。

## 2. 核心设计对象一：SKU 与三池（资产库页）

- 主视图是 SKU 列表，每行/卡：SKU 编码（等宽字体）、款式名、冷热分层标签（热/温/冷）、
  三池余量（素材 n · 标题 n · 正文 n）、余量预警标记、最近发布时间。
  上百个 SKU，信息密度要求参照现有任务页；需要按分层/预警/停发筛选与搜索。
- SKU 详情：档案区（含**固定话题标签**编辑，最多 5 个有序标签）+ 三个池的分页签：
  - 素材池：缩略图网格（视频型带时长角标，图集型带张数角标），每个素材包带生命周期状态：
    新入库（灰）/ 可用（绿）/ 已用尽·冷却中（琥珀，显示回可用日期）/ 退役（灰、弱化）。
  - 标题池 / 正文池：文本列表，每条带平台标签（小红书/抖音/…/通用）与使用次数。
- 收件箱状态区（页面次要区域）：待认领队列（一条内容 + 「指认 SKU」操作）、解析失败清单。
- 空态与预警是重点场景：冷门款「素材仅剩 1」的预警如何在列表与详情中不嘈杂地强调。

## 3. 核心设计对象二：任务单工作台（发布计划页）

发布计划页分三个页签：看板 / 任务单 / 策略与账号。

- **看板**：今日计划、已发布、失败、待核对四个计数；按平台与账号的完成情况；
  账号健康标记（正常/停用/当日熔断）；日报入口。
- **任务单**：按日期的任务单列表（状态：草稿灰 / 已确认·已导出·回收中蓝 / 已关闭绿）
  → 点进单日工作台：任务行列表，每行 = 封面缩略图 + 标题 + 话题标签 + 平台 + 账号 +
  定时时间 + 任务状态。人工动作：换某 SKU 的当日套装、改时间、删行、增行；
  底部主操作「确认」→「导出任务包」。缺料清单作为工作台内的提示条/侧栏呈现（不是报错）。
  同一 SKU 当天所有行内容相同（日内容套装），视觉上可按 SKU 分组折叠以减少重复感。
- **策略与账号**：分层频率规则、平台矩阵、账号档案卡（名称/平台/日限额/时段/状态）、
  时段模板编辑。低频操作，密度可低于工作台。

## 4. 核心设计对象三：单任务状态（沿用 5 视觉组）

| 状态 | 视觉组 | 说明 |
| --- | --- | --- |
| 待执行 | 灰 | 初值，已导出等待执行器 |
| 已发布 | 绿 | 执行器回写成功，带发布链接与截图入口 |
| 失败 | 红 | 六类失败原因（登录失效/风控/素材不合规/页面变更/超时/其他） |
| 疑似已发 | 琥珀 | 超时无回执，**必须人工核对**，是看板上最需要被注意的状态 |
| 已取消 | 灰（弱化） | 人工取消 |

「疑似已发」的处置交互需要设计：核对后人工定态为已发布或失败，且界面要明确传达
「禁止重发」的约束感。

## 5. 导航与徽章

- 新增两个导航项：资产库、发布计划。徽章沿用现有事件驱动机制：
  发布计划 = 待确认任务单数 + 待核对数；资产库 = 待认领数 + 余量预警数。
- 设置页新增「发布与同步」区块：本机根路径、执行机根路径、查重窗口天数、回执超时、
  每日生成时间、预警阈值等表单项（低频配置，参照现有设置页样式）。

## 6. 原型范围与自定项

- 必做：资产库页（列表 + SKU 详情）、发布计划页三页签、设置新增区块、导航徽章。
- 交互自定项（设计方判断）：SKU 列表用表格还是卡片、任务工作台按时间轴还是按 SKU 分组、
  收件箱状态区的位置（资产库内 vs 独立抽屉）、看板的图表化程度。
- 不需要设计：执行器一侧的任何界面、任务单 xlsx 本身的样式。
